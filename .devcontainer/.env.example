# Copy to .devcontainer/.env (gitignored). `docker --env-file` reads it at container start.
# The devcontainer's initializeCommand auto-creates .env from this file if missing.

# --- Leptos ---
# Bind 0.0.0.0 so the forwarded port (3000) is reachable from the host browser.
LEPTOS_SITE_ADDR=0.0.0.0:3000
LEPTOS_RELOAD_PORT=3001

# --- Neon Postgres (added during architecture-spike task 5) ---
# Spike-only: the native/server path may talk to Neon directly. Never commit real values.
# DATABASE_URL is the RUNTIME connection (non-owner `app_runtime` role once rotated).
# DATABASE_URL=postgres://USER:PASSWORD@ep-xxxx-xxxx.REGION.aws.neon.tech/DBNAME?sslmode=require
#
# MIGRATION_DATABASE_URL is used ONLY by `server --migrate` (the deploy/pre-deploy
# migration step) and must be the OWNER role (needs DDL). If unset, `--migrate`
# falls back to DATABASE_URL — fine for local dev where one role does both.
# On Render: set this to the owner URL and point DATABASE_URL at app_runtime.
# MIGRATION_DATABASE_URL=postgres://OWNER:PASSWORD@ep-xxxx-xxxx.REGION.aws.neon.tech/DBNAME?sslmode=require

# --- Neon Auth (Better Auth; already provisioned per Neon branch) ---
# Non-secret, branch-specific. This is the DEV branch's auth service (local dev uses
# the Neon dev branch); Render sets the PRODUCTION branch's URL in its own env.
# base_url = the JWT issuer; the JWKS the API verifies against is
# <base_url>/.well-known/jwks.json. Var name provisional until the auth middleware lands.
NEON_AUTH_BASE_URL=https://ep-dry-cell-atj9rpc2.neonauth.c-9.us-east-1.aws.neon.tech/neondb/auth

# --- Claude Code auth (skip in-container login) ---
# Container ~/.claude/.credentials.json is lost on rebuild, so `claude` re-asks.
# Generate a long-lived token on the host with `claude setup-token` (Pro/Max)
# and set it here; claude picks it up and skips login. Treat like a password.
# CLAUDE_CODE_OAUTH_TOKEN=sk-ant-oat01-...

# --- GitHub auth (see README.md "Git / GitHub auth") ---
# GH_TOKEN=ghp_...
