# Copy to .devcontainer/.env (gitignored). `docker --env-file` reads it at container start.
# The devcontainer's initializeCommand auto-creates .env from this file if missing.

# --- Leptos ---
# Bind 0.0.0.0 so the forwarded port (3000) is reachable from the host browser.
LEPTOS_SITE_ADDR=0.0.0.0:3000
LEPTOS_RELOAD_PORT=3001

# --- Neon Postgres (added during architecture-spike task 5) ---
# Spike-only: the native/server path may talk to Neon directly. Never commit real values.
# DATABASE_URL=postgres://USER:PASSWORD@ep-xxxx-xxxx.REGION.aws.neon.tech/DBNAME?sslmode=require
