/// App-level composites over the vendored primitives — shared by pages, but
/// not registry components (no bench section; see the module docs).
pub mod holding_stepper;
pub mod palette;
pub mod query_bar;
pub mod quick_add;
pub mod states;
pub mod ui;
pub mod view_switch;
pub mod viewport;
