//! My-cards mode surfaces (specs/app-ui.md). The sidebar collection tree
//! lives in [`tree`]; the page bodies (`/my`, `/my/collections/:id`, needs,
//! shopping) land with their own Stage 3 tasks.

pub mod tree;
