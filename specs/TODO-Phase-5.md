# Project TODO - Phase 5 — execution queue

**This file is the single source of truth for what to work on next when doing Phase 5 tasks.** The selection algorithm, spec gating, and definition of done are in [README.md](README.md) ("Working the queue"). State legend: `[ ]` available · `[~]` in progress · `[x]` done.

## Phase 5 — app UI

Replace the scaffold counter with the real app: the nine wireframed screens, the
shell, and the three custom gap components, built page by page so every task
ships independently through the merge gate. Design authority is the Phase 1b
artifacts (wireframes.pen, information-architecture, add-flow-prototype,
command-palette, component-gap-analysis) distilled into
[app-ui](app-ui.md); the per-task agent loop (orchestrator dispatches: an
implementation subagent builds + verifies platforms + authors e2e → an
adversarial-review subagent reviews code and test strength → validate → land;
revised 2026-07-25, fully Claude-driven) is [ui-work-loop](ui-work-loop.md).
The full three-browser e2e tier runs at the end of **every** task (revised
2026-07-20); stage boundaries (marked below) additionally carry the Android
release smoke at phase end.
UI work stays on the ~3K-printing POC catalog (maintainer decision
2026-07-17); quick-add realism / perf findings are recorded as deferred.

### Stage 0 — loop infrastructure

- [x] Android e2e spike: prove/disprove automated e2e against the Android emulator (Tauri debug webview remote debugging → `adb forward` + Playwright `connectOverCDP`, or Playwright's experimental `_android` API); record the outcome in ui-work-loop Findings and fix the loop's platform matrix per its decision tree (specs: [ui-work-loop](ui-work-loop.md)) — **PASS, decision-tree path 1**: `connectOverCDP` fully drives the debug webview (asserted counter increment + dark-toggle on device; live dev-server content proxied behind `tauri.localhost`); web + Android e2e every task, one release smoke at phase end; recipe + constraints in ui-work-loop Findings, probe `end2end/android-cdp-check.mjs`
- [x] Work-loop skills + permissions: `.claude/skills/ui-task-loop`, `e2e-suite`, `android-smoke` (+ `scripts/smoke-android.sh`), shaped by the spike outcome, mirrored into `.agents/skills/`; permission allowlist added to `.claude/settings.json` (diff surfaced for maintainer approval) — prereq: the spike (specs: [ui-work-loop](ui-work-loop.md)) — **done**: three skills landed with matrix path 1 baked in (Codex command names verified against the installed plugin), `smoke-android.sh` for the phase-end release smoke, mirrors byte-identical, allowlist exactly as specced (flagged in the PR for maintainer review); drive-by: realigned the stale `validate` skill with validate.yml (native-backend line, hosted bench)
- [x] E2E baseline reset: delete the counter suite; pre-seeded verified test user on the Neon dev branch + login fixture (email verification is ON — naive signup hangs on OTP); tier tags (`@fast` chromium per task, full chromium+firefox+webkit at boundaries); keep the `.mjs` probes as the probe layer (specs: [ui-work-loop](ui-work-loop.md)) — **done**: tiered config + storageState login fixture + baseline smoke (SSR asserted request-level); `seed-e2e-user.sh` idempotent from any state (fresh-checkout recreate verified); fast tier 4/4, full tier 7/7; Codex review 3 fixed / 1 disputed; found the release-build→watch asset-clobber hydration trap (e2e-suite skill + Findings)
- [x] Dev seed data: script building the test user's realistic tree via existing `CollectionStore` methods (Inbox + nested binders + a deck with commander/boards + holdings/desires across the POC catalog) — prereq for any `/my/*` screen (specs: [app-ui](app-ui.md)) — **done**: `app/src/seed.rs` (debug+hosted only) + `server --seed-dev <uuid>` + `scripts/seed-dev-data.sh`; every write through the real store methods (search→detail→first printing); sentinel idempotency + cleanup-on-error; ran against dev (5 collections / 29 copies / 4 desires / 21 moves / commander tag); Codex review 4 fixed (incl. release-binary exposure → debug_assertions gate) / 1 disputed (see app-ui Findings)
- [x] Dark palette + token migration: `.dark` OKLCH block in `style/input.css`; migrate hardcoded-hex pages (home remnants, auth_pages) onto tokens; vendor `theme_toggle`; class-on-`<html>` + persistence per the theme-persistence decision (specs: [app-ui](app-ui.md)) — **done**: full Rust/UI token set both modes, dark default via SSR-read `tr_theme` cookie on `<html>`, theme_toggle vendored (cookie-initialized both sides — no flash) + live bench section, HomePage/auth_pages hex-free (bounce-page raw HTML exempt); verified web (6/6 probe, hydration clean, fast tier) + Android webview on-device; Codex review 3/3 accepted+fixed (app-ui Findings)

### Stage 1 — component vendoring (vendor-component skill; bench section same commit)

- [x] Vendor batch V1 — static primitives: `button`, `input`/`input_group`, `badge`, `kbd`, `separator`, `checkbox`+`label`, `toggle_group`, `breadcrumb`, `skeleton`, `card` (specs: [app-ui](app-ui.md), [ui-components](ui-components.md)) — **done**: 11 components + bench sections in one commit; `variants!` hand-expanded (no leptos_ui/paste), undefined-token variants dropped, icons inlined, label peer-class upstream bug fixed as deviation; verified web (bench-check CLEAN incl. new interaction asserts, fast tier 4/4) + Android on-device; Codex review: 4 refuted with SSR evidence, 3 fixed, 2 disputed (app-ui Findings + gap-analysis adoption notes)
- [x] Vendor batch V2 — overlay foundations: `use_scroll_lock` asset, `dialog`, `popover`, `sheet`; deterministic caller-supplied IDs (no `use_random_id`), Leptos-owned open state, ESC-listener cleanup; verify CSS anchor positioning on the Android webview, JS fallback if unsupported (specs: [app-ui](app-ui.md), [ui-components](ui-components.md)) — **done**: scroll_lock as pure-Rust (refcounted + generation-guarded), dialog/popover/sheet with caller IDs + one-signal open state + overlay-stack topmost-ESC + inert/aria; anchor positioning confirmed on-device (Android Chrome 145) with a JS fallback for engines lacking it; bench-check extended (stacked-ESC, scroll-lock body styles, ID stability); Codex review 8/10 fixed (app-ui Findings)
- [x] Vendor batch V3 — `command` (rewired fully reactive — the shared core of quick-add / destination picker / ⌘K), `hover_card` (hover-intent timers in Leptos), sonner per the engine decision (specs: [app-ui](app-ui.md), [ui-components](ui-components.md)) — **done**: `command` reactive item-registry (filter Memo + ↑↓/⏎ nav, no JS script), `hover_card` shared-timer hover intent, `sonner` as a native Leptos toaster (programmatic `ToastHandle::show` + undo action) per the maintainer decision; bench-check extended (reactive filter+keyboard, toast+undo, hover handoff); verified web + Android on-device; Codex review 3 real bugs fixed (hover handoff, highlight clamp) / sonner cleared / order-caveat documented (app-ui Findings)

### Stage 2 — shell & public surface

- [x] App shell + routing: `/` auth redirect, route skeletons for the full map, top-bar mode switch, sidebar rail frame, mobile bottom tabs, `/my/*` auth-guard wrapper; delete the counter + `get_count`/`increment_count`/`storage`; fold the `/cards` placeholder into the shell; rewrite the shell e2e smoke — **stage boundary** (specs: [app-ui](app-ui.md)) — **done**: shell.rs (top bar + mode switch, rail frame, bottom tabs, user-menu popover, route skeletons), SSR 302 redirects (`SsrMode::Async`) + `data-ssr-path` webview shim (the Tauri proxy swallows 302s — hydration panic reproduced + fixed on-device), shared current-user resource with full-page auth transitions, `?next` honored (open-redirect hardened); Codex review 1 high fixed / 1 medium deferred to queue / 6 clean; full three-browser tier 28/28 + Android anon surface on-device (authed flows blocked by dev-proxy body/cookie stripping — ui-work-loop Findings); e2e mutation pass: 10/10 kills
- [x] Catalog page `/catalog`: `search_catalog` server-fn adapter (the exemplar) over `CatalogStore::search`; query bar with URL-canonical `?q=` state, debounce + stale-discard, SSR first page; grid/list toggle; card tiles (lazy images, skeletons) + list view on the vendored table; anonymous variant with sign-in prompts on quick actions (specs: [app-ui](app-ui.md), [catalog-search](catalog-search.md)) — **done**: `app/src/catalog.rs` + the `search_catalog` adapter shipped as a **GET** server fn (`GetUrl` codec — the Tauri dev proxy strips POST bodies, so a POST read is unverifiable on-device; verified working through it), opportunistic auth reusing `routes::catalog_backend`; URL is the sole query state with per-session (not per-keystroke) history; debounce closed at 250 ms and stale-discard *verified* as reactive_graph's version counter rather than hand-rolled; `?view=list` for SSR-correct layout; roving focus landed on `toggle_group` (V1's parked item) via a new `tabindex` prop + bench assertion. Codex review 2 fixed (errors blanked results — probe-confirmed; URL⇄field clobber — latent, hardened) / 1 accepted-as-documented / 1 disputed→queued; e2e mutation pass 11/11 kills, which exposed 2 vacuous tests. Verified web (SSR, hydration CLEAN ×4, bench CLEAN, fast tier 25/25) + Android webview 8/8 on-device. Paging (`?cursor=`) and the server-fn status-semantics gap queued as follow-ups
- [x] Filter rail + query↔rail sync: rail widgets two-way-bound to the grammar (comma-OR serialization; unrecognized terms preserved verbatim); mobile slide-over sheet with active-filter badge (specs: [app-ui](app-ui.md), [catalog-search](catalog-search.md)) — **done**: query grammar moved to `shared/` (the rail edits it in wasm) with `parse_tokens` keeping raw text so unowned terms survive byte-for-byte; one `owns_every_match` rule shared by read/rewrite; `t:` gained comma-OR (grammar + `ILIKE ANY`, deviation recorded in catalog-search); `<details>` sections, mobile sheet + badge; fixed an SSR regression where `bind:value` rendered no `value` attribute (the query bar included); added a `data-hydrated` test seam that also fixed 8 pre-existing firefox/webkit failures; needed `#![recursion_limit = "512"]` for aarch64-linux-android. Verified: unit 20/20 + 22 in shared, e2e 118/118 three browsers x3, Android on-device CDP check, Codex mutation pass 10/10 caught (app-ui Findings)
- [x] Card detail `/cards/:id` + previews: full page (printings, rulings, your-copies when authed), desktop `hover_card` preview, touch bottom-sheet with expand; includes the multi-face image projection fix (`COALESCE` with `faces->0`) in the hosted summary/detail projections (specs: [app-ui](app-ui.md)) — **done**: `app/src/cards.rs` (detail page + the `CardPreview` composite) behind a `card_detail` GET server-fn adapter with opportunistic auth; the route needed `SsrMode::Async` — under the default out-of-order streaming the page shipped a skeleton with the content in a `<template>`, and the SSR test couldn't tell (now asserts the skeleton's absence). The projection fix went to **all six** sites, not the two the spec named — 128/128 DFC printings in the POC catalog resolve art, zero working rows touched. `hover_card` gained a `disabled` prop (+ bench section) because touch fires a synthetic `mouseenter`, so a tap opened both affordances; review found the disable must also cancel the pending timer. Preview bodies mount lazily — eager rendering duplicated every card name into the DOM and broke three pre-existing catalog tests. Grid tiles opt out of hover (deviation: the tile is already the art). Mutation pass 5/5 kills, two only after rewriting vacuous assertions (a tap-based `disabled` test that passed for an unrelated reason, and `toBeVisible()` on a sheet that stays in layout when closed). Full three-browser tier 148/148, Android real-webview probe 11/11, bench CLEAN, gate green. Codex step-2 could not be read in an autonomous run — substitute reviewer used, gap filed (see Findings)
- [x] Destination picker + Want/Have quick actions + undo toasts: sticky `popover`+`command` picker on catalog surfaces, add adapters over batch-add, sonner undo wiring — **stage boundary** (specs: [app-ui](app-ui.md)) — **done**: `app/src/catalog/destination.rs` (picker + shell-level state, `tr_dest` cookie following `theme_toggle`'s SSR-readable reasoning) + `QuickAddButton`/`raise_add_toast` in `app/src/catalog.rs` + `quick_add`/`undo_quick_add` adapters. Three framings did not survive contact: **batch-add can't back it** (`Vec<LineResult>` carries no ids, so no undo handle) — a Have routes through `move_cards` with `from = None`, the intake form of a move, which is the only call that *returns* the move id undo targets; **`+ Want` is not undoable at all** and its toast ships without the action rather than lying (desires are outside the move ledger and no desire-quantity op exists — queued as a follow-up); and **`CardSummary` needed `printing_id`** because holdings are per-printing while a catalog row is per-oracle — both per-oracle projections now share one `REPRESENTATIVE_PRINTING_JOIN` lateral returning the same printing the old image-only subquery did. The `command` mount-order caveat was **discharged, not carried**: the picker sorts its data before any item mounts and typing hides rather than reorders, so registration order still equals document order — no `compareDocumentPosition` needed (caveat stands for quick-add and ⌘K). Codex review 2/2 accepted and fixed (stale destination after a rename/delete — the module doc was contradicting its own code; and a caller-supplied `AddLine` that let `quantity: 20` through an endpoint meaning "one copy" — severity argued down from medium, since `moves` RLS is `ENABLE`+`FORCE` and the same caller can already reach the machine route, but fixed anyway). Mutation pass **6/6 kills** after strengthening three assertions the analysis exposed — the undo test passed with `undo_move` stubbed out, and now brackets the add with real `collection_view` reads (`present` n→n+1→n). Verified: unit 84, full three-browser tier 175/175, hydration CLEAN anonymous *and* authed (added `hydration-check-authed.mjs` — the anonymous probe can't see a session-gated component), bench CLEAN, Android dev-attach 8/8 on the real webview (anonymous surface only, per the fixed platform matrix)

### Stage 3 — My-cards surface

- [x] Collection tree, read-only: nesting, per-node collapse, pinned system rows, selection, rolled-up count badges; tree server-fn adapter (specs: [app-ui](app-ui.md)) — **done**: `app/src/my/tree.rs` (pure client-side assembly — Inbox pin, sibling order, orphan/cycle defense, unit-tested — plus `CollectionTreeNav`) over a new one-round-trip `CollectionStore::collection_tree` read (own-present per collection + shopping-short badge; lazy Inbox via a shared `ensure_inbox`; hosted SQL + native client + `GET /api/collections/tree` + GetUrl adapter), rendered in the shell rail with pinned All-cards/Shopping-list rows, per-node collapse on the newly vendored `collapsible` (+aria, +inert-when-closed) with `item` rows (variants! hand-expanded, href arm), prefix-match selection, and the mobile tab badge off one shared shell resource refetched by quick-add/undo. Codex review 2/2 accepted+fixed: the reparent endpoint now rejects the Inbox (409 verified live — a nested Inbox defeated the pin), and selection survives a collection's `/needs` subpage. Verified: SSR authed curls, hydration CLEAN anon+authed, bench CLEAN (new sections asserted), full tier **196/196**, Android webview probe PASS, mutation pass **7/7 kills** (app-ui Findings)
- [x] Collection tree, management: drag reparent/reorder, context-menu create/rename/delete with dialog confirms, cycle-guard error surfacing (specs: [app-ui](app-ui.md)) — **done**: `app/src/my/tree_manage.rs` (shared context menu, three confirm dialogs, drag commit via a pure unit-tested `plan_drop`) + drag/menu wiring in `app/src/my/tree.rs` + newly vendored `context_menu` (rewired to `popover="manual"` with our own outside-pointerdown/ESC dismissal + a deferred, generation-guarded open — `popover="auto"`'s light-dismiss races the right-click across engines) + five thin server-fn adapters in `app/src/lib.rs`. Cycle guard is client-first (dragged node's subtree undroppable, no request sent) with the unchanged 409 as backstop; the e2e pins both. The `ContextMenu` provider had to move inside the `Suspense` (a provider above the async boundary doesn't reach `use_context_menu()` inside it). Codex review 4/4 resolved (delete-confirm now snapshots its subject — high; generation-guarded deferred open — med; honest partial-commit toast — med; position-collision documented + queued). Mutation pass 12 analyzed → 2 real test gaps strengthened and kill-verified (bench outside-click, cycle-guard no-request). Verified: unit 81, SSR authed markup, hydration CLEAN anon+authed, bench CLEAN, full three-browser tier **226/226**, Android webview `android-tree-manage-check.mjs` PASS on Chrome 145, gate green. Queued two follow-ups: keyboard/touch move path (drag is mouse-only) and the fractional-index rebalance (app-ui Findings)
- [x] Android release auth check: verify sign-in + an authed page work in the release APK — the dev proxy strips POST bodies and Cookie headers, so the release protocol handler's behavior is unproven (specs: [ui-work-loop](ui-work-loop.md)) **done**
- [x] The merge gate's `cargo leptos build --release` clobbers `target/site/pkg` under a running debug watch server, hydration-panicking every page until the watch is restarted; a source touch does not reliably fix it. Currently only a documented workaround in the e2e-suite skill. Give the gate its own `--target-dir` so verifying-after-gating stops costing a restart (specs: [ui-work-loop](ui-work-loop.md)) — **done**: `cargo leptos build` has no `--target-dir` flag, so `site-root` became the `CARGO_TARGET_DIR/site` marker (resolves to `target/site` by default — watch/Docker/Tauri unchanged) and the gate now runs `CARGO_TARGET_DIR=target/gate cargo leptos build --release`, writing its site to `target/gate/site`. Verified live: full gate green (8/8, macOS host incl. `three_rings`) while a watch served :3000, `target/site/pkg` byte-identical before/after, `/login`+`/dev/components` hydrated CLEAN with no restart. Only the release step is isolated (clippy/test keep sharing `target/` for dep reuse); CI unchanged (no watch there; preserves rust-cache paths). Docs updated: validate skill (+`.agents` mirror), CLAUDE.md/AGENTS.md, e2e-suite clobber note (see ui-work-loop Findings)
- [x] Re-add the component variants V1 dropped for want of tokens (`button` Warning/Success/Bordered, `badge` Success/Warning/Info) together with the `style/input.css` tokens they need — the polish task's error/empty states are the screens that want them (specs: [app-ui](app-ui.md), [ui-components](ui-components.md)) — **done**: full upstream `success`/`warning`/`info` token families (both modes) + `@theme` mappings + bench-panel mirror; **four value deviations for WCAG AA** — upstream's light bases are 2.9–3.3:1 under white text, and `hover:bg-*/90` alpha-composites over the page so the base needs hover headroom (light bases → 0.48 L, dark warning → 0.67, dark info-foreground → dark text; computed in gamma-encoded sRGB, Codex-recomputed to four decimals, every enabled pair ≥ 4.5) — plus `Bordered` on the token border instead of upstream's hardcoded zinc. bench-check gained token-variant assertions (bg/text emission, family distinctness, Bordered currentcolor guard); mutation pass 4/4 kills. Full tier 226/226, Android webview variant drive PASS on-device (see app-ui Findings)
- [x] Decide Playwright-in-CI before marking ui-work-loop implemented — the spec's open question is deferred "to phase end" but the polish task doesn't carry it, so it would expire unanswered (specs: [ui-work-loop](ui-work-loop.md)) — **decided: no** — the e2e tiers stay a local-loop precondition for `[x]`; the gate stays creds-free. Four grounds (standing no-Neon-creds rule; hosted-auth fixture dependency; concurrent-run write races on the seeded e2e user; per-push gate latency), the technically-workable-but-declined anonymous `--no-deps` subset, and explicit revisit conditions all recorded in ui-work-loop Findings + the Decisions log. Codex adversarial review of the decision: 4 findings, 3 accepted (rationale de-overstated ×2, stale playwright.config.ts tier comment fixed), 1 no-change
- [x] Count stepper (custom): hover/focus-revealed `− n +`, click-to-type, keyboard ±, commit-on-blur; optimistic update + undo toast; standalone bench section — **done**: `app/src/components/ui/count_stepper.rs` (composes vendored `Button`+`Input`; caller-owned `value` signal, optimistic `on_commit(from,to)` fired last, undo toast re-commits through the same channel) + bench section (`app/src/bench/count_stepper.rs`: happy-path + failing-save steppers). Three lifecycle traps solved: blur-commit is **deferred** (the display⇄edit swap fires a spurious `focusout` — immediate commit would close edit mode the instant a click opens it; removing the defer breaks click-to-type, mutation-verified), the ± buttons `preventDefault` pointerdown + programmatically anchor focus (WebKit doesn't focus a clicked `<button>`), and the edit field is the vendored `Input` so the `value` attribute is seeded. Codex review 4/6 fixed (deferred-commit disposal guard, read `label` before `on_commit`, edit-mode `max` clamp, inert ± at bounds) + 2 addressed in e2e; mutation pass strengthened commit-cardinality + optimistic-first assertions (2 gaps kill-verified), min-bound dead-session gap documented as unobservable/harmless. Verified: hydration CLEAN (anon+authed), bench-check CLEAN, `android-stepper-check.mjs` PASS on Chrome 145, full three-browser tier 250/250, gate green. Wiring into the collection view rides `/my/collections/:id` (app-ui Findings) — **build click-to-type on the vendored `Input`, not a raw `<input>`**: `bind:value` is a client-side binding that emits no `value` attribute, so a hand-rolled SSR'd field renders empty until wasm lands. `Input` seeds the attribute from `bind_value` itself (PR #43); a bare element re-inherits the trap, and it is invisible in review because the field merely looks empty for a beat (specs: [app-ui](app-ui.md))
- [x] Double-faced cards have no way to see the back face. `/cards/:id`, the hover preview and the touch sheet all render face 0 only, and the heading shows the combined `"Front // Back"` name — so the back is unreachable in the UI. Needs a flip control on the card art that swaps **both** the image and the oracle block (name / mana cost / type line / text / P-T). Scope note from the card-detail task: `CardDetail.card_faces` already carries the per-face *oracle* data as raw jsonb, but the **back-face image is not plumbed through** — `PrintingSummary.image_uri` flattens to a single string, while the `printings.faces` jsonb array holds an `image_uris` per face. So this needs a DTO change (a per-face image list) plus the projection to fill it, not just a UI control. Applies to `transform` / `modal_dfc` / `reversible_card`; `split` / `flip` / `adventure` / `aftermath` are one image and want rotation or nothing, so key the control off `layout` rather than "has more than one face". **No ingestion work needed** — verified on the dev POC catalog that both faces' `image_uris` are present in `printings.faces` and per-face names/text in `cards.card_faces`; it is entirely projection + DTO + UI (specs: [app-ui](app-ui.md), [data-model](data-model.md)) — **done**: `shared::BACK_FACE_LAYOUTS` (widened to `double_faced_token`/`art_series` — same per-face art in the data, same defect) drives one gate across SQL (`summary_select()` CASE), the server-side `CardSummary.faces`/`CardDetail::flip_faces()` builds (both fail closed), and the UI; `PrintingSummary.face_image_uris` + `CardFaceSummary` DTOs (`serde(default)` for version skew); `FlipButton` on the detail hero and inside both preview bodies swaps name/mana/type/oracle/stats **and** art per face, combined name kept as a subtitle. Codex review: zero findings; mutation pass 4/4 kills after it exposed the art-pairing gap (tests only checked "src changed" — now exact equality in previews, `/front/`↔`/back/` path on detail). Verified: shared units 26, hydration CLEAN anon+authed, full tier **265/265**, `android-dfc-check.mjs` 12/12 on-device, gate 8/8 (app-ui Findings)
- [x] `/my` All cards view: aggregate table with three-count columns + expandable location summary, quick search, keyset paging (specs: [app-ui](app-ui.md)) — **done**: `app/src/my/all_cards.rs` over a reworked `CollectionStore::all_cards(q, page)`. Three framings changed on contact: the everything-view was **missing every card you only want** (`all_cards` inner-joined `holdings`, so a desired-but-held-nowhere card existed on the shopping list and nowhere in `/my` — now `held FULL OUTER JOIN wanted`); "same row treatment as collection view" is enforced by rendering the **same DTO** (`AllCardsRow` carries a whole `CardSummary`, so the row reuses `CardPreview`/flip and the SQL reuses a new `summary_select_with()`), with `owned`/`in_collections` *removed* as fields and derived instead so no stored copy can contradict `locations`; and the spec's `7 across 3 collections` phrasing only works plural, so the WHERE column has three shapes (`—` / `3 in Trade Binder`, linked / the expandable summary). `NeedLocation` → `CardLocation` (identical shape, two callers). `/catalog`'s query bar was extracted to `app/src/components/query_bar.rs` and shared rather than copied — four separately-earned behaviors (debounce, `self_pushed` URL⇄field guard, per-search-session history granularity, timer cleanup). Quick search is a plain name substring, not the catalog grammar, but through the same escaping helper (`?q=%` matches nothing). Paging is forward-only by design (Back walks the pages; the pager adds "Back to the start"), and a search edit drops the cursor. **The dev seed grew a bulk box** (`app/src/seed.rs`, own sentinel, 60 cards, skips whatever is currently short) because at ~19 distinct cards against a 50-row page the "Next page" link was unreachable by any browser test. Codex review: **zero findings in the feature code**, 3 in the tests, all 3 fixed (WANTED verified against itself → now cross-checked against the shopping list's independent projection; Next-page never clicked → now clicked; the paging probe unregistered → `probe:*` npm scripts + e2e-suite skill). Verified: unit 85, full three-browser tier **301/301**, paging probe at limits 3 and 7, hydration CLEAN anon+authed (incl. `?q=` and `?cursor=`), bench CLEAN, `android-all-cards-check.mjs` 10/10 on Chrome 145, gate green (app-ui Findings)
- [x] `/my/collections/:id` binder/deck view: header counts + needs chip, folder rows above cards, deck variant (format/commander header, type groupings, Want-led default, teardown), stepper in place, mobile drill-down (specs: [app-ui](app-ui.md)) — **done**: `app/src/my/collection.rs` (binder + deck variant on one page — breadcrumb, mobile drill-down that walks *up the tree*, needs chip, commander card with derived colour identity, folder rows above cards sharing the three numeric columns, `CountStepper` in HERE, deck type/board sections with slot counts, Want-led add default, teardown dialog, in-collection quick search and keyset paging) over a reworked `CollectionStore::collection_view` — which **returned no desire-only rows** until this task (inner join on `holdings`, so a card a deck wants and doesn't hold didn't exist in the view while the chip counted it; same `FULL OUTER JOIN` fix `all_cards` needed), plus `totals`, `commanders`, `holding_id`, `faces` and `q` threaded through trait / native client / router. Two adversarial review rounds, 8 findings then 6. Round 1's **blocker**: committing HERE to 0 ran `DELETE FROM holdings` while the stepper's own Undo re-POSTed the dead id — success toast, copies gone; floored at `min=1`, with undoable removal filed for the move-flows task. Also fixed: a tree refetch remounting stepper rows and silently disarming Undo, a URL-keyed optimistic delta that double-counted the header after teardown, and three render rules that were **unfalsifiable against the seed fixture** (no rollup, no nested folder with children, no repeated printing — fixed in `app/src/seed.rs` with a guarded `Depth Box → Depth Shelf → Depth Drawer` block). Round 2's needs-chip finding **disputed and upheld** (the IA puts the chip on "a deck or collection header"; the spec's deck-variant bullet was the line that read wrong, corrected). Verified: merge gate all 8 steps green, `cargo test --workspace --exclude frontend` 123 passed, hydration CLEAN on 7 URLs, SSR curls on binder + deck, Android probes 7/7 + 6/6 + CDP PASS, full three-browser tier **367/367**. Round 2's remaining findings parked as Phase 5 discoveries by maintainer decision on loop cost (app-ui Findings)
- [x] Quick-add panel: `command`+`popover`+`input`+`kbd` composite; keystroke contract `↑↓ ⏎ ⇧⏎ ⌥⏎`; Want-led in decks, Have-led elsewhere; typeahead adapter; e2e asserts the keystrokes; time-to-enter-50 observations recorded (POC-catalog realism deferred) (specs: [app-ui](app-ui.md)) — **done**: `app/src/components/quick_add.rs` wraps the page's `QueryBar` in a `Command`, with a pure `decode(key, shift, alt, rows, counting) -> Action` keystroke contract (12 unit tests), candidates from `search_catalog` keyed on the committed `?q=`, and an `IN THIS COLLECTION` context section folded out of the collection read. Supporting seams: `use_command_nav()`/`CommandNav` lets a **foreign** input drive `command`'s registry, `QueryBar` gained `on_key`/`reset`, and `quick_add` gained a server-clamped `quantity` (`clamp_quick_add_quantity`, `QUICK_ADD_MAX = 99`) because `⇧⏎ set count` would otherwise need one undo per copy. **The `command` ordering caveat needed no fix** — the list is rebuilt inside a `Suspend` per query, the full-remount case the caveat exempts. **Deviation: not the vendored `popover`** — `popover="auto"` light-dismisses on the same `pointerup` that focuses the field, and the router detaching this route's subtree hides a showing popover *without* firing `toggle`, desyncing the `open` signal; an absolutely-positioned panel with hand-rolled Escape/outside-pointerdown has neither problem, and the destination picker keeps using `popover`. **time-to-enter-50: 50 cards / 350 keystrokes (7.0 per card) / 1 pointer action / 97.3 s (1.95 s/card)**, of which typing is 0.36 s — the rest is the 250 ms debounce plus round trips. One review round, **zero majors**, 12 minors filed below. Verified: gate all 8 steps green (135 tests), hydration CLEAN on 3 anon + 5 authed URLs, bench probe CLEAN, SSR curls on binder (`⏎ Have`) and deck (`⏎ Want`), full chromium tier **131/131** (8 new), Android CDP + `android-quick-add-check` PASS (app-ui Findings)
- [x] Selection tray (custom gap component), read-only: docked thumbnail stack + count + "Move to…" (inert) + clear; cross-view selection state that survives mode switches and navigation; mobile docking above the tab bar; bench section in the same commit. **No writes** — the tray collects a selection and renders it; the move it triggers lands in the next task (specs: [app-ui](app-ui.md)) — **done**: `app/src/components/ui/selection_tray.rs` (the pill) + `shell::SelectionTrayDock` (the fixed positioning), splitting the wireframe's own Tray Wrap / Selection Tray frames so the bench can render the pill inline. The load-bearing decision is the **enum** selection key — `Held { collection_id, printing_id, board }` for collection rows vs `Card { oracle_id }` for `/my` rows, because a `/my` row aggregates by oracle and `CardSummary::printing_id` is only a has-art-first *representative*. Chosen over `from_collection_id: Option<Id>` on purpose: `None` in `MoveItem` means external intake, so an Option key would let batch move conjure copies; **the enum makes that a compile error and hands batch move a required resolution step**. `board` rides along even though `move_cards` is hardcoded to `'main'`, so mainboard/sideboard rows stay 1:1 with their checkboxes. Selectable where copies exist (`present > 0` / `owned > 0`); desire-only rows get no checkbox. Cross-view survival verified structurally — `provide_selection()` runs exactly once in `AppShell` above the whole routed subtree, and `Checkbox` is fully controlled, so the `/my/collections/:id` DOM churn re-derives rather than resets. Review CLEAN, zero majors; nine minors filed below, three of them severity-checked by the orchestrator rather than accepted on the reviewer's call. A reported reproducible `smoke.spec.ts:92` timeout **did not reproduce** — 138/138 in 52.3 s at default workers — so the proposed config change was dropped and filed as a suspected flake. Verified web (e2e 138/138, hydration CLEAN ×4, SSR non-empty ×3) + Android 12/12 on-device
- [x] Batch move: wire the tray's "Move to…" to `move_cards` over `suggested_destinations` — destination picker reused from the catalog quick actions, batch write, one undo covering the whole batch (specs: [app-ui](app-ui.md), [collection-api](collection-api.md)) — **done**: `app/src/my/move_selection.rs` + a `move_selection` server fn, writing through the existing `CollectionStore::move_batch` (collection-api's actual "Move (batch)", one transaction, all-or-nothing). Picker **extracted, not forked** — `catalog/destination.rs` now exports `DestinationList`/`DestinationOption`/`DestinationChoice` for both call sites, with tray options folded across the selection (`selection_destinations` sums shortfall per collection → `wants 2`) and ranked ahead of the rest. One undo for the batch needed a new `undo_moves(Vec<Id>)` trait method: the ledger has no batch id, so looping the per-move undo would be N transactions with a partial-revert failure mode — the defect shape this repo already hit. **Review found one major, promoted from the implementer's own "annoying" follow-up**: `present` groups by `(printing_id, board)` so a foil-only row is selectable, while `holding_take` matches the full grain — the entry returned `Conflict` and the single transaction killed the *entire* batch behind an error naming no card, on data present in dev today. Fixed by **refusing, not widening the write**: new ungrouped read `holdings_of_oracle`, resolution as pure functions deciding *movable* before any `MoveItem` exists, and named per-entry `Grain`/`Board`/`NoCopies` refusals that stay checked while the rest of the batch moves. Mutation-verified (reverting `movable` fails both new tests with the reported whole-batch death). The third task's fence held — no grain on `MoveItem`/`CardRow`/`holding_take`. RLS confirmed `FORCE`d on `moves`/`holdings`/`collections`, so the new undo route cannot reach another user's moves. Verified web (e2e 143/143 at `--workers=1`, hydration CLEAN, before/after data read back through the API) + Android 16/16 on-device
- [x] Undoable removal + deck teardown: `move_cards(to = None)` addressed by **grain** — needs finish/condition/language on `CardRow` (or a grain-addressed write) and a `board` on `move_cards`/`holding_take`, both currently hardcoded to `'main'`. Then drop the count stepper's `min=1` floor (it exists only because committing 0 ran `DELETE FROM holdings` behind a success toast) and add the deck "Empty deck…" teardown — single destination or "Return to previous locations". **Carries the trait/backend work of the three; the binder-removal discovery below is this task's real acceptance criterion** — **stage boundary** (specs: [app-ui](app-ui.md), [collection-api](collection-api.md)) — **done**: `migrations/0009_move_boards.sql` puts `from_board` **and** `to_board` on the ledger (a move's ends need not agree, and undo must reverse both exactly), and new `CollectionStore::move_holding` is addressed by **holding id**, resolving grain/board/collection/quantity off that row `FOR UPDATE` **inside the write transaction** — which also closes the row-path TOCTOU batch move had to leave open. The `min=1` floor is gone and the defect behind it is unreachable, not re-routed (`set_holding_quantity(id, 0)` now has no UI caller). Undo re-creates the holding with a **new id**; the row refetches via `HoldingsRevision`, and a stale id errors visibly rather than touching another row. Latent bug fixed in passing: `add_holding`'s intake move recorded no board, so undoing a `+ Have` onto a sideboard took copies off the mainboard. Batch refusals became capabilities — `SkipReason::Board` is gone; `Grain(n)` is deliberately kept for a stack with several grains and no default one. **Seven mutations, seven deaths**, including the two silent-corruption cases (undo ignoring the ledger's board; the write using the default grain). Review: **zero code majors**, one *operational* blocker — the deploy ordering below. Verified web (e2e 149/149 at `--workers=1`, hydration CLEAN, round trips read back through `/api/cards/{id}/holdings`) + Android stepper/collection probes green. ⚠️ **`scripts/migrate.sh prod` must run before this merges** — the new code names `moves.from_board`/`to_board` in three places and cannot run against the unmigrated schema; reads are untouched, so an unmigrated prod would look alive while every write 502s
- [x] Needs view + pick list + `/my/shopping`: owned-elsewhere vs short buckets, Pull / Pull-all → pick list grouped by source (client-composed from `move_cards` + `suggested_destinations`), shopping list page with text export (specs: [app-ui](app-ui.md)) — **done**: `app/src/my/needs.rs` + `app/src/my/shopping.rs`. **The task line's composition was wrong and the spec is being corrected, not the code** — `suggested_destinations` ranks *destinations* for a card, but here the destination is the page itself, so the read needed is source-side; shipped as `needs` + `holdings_of_oracle` + `move_batch`. **Quantity is never the caller's**: `PullItem` carries no quantity at all, and `pull_needs` re-runs the same allocation over its own fresh RLS-scoped read, accepting only *which* (card, source) lines to apply; a gap closed since render refuses as the new `SkipReason::NoLongerNeeded`. Review proved the pick-list walk is **order-independent** (locations sorted quantity-desc ⇒ draining one line leaves the others' greedy shares unchanged), and that the buckets cannot diverge from the needs chip (identical CTEs; `sum(allocate(gap, locations)) == owned_elsewhere` is an identity). **Needs stays board-blind, deliberately**: a board-aware need would manufacture rows whose Pull cannot work, since moves always land `to_board = main` — pinned by an e2e that fails if `needs()` is made board-aware. Review CLEAN, zero majors; ten of twelve minors filed, two fixed (the empty state's unqualified "Nothing missing" claim, and a hole where duplicate `PullItem`s multiplied a pull). Verified web (e2e 154/154 at `--workers=1`, hydration CLEAN, pull round-trips read back through `/api/cards/{id}/holdings`) + Android probe green including a real webview tap. ⚠️ Stacked on the removal/teardown PR — inherits its `scripts/migrate.sh prod` prerequisite
- [x] Catalog paging beyond the first page: `?cursor=` in the URL + a load-more/next-page control on `/catalog` (the search adapter already takes a cursor; the page only ever renders page one) — deferred from the catalog-page task (specs: [app-ui](app-ui.md), [catalog-search](catalog-search.md)) — **done**: `catalog_url` gained an always-explicit cursor parameter, the results `Resource` is keyed on `(url_q, url_cursor)`, and a forward-only `Pager` (Next + "← Back to the start") mirrors `/my`'s shapes. **Cursor clearing is a property of the URL the existing writers build**, not a new writer watching `q` — review's exhaustive grep found exactly three call sites navigating to a `/catalog` URL with a query (`QueryBar::commit`, `rail::use_navigate_query`, `ViewSwitch::go`), the first two passing `None`, with `use_commit` and `reset_all` both funnelling through `use_navigate_query`; no path can change `q` while keeping `cursor`. The **view switch preserves the cursor** deliberately (relayouting is not a query edit; the keyset compares tuples), which forced the pager hrefs to be reactive closures rather than baked. The filed rail/debounce race was **not compounded**: no timer and no `Effect` on `q` were added, so an armed debounce firing after a Next click commits page one of the newest text. One deliberate deviation from `/my`: its error arm is a dead end, but paging makes an error reachable *with nothing to fix* (a shared cursor link can be stale), so the error and empty arms both offer a way home. **Mutation testing found a real survivor** — "the last page offers no next" passed with the cursor ignored entirely, because `bolt` fits one page — strengthened until it died. Review CLEAN, zero majors; eight minors filed. Verified web (e2e 151/151 at `--workers=1`, hydration CLEAN ×4 including a past-the-end cursor, SSR curl of a cursored URL renders page two) + Android 11/11 on-device
- [x] Catalog search never fills `CardSummary::owned`, so the tile's "N owned" badge is unreachable: `HostedBackend::search`'s projection selects no `owned` column (only `card_summary` does), leaving it `None` on every search hit — the badge in `CardTile`/`ResultsList` is dead code and an authed catalog looks identical to an anonymous one. Needs the `owned_by_card` join inside the scoped tx, authed-only. Found while writing the card-detail e2e, which had to stop using `owned` to locate seeded holdings (specs: [app-ui](app-ui.md), [collection-api](collection-api.md)) — **done**: new `owned_by_oracle(&[Uuid])` does **one** `SELECT … WHERE oracle_id = ANY($1)` inside `scoped_tx`, and `card_summary` was rewritten onto the same helper so the tile and the detail page cannot drift. **The scoped and unscoped reads were kept separate deliberately** — folding the join into the paging query would wrap the public, cacheable, hottest read in a transaction for a column anonymous callers never see, or else duplicate the grammar/keyset/cursor construction; ownership also stays out of the paging query so it structurally cannot affect which rows return. `native.rs` needed no change (verified: opportunistic `catalog_backend` + JSON round trip). **Tenancy verified against real multi-tenant dev data** — `security_invoker=true` from the migration and live `pg_class`, `FORCE ROW LEVEL SECURITY` on both tables, pool role `rolbypassrls=false`, and for two oracle_ids genuinely owned by two different users the authed API returned this user's count, not the other's and not the sum. The card-detail e2e now *uses* `owned` to locate seeded holdings instead of walking four candidates. Review CLEAN, zero majors; seven minors filed. Verified web (e2e 153/153 at `--workers=1`, hydration CLEAN ×4, SSR shows the badge authed and **zero** occurrences anonymously) + Android anonymous half on-device
- [x] Set facet as a real picker: add a `list_sets` read adapter and replace the rail's comma-separated set-code text input with a multi-select over actual set names/codes (specs: [app-ui](app-ui.md), [catalog-search](catalog-search.md)) — **done**: `CatalogStore::list_sets` (trait, both backends, `GET /api/catalog/sets`, thin GET server fn, anonymous — sets carry no ownership) + `SetPicker` in the rail; `RailState.set` became `Vec<String>` and the comma round-trip is gone. **The selection is the query text and is never intersected with the fetched rows** — that is what stops the widget silently dropping a pasted code, and it means there is no "unknown code" branch to get wrong: `s:xyz` behaves exactly like `s:mh3`, as does a real code outside the 25-row window. **Bounded server-side search, forced by a measured O(n²)**: `CommandItem`'s `highlighted` memo calls `visible_ids()`, which clones the items vec and reads every item's `visible` signal, and `Command`'s highlight-reset `Effect` invalidates all of them per keystroke — so ~1050 sets is not viable (review confirmed the complexity claim independently). Rows remount fully per query, which is the safe case for the recorded `visible_ids()` ordering caveat. **Review found one major**: three states — not-fetched, fetch-failed, and genuinely-empty — were crammed into `Vec<SetSummary>`, so a failed fetch and a collapsed section both rendered "No set matches.", an authoritative claim about the user's catalog (and on native an offline phone is the *normal* failure). Fixed with `Option<Result<…>>` and four arms, an error arm carrying the shared `describe_error` wording plus a **Try again** that bumps an attempt counter, and the loading arm finally reachable; the lazy fetch was kept. **The failure was induced for real** (`page.route` fulfilling a 500 against the GET server fn), and two mutations each killed exactly their own test, with liveness proved by `strings`-ing the served wasm rather than trusting the watch. Verified web (e2e 162/162 at `--workers=1`, hydration CLEAN, SSR shows chips + list for `?q=s:mh3,lea` and zero rows bare) + Android rail probe 11/11 on-device
- [x] ⌘K command palette: `command`+`dialog`, desktop logged-in only, Places + fixed 3-command registry per design/command-palette.md — **`command` ordering caveat:** its item registry is built in *mount* order and `visible_ids()` returns that order, which equals DOM order only while items mount in document order or fully remount. If this surface keys a list that **reorders in place**, ↑↓ will silently visit rows out of visual order — add the `compareDocumentPosition` sort the V3 review described (app/src/components/ui/command.rs, the `visible_ids` comment). Remounting the list per query needs nothing. (specs: [app-ui](app-ui.md)) — **done**: `app/src/components/palette.rs`, mounted once in `AppShell` and rendering nothing unless desktop **and** signed in. **The caveat was live here and remounting alone did not discharge it** — three facts, each measured: a plain `{move || …}` closure is *not* a remount (tachys diffs an unkeyed view collection positionally and reuses the DOM nodes while re-running the item registrations, so `command`'s registry grew while the nodes stayed), fixed by `<For each=move || [rows.get()] key=RowSet::key>` — a `<For>` of **one** keyed on the row set's identity; and even then `CommandItem` registers from its *component body*, which Leptos runs at **view-construction** time, so building both groups at their `let` bindings and only *then* choosing DOM order left places always registered first. Typing one `n` drew COMMANDS on top while `visible_ids()[0]` was `Inbox` — highlight in the second group, `↑` clamping, top rows unreachable upward. Fixed by deciding order *before* constructing either group; **the `compareDocumentPosition` sort stays deferred**, now also because `visible_ids()` is a measured O(n²) path and a DOM-ordering pass would put layout reads inside it. Three deviations from the design, each forced: `should_filter=false` with a hand-written fuzzy ranker (the primitive's lowercase `contains` cannot match `trabin` → `Trade Binder` and cannot rank); **groups ranked by their top match** rather than fixed, because "grouped" and "best match pre-selected" conflict under a fixed order (and this is what made DOM order variable in the first place); and an invented cold-start PLACES group, without which a first-ever `⌘K ⏎` would run `New binder…`. `Undo last move` is **session memory, not an endpoint** (no `undo_last_move` server fn; it races a second tab), and the review's two orchestrator-promoted majors were both that invariant — teardown recorded nothing, so `⌘K → Undo last move` reversed an *older unrelated* move and left the teardown standing (`TeardownReceipt` now carries `move_ids` instead of a count that could disagree), and a toast's Undo never cleared the record, so the palette replayed a dead id and raised a **false success toast over a no-op** (new id-matched `LastMoveState::forget`, since a toast outlives the row that raised it). An audit of all five UI-reachable ledger writers found no third offender. Verified: gate **8/8** on macOS incl. `three_rings`, full chromium tier **190/190** at `--workers=1`, hydration CLEAN anon ×4 + authed ×5, bench CLEAN, SSR curls showing zero palette markup with `id="tree-create"`/`Trade Binder` as positive controls, Android CDP PASS + `probe:android-palette` CLEAN at 540 px with `CommandDialog` itself driven on the real webview as the negative check's positive control. Major-1's regression test confirmed failing before the fix and passing after; the `forget` fix mutation-checked (app-ui Findings)
- [x] `scripts/seed-dev-data.sh` cannot run in the canonical devcontainer: it shells out to `psql` to resolve the e2e user's uuid from `neon_auth."user"`, and the image ships no postgres client — so the documented prereq for every `/my/*` screen only works on a host that happens to have psql. Per CLAUDE.md a missing tool is an image bug: add `postgresql-client` to `.devcontainer/`, or resolve the uuid without psql. Worked around host-side during the card-detail task (specs: [dev-environment](dev-environment.md), [app-ui](app-ui.md)) — **done**: `postgresql-client` added to `.devcontainer/Dockerfile` and merged with the card-detail task (PR #43); verified `psql 17.10` in a local build, and the published `dgoings/three-rings:latest` now carries it (arm64 digest `e08bb209`, pushed 2026-07-20). `seed-dev-data.sh` and `migrate.sh` both run in-container again
- [x] Tree move has no keyboard/touch path: reparent/reorder is HTML5 drag-and-drop only, which doesn't fire on touch (no drag on mobile) and has no keyboard equivalent — so on a phone or from the keyboard a collection cannot be moved at all. The IA calls for "move via context menu" alongside create/rename/delete (design/information-architecture.md → My cards), which the management task scoped to drag. Add a context-menu **"Move to…"** action (a destination picker over the collection list, reusing the `command`/`popover` picker; it commits through the same `reparent_collection` + `reorder_collection` adapters) so move is reachable without a mouse (specs: [app-ui](app-ui.md)) — **done**: `Move to…` as a fourth menu action in `app/src/my/tree_manage.rs` (`MoveReq` snapshotted on open like `DeleteReq`, pure `move_destinations`/`plan_move`, a fourth `tree-move` dialog over the shared `DestinationList`), committing through the existing adapters with no new endpoint. **Three things were bigger than the task line implied.** (1) There was **no keyboard route into the context menu at all** — rows are `<div>`s whose only focusable children are the link and the chevron, and the panel is a `popover="manual"` with no focus management, so building only the menu item would have left the task's own premise unsatisfied; added a real `⋯` button (`opacity-0`, not `hidden`, so it stays tab-reachable), the `ContextMenu`/`Shift+F10` chord, and focus-on-open + ↑↓/Home/End roving + ESC-restores in the primitive. (2) **A real long-press does not produce `contextmenu` on the Android webview** — three code comments had asserted otherwise since the menu was vendored, because the 2026-07-20 "verified on the real webview" run dispatched a *synthetic* `contextmenu` event and so tested the handler, not the gesture; re-measured (1.2 s held touch yields nothing, a tap on the same page produces a click) and all three corrected. This is why the `⋯` button exists. (3) **The `hidden md:block` gap was worse than filed** — not just invisible dialogs: the entire `aside` was `display:none`, so a phone had **no collection tree at all** and create/rename/delete rendered into a hidden subtree; fixed in scope by hoisting `TreeDialogs` to the shell and turning the rail into a CSS slide-over drawer (`left`, **not** `translate-x` — a transformed ancestor becomes a containing block and would re-base every `fixed` dialog), My-cards-only so Catalog keeps its designed filter path. **The task brief's Inbox premise was wrong**: `AND NOT is_inbox` guards the reparent *subject*, so the Inbox is a legal destination (drag already allowed it) and `Move to…` is instead withheld from the Inbox's own menu. **Stated gap**: reparent is covered (including out to top level, landing last among its new siblings via `reorder_collection`), but **reordering among siblings you are already among stays drag- and therefore mouse-only** — filed below rather than left in a code comment. `destination.rs` was extended for its third consumer, not forked (`DestinationRow` extracted; the tree's `Top level` row carries no `Destination`). Review **CLEAN, zero majors** — it independently confirmed the subtree exclusion is complete, the two-write failure window is honestly worded, and that nothing enables `leptos/delegation` (which would have made `element_anchor`'s `current_target()` — and the pre-existing `drop_intent` — silently return `None`); ten minors filed. Verified: gate **8/8** on macOS incl. `three_rings`, full chromium tier **198/198** at `--workers=1`, hydration CLEAN anon ×4 + authed ×5, bench CLEAN with the new keyboard block kill-checked, authed SSR curl showing one `id="tree-move"` and zero `destination-option` while closed, `probe:android-tree-move` PASS driving real `Input.dispatchTouchEvent` taps on the webview (app-ui Findings)
*The single "Polish + full pass" line was split into the four tasks below on
2026-07-26 (maintainer decision). It bundled five workstreams — a states sweep
across nine screens, a responsive audit, two unbuilt wireframed surfaces, the
stage verification, and two spec status flips — and one agent absorbing that in
one context is the loop's top cost driver. Two sub-items were already settled
when the split was made: **no `@flaky` tests exist**, so that clause needed no
work, and the **Android release smoke is maintainer-owned** (`artifacts.yml` is
`workflow_dispatch` and the maintainer handles Tauri releases manually), so it is
recorded rather than run by the loop. The mobile `/my` and header-kebab entries
were promoted out of Phase 5 discoveries by the same decision — they are
wireframed screens, not polish.*

- [x] Mobile `/my` root does not match its wireframe: `design/wireframes.pen` → *Mobile — My cards root* (390 px) is a **drill-down list of collections** — All cards / Inbox / Binders / Decks / Shopping list, each chevroned — but the app renders the desktop All-cards card table there instead. The slide-over rail drawer added by the tree-move task made the tree *reachable* on touch; it is not this frame. Build the frame as designed, keeping the IA's "navigation collapses, features don't" rule (specs: [app-ui](app-ui.md), [ui-design](ui-design.md)) — **done**: `app/src/my/root.rs`, whose pure `root_rows` projects the **same** `AssembledTree` the desktop rail renders, so the list and the rail structurally cannot disagree; plus a new `/my/all` route giving the aggregate table a URL a phone can reach. **`/my` emits both markups and CSS picks — the width is never resolved in code**, because SSR cannot know viewport width and resolving a media query in Rust would make the server's markup disagree with what hydrates; the new `PROBE_WIDTH` env on `hydration-check-authed.mjs` makes that checkable rather than argued. **The review's one major was a regression this task introduced, and the lesson generalizes: a CSS-hidden fallback is not a fallback.** With `collection_tree()` erroring at phone width, the error line rendered while the All-cards table beside it was `display:none`, so the document held **no link to `/my/all`, none to `/my/shopping`, and none to any collection** — the drawer reads the same failed resource and the bottom tab points back to `/my`, so My-cards mode was a dead end on touch, where before the CSS switch the same failure degraded gracefully. Fixed with `fallback_rows`, built through the *same* row helpers as the happy path, behind an `role="alert"` line that blames the collections rather than the page; `RootRow::count` became `Option<i64>` and the fallback carries `None`, since both totals come from the read that just failed and a `0` would be a number the app cannot vouch for. **`page.route` cannot induce a server-resolved resource failure** — `/my` is `SsrMode::Async`, so a `goto` makes zero browser requests beyond the document (measured); the working mechanism is an **SPA navigation** from `/dev/components`, which does not mount `AppShell`, and the test asserts the interception happened so it cannot pass without inducing anything. One forwarded finding was **disputed and the dispute upheld** (`h1:visible` had not lost its strict-mode catch — `toHaveText(string)` already requires a single match, mutation-verified). Also fixed a **pre-existing 3 px wrapper-local overflow** at 390×844 that had been invisible to document-level assertions since `/my` shipped, and learned that `mx-*` on a `w-full` `Separator` reproduces the same class of bug. Thirteen minors filed. Verified: gate **8/8** on macOS incl. `three_rings` — with a cargo fingerprint cache hit called out and re-run from scratch, and `three_rings` confirmed lint-checked via `--message-format=json` rather than inferred — full chromium tier **210/210** at `--workers=1`, 239 workspace tests, hydration CLEAN at default width *and* `PROBE_WIDTH=390`, `probe:android-my-root` PASS driving a real touch tap that drills in, overflow check kill-verified (app-ui Findings)
- [x] Collection-header kebab, desktop and mobile: the `Header Kebab` and `M Header Kebab` frames in `design/wireframes.pen` are unimplemented. It is the second designed home for the same create/rename/delete/**move** actions the tree's context menu now carries, and on a phone it is the natural one — the tree is behind a drawer there. Reuse `tree_manage.rs`'s existing menu, dialogs and adapters rather than adding a parallel set; note the four minors filed against `ContextMenuItem` (no `aria-disabled`, no typeahead, ESC not overlay-stack-coordinated, focus-restore stuck-flag) all become more visible with a second consumer (specs: [app-ui](app-ui.md), [ui-components](ui-components.md)) — **done**: `HeaderKebab` in `app/src/my/collection.rs`, a real button whose panel **is `TreeMenu` itself**, so the two menus' offered *and withheld* sets cannot drift (an e2e asserts both panels' full ordered label lists). **But the task's headline result is that a second consumer of a shared menu is where "the tree refetch is enough" stops being true** — and, following that thread, **a live data-correctness bug on `main` traced to a Leptos resource-id collision.** An SPA navigation into `/my` (the sidebar's `All cards` row, the breadcrumb root) rendered "You haven't added any cards yet" with **zero network requests** for a user with 100 copies, while a direct load was correct. `initial_value()` reads `__RESOLVED_RESOURCES[id]` with **no `during_hydration()` check** (`leptos_server-0.8.6/src/resource.rs:399`), an `SsrMode::Async` page serializes three times at disjoint id ranges while the client consumes only the first, and `AllCardsView` and `SearchResults` are **byte-identical when empty** — so quick-add's closed-panel payload cross-decoded into an empty All-cards view. **A regression from #74, proven by deleting `MyRootNav` and watching the bug vanish**; the colliding slot was pinned at **id 12** by identification-by-removal (the review's derived guess of 10 was wrong while its stride fingerprint was right); and it was **collection-dependent** — Trade Binder and Bulk Box broke while Shoebox and Commander Deck worked on identical slots — which is why it hid. Fixed with a named-field `AllCardsPayload` + `deny_unknown_fields`, chosen over re-numbering ids because re-numbering leaves the next identical-shape collision waiting; the **same-type** case (`/my` vs `/my/all` with different `?q=`) is provably not closable that way and is filed, as is the upstream one-liner. Two of my own predictions were **disputed with evidence and the disputes upheld** (the `?q=` corollary does not reproduce — latent, not active; `view_res` is safe both directions). Also fixed: mutations invisible on the page you performed them from (title/counts/folder rows come from `collection_view`, so no `tree.refetch()` could reach them — `TreeManage::revision` as a `view_res` source), deleting the collection you are viewing leaving a dead id (`route_after_delete`, cascade-aware, fixed in the shared dialog so the tree-row path benefits too), and review's second major — the delete carve-out from `revision` was too broad, so deleting a *sibling* left its folder row on the page linking to a deleted id with its copies still counted. `npm run diag:resource-ids` kept as the tool that pinned slot 12. Verified: gate **8/8** with **four** cargo cache hits called out and re-run from scratch, full chromium tier **223/223** at `--workers=1`, hydration CLEAN ×4 and ×3 at `PROBE_WIDTH=390`, SSR curls showing no cross-panel testid collision from two menus in one document, `probe:android-header-kebab` PASS on real touch (44×44, aim-before-open, clamped, `on_select`). Both majors' tests confirmed failing before their fix. Eleven minors filed (app-ui Findings)
- [x] Empty / error / loading states across all nine screens: every surface's zero-data, failed-fetch and in-flight arms, audited screen by screen. **The `success`/`warning`/`info` Badge and Button variants re-added in Stage 3 still have no app consumer** (app-ui.md:2563) — these are the screens they were re-added for. Prior art for what "honest" means here: the set picker's four-arm `Option<Result<…>>` (not-fetched / failed / genuinely-empty are three different claims, and collapsing them made a failed fetch assert the user's catalog was empty), the needs view's unqualified "Nothing missing", and catalog paging's error arm offering a way home because a shared cursor link can go stale with nothing to fix (specs: [app-ui](app-ui.md), [ui-components](ui-components.md)) — **done**: `app/src/components/states.rs`, where **the banner takes the error, not a message** — `ErrorNote` classifies the wire prefix four ways and picks affordances from the classification (`Missing`/`Request` withhold the retry because it re-sends the same doomed request, `Transport` offers it, `Session` offers neither and links to sign-in), since a uniform "Try again" would be a lie on most pages. Five surfaces had hand-rolled the same banner, each offering a different amount of nothing. **The dishonest states found were the point**: `unwrap_or_default()` on the collection list in **two** shipped pickers made a failed fetch assert the account has no collections (and on the native backend an offline phone is the *normal* failure); `/cards/:id`'s failure arm was a page with **no link or control at all**; an expired session rendered raw `unauthorized: invalid token`; and **an existing test asserted the dishonesty and called it honest** — `selection-tray.spec.ts:120` expected "No collection to move to." with a comment explaining that was correct for an anonymous page, when the read 401s (the on-device probe asserting the same string was corrected with it). **Review found the one surface where the collapse survived — and that the new docstring claimed otherwise**: the tree's `Move to…` flattened a failed read to `Vec::new()`, and since `for_collection` deliberately degrades `forbidden` to `{self}` the item stayed live, rendering only `⬆ Top level` with no error line — the dialog silently asserted root was the only destination and offered a reparent as the only action, **a real write on a false picture**. `⬆ Top level` was deliberately kept (it is the one destination that never needed the tree; removing it would be the CSS-hidden-fallback mistake in reverse) but no longer stands alone and unexplained. **Three retry buttons bypassed the new classifier and were promoted from minors as scope completion** — including `PickerBody` printing *"Couldn't load your collections: invalid token"* over a retry that 401s forever, verbatim the defect the component exists to prevent, and `tree.rs`/`root.rs` **disagreeing with each other about the same shell resource**, now settled by one `tree_retryable` decision point. **`Failure::Session` turned out reachable on every `/my/*` route via a two-credential mismatch** (see the filed follow-up): `RequireAuth` awaits a read that falls back from `tr_jwt` to `tr_session` and re-establishes, while every data server fn reads `tr_jwt` only with no fallback, and the refreshed JWT lands on the *response* — so the guard succeeds while the data layer 401s, for 15 minutes, on any idle tab. Also fixed: the catalog's dimmed last-good page was unlabelled and `aria-hidden`, so nothing said *why* results sat under an error. Verified: gate **8/8** with **no** cargo cache hits and the three token families confirmed emitted into the release CSS with resolved colours in both themes, workspace tests **254**, full chromium tier **234/234** at `--workers=1`, hydration CLEAN anon ×5 / authed ×6 / ×6 at `PROBE_WIDTH=390`, `probe:android-states` PASS (all four classes, real touch on Try again, all three tone badges resolving to real oklch), `probe:android-selection-tray` restored to PASS. Major kill-verified. Ten minors filed (app-ui Findings)
- [x] Responsive audit + stage close: load each of the nine wireframe frames at its own width (3 desktop at 1440, 5 mobile at 390, plus the hover-preview overlay) and reconcile the rendered screen against the frame — layout, affordances present, what collapses. Known targets going in: the **16×16 px selection checkbox** tap target on a 390 px phone (filed; the adjacent cell is the card-detail link, so a mis-tap navigates away), the toast painting over the selection tray's clear `×`, and the tray centering on the viewport rather than the content column at 1440. **Also kill-verify the mobile overflow test** — app-ui.md:1198 records that it measured `document.documentElement` while `TableWrapper` is `overflow-auto`, so stripping the progressive-column classes produced 92–128 px of sideways scroll at 390×844 with the test still green; the corrected wrapper-scoped assertion is reason-verified but its kill check was cut short. Then close the stage: full e2e tier, record the maintainer-owned Android release smoke, and mark app-ui and ui-work-loop implemented — **stage boundary** (specs: [app-ui](app-ui.md), [ui-work-loop](ui-work-loop.md)) — **done, and Stage 3 closes with it**: all nine frames reconcile or deviate-with-the-deviation-recorded. Four touch targets fixed and measured on the real webview (selection checkbox 16×16 → **44×44** with the card link 8 px beyond it; the toast now clears the tray by 10.5 px and the tab bar by 21 px; the tray dock centre moved 720 → **840** onto the content column; the rail toggle 27.8×26 → **44×44**, and it is the *only* touch route into tree management since a long-press raises no `contextmenu` on the webview). **The kill-verification `app-ui.md:1198` left unfinished is complete and its caveat struck** — under the re-applied mutation the corrected wrapper-scoped assertion fails, and across all nine collections wrapper overflow reads **58–148 px while `document.documentElement` reads exactly 0**, so the original assertion could not have failed. **Adding SPA click-through to the audit — because #74's bug hid behind direct loads — found a second *live* collision**: clicking Catalog from a collection rendered the collection's own cards as search results with a false count and zero fetches, deterministic on 4 of 9 collections; fixed and verified 0 wrong out of 20, red-then-green. **Review then overturned the audit's own conclusion**: the "two payloads remain" scan was for `Result<T,_>` and structurally could not see the `Option<_>` family, which is strictly more promiscuous since a bare `null` decodes into *every* `Option`-typed resource — and `/cards/:id`'s `None` arm was rendering "That card id isn't valid." for cards that exist. Measured: the mechanism is **armed but does not fire today** (landing id 64/66 against 13/19 slots serialized — ~50 slots of headroom that is an accident, not a design). **The cross-shape class is now closed**, by five named-field wrappers plus *tested* skips (shell-level ids cannot drift; two `Option` defaults degrade to the honest resting state; two payloads self-discriminate) — and one skip was **wrong and caught by measuring it**: an `{"Ok":[]}` flood made the destination picker show 0 of 10 collections with no error and no request, because an array payload is promiscuous the same way `null` is. Same-type collisions remain the one open hole, filed. Verified: gate **8/8** with **four** cache hits called out and re-run from scratch, workspace tests **256**, full chromium tier **249/249** at `--workers=1`, **24 table surfaces at 390 px with zero document and zero wrapper-local overflow**, a **39-edge SPA sweep** content-checked against the API's own JSON, and **seven** Android probes green on the real webview. Both specs flipped to `implemented`; `app-ui`'s header records that one drawn control (the overlays' `+ Want`/`+ Have`) is knowingly unbuilt and filed. **No release APK was built or triggered — that smoke stays maintainer-owned and no release coverage is claimed** (app-ui + ui-work-loop Findings)