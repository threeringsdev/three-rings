# Project TODO — execution queue

**This file is the single source of truth for what to work on next.** The selection algorithm, spec gating, and definition of done are in [README.md](README.md) ("Working the queue"). State legend: `[ ]` available · `[~]` in progress · `[x]` done.

Phases execute top to bottom; tasks within a phase top to bottom. A task's `(specs: ...)` lists every spec it is gated on — all must be `accepted` (status is read from the spec files, not recorded here). Tasks without a specs annotation are ungated.

## Phase 0 — dev environment (devcontainer)

Keeps the Rust toolchain off the host; see the Decisions log and `.devcontainer/README.md`.

- [x] Author `.devcontainer/` and build the `dgoings/three-rings` dev image (v1: Rust/Leptos/Tauri CLIs — cargo-leptos, cargo-generate, cargo-tauri, leptosfmt); wire devcontainer (image, ports 3000/3001, env-file, zshrc mount)

## Phase 1 — architecture spike

Ordered riskiest-first; see the spec's Failure policy — if the Android gate fails, STOP the phase. Executed inside the devcontainer (Phase 0); macOS desktop is host/CI-side — see Decisions log.

- [x] Compare scaffold bases (start-tauri-fullstack vs. tauri-leptos-ssr); record choice + rationale in architecture-spike.md (specs: [architecture-spike](architecture-spike.md), [ui-components](ui-components.md))
- [x] Scaffold Cargo workspace from the chosen base; commit unmodified (specs: [architecture-spike](architecture-spike.md))
- [x] Verify web target: `server` binary locally, SSR + hydration (specs: [architecture-spike](architecture-spike.md)) — done early via devcontainer smoke (see Findings)
- [x] Build + run: Android (emulator OK) — the architecture gate; static page sufficient (specs: [architecture-spike](architecture-spike.md)) — **PASS** on the Flip 7 emulator, incl. hydration + server fns (see Findings)
- [x] Set up Neon project (free tier): one trivial table, seed rows, sqlx connectivity from the server path (specs: [architecture-spike](architecture-spike.md)) — verified in-container: `cards` migrated + seeded, probe read 3 rows (see Findings)
- [x] One server function + one page rendering DB rows, using at least one Rust/UI component (specs: [architecture-spike](architecture-spike.md), [ui-components](ui-components.md)) — `/cards` SSRs Neon rows in the vendored Rust/UI table (see Findings)
- [x] Build + run: macOS desktop target (embedded Axum) (specs: [architecture-spike](architecture-spike.md)) — **PASS**, host-built; dynamic port, SSR + `/cards` from Neon (see Findings)
- [x] Write up findings in architecture-spike.md; mark spec implemented (specs: [architecture-spike](architecture-spike.md)) — spec status → implemented; Phase 1 complete

## Phase 1b — UI design — parallel with Phase 1, human-led

- [x] Information architecture / nav structure (specs: [ui-design](ui-design.md)) — two modes (Catalog / My cards); see design/information-architecture.md + spec Findings
- [x] Wireframe core screens (catalog search, collection, add-flow, auth, shell) (specs: [ui-design](ui-design.md)) — design/wireframes.pen: 9 screens + overlay, desktop + mobile; see spec Findings
- [x] Prototype the add-to-collection flow (specs: [ui-design](ui-design.md)) — storyboards in design/wireframes.pen + input-cost accounting; see spec Findings
- [x] Component gap analysis vs. Rust/UI registry (specs: [ui-design](ui-design.md), [ui-components](ui-components.md)) — design/component-gap-analysis.md: 27 primitives vs rust-ui@43e1e32, 3 gaps (tree, count stepper, selection tray) + SSR code review of 6 components; see spec Findings
- [x] Design the global ⌘K command palette (actions, scope, wireframe) (specs: [ui-design](ui-design.md)) — design/command-palette.md + ⌘K region in wireframes.pen (P1/P2); places + 3-command registry, desktop-only; see spec Findings
- [x] Wireframe hygiene: delete disabled ghost nodes flagged by layout scans (mobile `/`-hint frames and similar), so problem scans stay a usable signal (specs: [ui-design](ui-design.md)) — 4 ghost `/`-hint frames deleted; scans clean bar the intentional filter-sheet clip; see spec Findings

## Phase 2 — delivery pipeline

Code leaves the laptop: CI validation, remote-checkable artifacts, agent self-sufficiency. Ordered by dependency; budget policy and design details in the spec.

- [x] GitHub: create a free org + public repo under it (Blacksmith requires org ownership; public because free-plan orgs can't gate private repos), push `main`, enable auto-merge, branch protection requiring the validate check (blocked on the validate workflow existing), install the Blacksmith GitHub app, add secrets (`ANDROID_KEYSTORE` + passwords; `RENDER_DEPLOY_HOOK` only if needed) — org/public repo/`main`/Blacksmith/`ANDROID_*` secrets done; branch protection (required check is the **job** name `Format, Clippy, Test & Build`, not the workflow name) + auto-merge enabled and **proven** — PR #1 and PR #2 both auto-merged on green (`RENDER_DEPLOY_HOOK` not needed; Render's GitHub integration auto-deploys) (specs: [delivery-pipeline](delivery-pipeline.md))
- [x] Replace template `ci.yml` with the validate workflow: fmt, clippy (ssr + wasm, incl. `src-tauri` via Tauri linux deps), test, `cargo leptos build --release`; rust caching — `.github/workflows/validate.yml` on `blacksmith-4vcpu-ubuntu-2404`, every push + PR, green on `main`; a rust-cache `cache-bin` bug (dropped `cargo-leptos`) and the Node 20 action deprecation were found + fixed on first CI (see the 2026-07-11 Decisions log) (specs: [delivery-pipeline](delivery-pipeline.md))
- [x] Android APK artifact job (`main` + `workflow_dispatch`, `paths-ignore` for docs-only pushes): NDK r28+ (closes the 16 KB finding), aarch64 APK, signed with the keystore secret — builds on merge to `main`; signed APK published to the rolling `latest` release, installed + ran on device; required a backward-compatible `src-tauri/gen/android/app/build.gradle.kts` signing/version edit (see the 2026-07-11 Decisions log) (specs: [delivery-pipeline](delivery-pipeline.md))
- [x] macOS `.dmg` artifact job (`workflow_dispatch` only — Blacksmith macOS at $0.08/min) — authored + gated; **valid ad-hoc signing** added (`signingIdentity: "-"` → opens on any Mac via one-time right-click → Open, no longer "damaged"); Developer ID + notarization deferred (blocked on org Account-Holder cert access — recipe in spec); dispatch to verify (specs: [delivery-pipeline](delivery-pipeline.md))
- [x] Rolling `latest` prerelease: artifact jobs publish APK + `.dmg` to stable URLs — live with the APK asset + per-asset SHA/timestamp provenance; `.dmg` half pending its first dispatch (specs: [delivery-pipeline](delivery-pipeline.md))
- [x] Web deploy: multi-stage `Dockerfile` (server binary + site, `PORT`→`LEPTOS_SITE_ADDR` entrypoint) + Render service with GitHub integration; Neon branch split (project main → Render `DATABASE_URL`, new `dev` branch → `.devcontainer/.env`); verify `/` + `/cards` at the public URL — **live** at https://three-rings-6p5o.onrender.com: root `Dockerfile` (rust:1-bookworm → debian:bookworm-slim, 114 MB) merged via PR #2 (auto-merged on green); Render `three-rings` service (Docker runtime, auto-deploy on `main`, free/spin-down, Ohio) SSRs `/` + `/cards` from the Neon **production** branch — `DATABASE_URL` is its only manual env var (all `LEPTOS_*` baked into the image); Neon `dev` split done earlier (specs: [delivery-pipeline](delivery-pipeline.md))
- [x] Agent self-sufficiency: root `CLAUDE.md` (commands, what-runs-where, queue conventions), in-container git/`gh` auth docs, update `.devcontainer/README.md` table (CI owns Android/macOS builds) — authored + merged, plus `scripts/gen-android-keystore.sh` (specs: [delivery-pipeline](delivery-pipeline.md))
- [x] Prove the loop end-to-end: fresh container → agent does a trivial task → PR → auto-merge on green (zero human touch) → verify web URL + APK update away from the laptop, dispatch and check the `.dmg`; mark spec implemented (specs: [delivery-pipeline](delivery-pipeline.md))

## Phase 3 — foundations

- [x] Auth (Neon Auth = Better Auth). **Done:** `app_runtime` non-owner role + `trusted_origins`; migrations moved out of the app (PR #3); JWKS EdDSA verify middleware + `AuthUser` extractor + `/api/me` probe (PR #4); **path-B cookie-session proxy** (`tr_session`/`tr_jwt`/`tr_challenge` httpOnly cookies, server fns at `/api/*`, silent JWT refresh), Leptos `/login`+`/signup`+OTP screens, **Google OAuth kept fully path B** via the verifier/challenge exchange (`/auth/callback`), toggles decided (email verification **on** — `require_email_verification`, OTP method; Google **on**, shared creds), and the **Tauri desktop webview spike PASS** (full gated sign-up→OTP→auto-sign-in inside WKWebView). Fixed en route: step-5 verifier 401'd all real tokens (jsonwebtoken v9 `aud`), and a unit `Suspense` fallback broke hydration app-wide (see auth.md Findings). All flows verified live on the dev branch (headless UI drives in `end2end/*.mjs`). Still riding other tasks: `SET LOCAL app.user_id` (data-model per-request tx) (specs: [auth](auth.md))
- [x] Build perf 1/2 — split the release profile so the SSR `server` binary skips the wasm-only `lto`/`codegen-units=1`/`opt-level=z` (`bin-profile-release` → `server-release`, = Rust stock release defaults); measured cold `-p server` on 18 cores **44.3s → 31.5s (~29%)**, OLD used only ~4× parallelism vs NEW ~10×; wasm bundle keeps size-opt. Render builder core count unverified — confirm on first deploy (specs: [delivery-pipeline](delivery-pipeline.md))
- [x] Build perf 2/2 — Dockerfile cargo-chef dependency-caching refactor (+ optional mold linker, tailwindcss prefetch); verify Render deploy time before/after — done: chef/planner/builder stages with two cooks mirroring cargo-leptos's exact invocations (native `server-release`; wasm `release` @ `target/front`); mold scoped to native triples; tailwindcss/wasm-opt/wasm-bindgen pinned on PATH (cargo-leptos prefers PATH tools → zero per-build downloads/API checks). Local Docker: cold **137s → 9s** on a source-only edit (cook layers CACHED, only workspace crates recompile). Render build-cache persistence **verified** from deploy build logs (cache-manifest import + CACHED build-stage layers); also surfaced that PR #5's squash had missed the `e5df220` Dockerfile fix — main deploys were `build_failed` until PR #6 landed it mid-task (specs: [delivery-pipeline](delivery-pipeline.md))
- [x] Build perf follow-up — confirm cook-layer reuse on Render — **confirmed: Render's registry cache import works across deploys on ephemeral builders**, twice over: the d7b3dc0 failed-deploy log showed `CACHED` build-stage layers, and the ba90076 (auth) deploy showed all chef toolchain layers `CACHED` while the two cooks *correctly* re-ran (that PR changed Cargo.toml/lock → new recipe; 7m00s vs 6m13s fully-cold). The pure warm case (no manifest change) is this task's own docs-only deploy — cook layers expected `CACHED` with only workspace crates rebuilding; verified post-merge in the PR record. sccache+S3 fallback not needed on current evidence — see delivery-pipeline "Build performance (deploy time)" (specs: [delivery-pipeline](delivery-pipeline.md))
- [x] Desktop Google OAuth via the system browser — **done, maintainer-confirmed on desktop**: opener plugin via a custom `open_url` command, `callbackURL = http://localhost:<embedded port>/auth/callback`, challenge/session parked in the single-user embedded server's memory (`app/src/auth/native.rs`), webview polls `current_user` (2 s, ≤3 min) instead of focus-refetch. Shipped with the production-shakedown fixes: `newUserCallbackURL` (first-time Google users skipped our exchange entirely), Google button on `/signup`, back-to-home links, `.env` autoload (dotenvy). Hard-won Tauri v2 findings (silent shell-open rejection; app commands are ACL-gated; loopback pages are *remote* origins needing `remote.urls` in the capability) in auth.md. **Android leg misbehaved and got its own fix**: a frozen backgrounded app can't serve the loopback callback, so Android returns via the `/auth/app-return` bounce + `three-rings://` deep link (PR #12) — maintainer-verified on device, first-time-Google path included (specs: [auth](auth.md))
- [x] Password reset flow — **done**: probed live first (the email-otp plugin's dedicated `request-password-reset` / `reset-password` routes exist on the hosted service; OTP shape chosen to match the verification UX), then `request_password_reset`/`reset_password` server fns with auto-sign-in after reset (upstream issues no session; it marks the email verified) and a ResetCard behind "Forgot password?" on `/login`. Verified live on dev — full API cycle (old password rejected, OTP replay rejected) + headless UI drive, no hydration regressions. Pure server-fn + page work, so it rides every surface (specs: [auth](auth.md))
- [x] Add email+password to a Google-first account — **done, maintainer-confirmed live on production (2026-07-13)**: "Forgot password?" with the google-only account set a password and landed signed in; password and Google sign-in now coexist on the account. Mechanism shipped with the reset flow (upstream's reset *creates* the `credential` account when none exists — better-auth source, quoted in auth.md Findings), and the sign-up "User already exists" error points at it (specs: [auth](auth.md))

## Phase 4 — data & API layer

Build the schema, the data-access layer, the API, and the catalog. Spec-review
(fleshing drafts → `accepted`) is the README's blocked-queue meta-action, not a
task here; these tasks are the implementation, each gated on its `accepted` spec
and carrying the `mark <spec> implemented` step on that spec's terminal task
(see the 2026-07-15 Decisions entry). Every Phase 4 spec is now `accepted` or
`implemented`. (ui-component-bench led the phase as independent UI infra and
is done — spec `implemented`.)

- [x] ui-component-bench → the bench page (independent UI infra — gated only on its own spec, runnable in parallel with the data chain below). Feature-gated (`component-bench`) `/dev/components` route in `app`: section registry + jump-nav, the `table` section, a static theme-token panel + bench-local light/dark toggle, and the "add a component's bench section in the same commit" adoption convention; reachable in the native webviews (WKWebView / Android WebView) for the anchor-positioning check. mark ui-component-bench implemented once the harness + convention are in place and the current vendored set (`table`) is covered (specs: [ui-component-bench](ui-component-bench.md)) — **done**: `app/src/bench/` registry + demos, one `--features component-bench` flag wired through `frontend`/`server`/`src-tauri` + `beforeDevCommand`, bench clippy lines in validate.yml, SSR/hydration/toggle verified via [end2end/bench-check.mjs](../end2end/bench-check.mjs) and feature-off exclusion confirmed; spec → implemented (route-macro workaround + decisions in its Findings)
- [x] data-model → migrations. Write + run the initial migrations (catalog group; collection group + RLS enable/force/policies + the `app_runtime` role) via [`scripts/migrate.sh`](../scripts/migrate.sh); collection tables FK `neon_auth."user"` (uuid, hard `ON DELETE CASCADE`); resolves data-model's execution-deferred OQs (denormalized `user_id`, condition/language granularity); mark data-model implemented (specs: [data-model](data-model.md), [auth](auth.md)) — **done**: `0002_catalog`/`0003_collections`/`0004_catalog_app_readonly` applied to the Neon **dev** branch; RLS verified end-to-end (per-user isolation, forced); a pre-existing Neon default-ACL over-granted catalog CRUD to `app_runtime` → `0004` revokes it on existing catalog tables and `0005` narrows the default privilege so all future tables are app_runtime-read-only by default (no per-table revoke needed again); `migrate.sh` hardened against the `sqlx::migrate!` new-file staleness bug. **Prod migration deferred** to the data-access-backends deploy (dropping spike `cards` is breaking vs. the live spike code — expand-first); spec → implemented (see Findings)
- [x] data-access-backends → the trait layer. Per-domain `CatalogStore`/`CollectionStore` traits, `HostedBackend` (sqlx) + `NativeBackend` (HTTPS) structs, the `shared/` crate (DTOs + the cross-backend error enum), the `ssr`/`hosted`/`native` feature split, the per-request `SET LOCAL app.user_id` tx; remove spike-era direct DB access (`app/src/db.rs`); **migrate the Neon production branch (`scripts/migrate.sh prod`) as part of this deploy** — the data-model migrations were applied to dev only, deferred on prod because dropping the spike `cards` breaks the live spike `/cards` (expand-first, see data-model Findings); mark data-access-backends implemented — prereq: the migrations (specs: [data-access-backends](data-access-backends.md), [data-model](data-model.md)) — **done**: new `shared/` crate (DTOs + `ApiError`); `ssr` decomposed → `hosted` (sqlx, the `server` build) / `native` (HTTPS client, the `src-tauri` build, **no sqlx linked**); per-domain traits, one backend struct per target carrying the session credential; hosted `scoped_tx` runs `set_config('app.user_id',$1,true)` per request; UI calls in-process server fns, native calls explicit hosted JSON routes (`/api/catalog/count`, `/api/collections`); spike `db.rs` probe + `get_cards`/`Card` removed, `/cards` now shows the catalog count via the seam. Gate reproduced in-container + proven live on dev (catalog read `{"cards":0}`, 401 on the session route, scoped_tx OK). Added a dedicated `-p app --features native` clippy gate line (workspace unification masks the native arms) and moved the bench line to `hosted`. **Prod migrated** (`0002`–`0006` applied to the Neon production branch 2026-07-16, verified identical to dev); the native `401` refresh rides the follow-up below. Spec → implemented (see Findings)
- [x] data-access-backends → native 401 refresh + offline behavior. Implement the native client's silent JWT re-mint from `tr_session` on a `401` (stubbed `TODO` in `app/src/backend/native.rs`) + a defined offline-unreachable behavior (data-access-backends OQ#3) — needs the session cookie-jar plumbing that lands with collection-api's native client, so it rides that work (specs: [data-access-backends](data-access-backends.md)) — **done**: `NativeBackend::send` re-mints a fresh JWT from `tr_session` (auth's `mint_jwt`) on a hosted `401` and retries once (hard one-retry bound); `authed` now carries `(tr_jwt?, tr_session?, origin)` and `require_session` accepts a refresh-only session; **unreachable → `Upstream`, auth-error → `Unauthorized`** is type-distinct (OQ#3 closed); cookie re-host stays the `current_user` poll's job (backend stays pure transport). Two `native.rs` unit tests lock the session classification; native + workspace gate green (the network path is device-exercised — needs a live deployment). (The sibling prod-migration deploy step was **done** 2026-07-16: `0002`–`0006` on the Neon **production** branch, verified identical to dev.)
- [x] collection-api → endpoints. Both domains: collection (tree CRUD + reparent cycle check + lazy inbox, holdings/desires + batch add, moves incl. undo/pull/teardown, keyset reads with per-page aggregates) and catalog (`card_detail`, `card_summary`, the search endpoint shell); mark collection-api implemented — prereq: the trait layer (specs: [collection-api](collection-api.md), [data-access-backends](data-access-backends.md), [data-model](data-model.md)) — **done**: built as six verified slices (tree/inbox, holdings-desires, CollectionView, moves, global reads, catalog), every op a `CatalogStore`/`CollectionStore` trait method with a `shared/` DTO, implemented by `HostedBackend` (sqlx) + `NativeBackend` (HTTP client) and mirrored by explicit hosted JSON routes. Keyset pagination on the large reads; per-page aggregates; opportunistic auth on catalog reads; board-agnostic moves (mainboard) with undo/teardown; ownership guard beneath RLS. Each slice proven end-to-end against the Neon **dev** branch (temp drivers, reverted); full merge gate green in-container. sqlx gained `uuid`+`json`. **Pull/pull-all** are client-composed from `move_cards` + `suggested_destinations`; **needs/shopping** return full lists (keyset follow-up below); **no per-op Leptos server-fns** (thin per-screen UI adapter, rides with the UI tasks); **tags on CardRow** stay card-tagging's. Spec → implemented (see Findings)

- [x] card-tagging → schema migration. Migration `0006`: `card_board` enum + `board` on `holdings`/`desires` (folded into their uniqueness keys), `tags` + `card_tags` (RLS enable/force/policies incl. world-readable system tags; seed the `commander`/`companion` system tags before FORCE; `app_runtime` write grants per `0005`'s convention), drop the empty `deck_commanders`; via [`scripts/migrate.sh`](../scripts/migrate.sh) (dev now, prod with the data-access deploy) — prereq: the data-model migrations (specs: [card-tagging](card-tagging.md), [data-model](data-model.md)) — **done**: `0006` applied to dev; boards folded into both uniqueness keys, system tags seeded before FORCE, dual `tags` policy (system-readable / owner-writable) + `card_tags` owner policy verified end-to-end; `app_runtime` SELECT came free from the `0005` default-privilege flip; `deck_commanders` dropped (see card-tagging Findings)
- [x] card-tagging → tag/board API. The tag CRUD + assignment + board + commander/companion endpoints (collection-api §Tags & boards): scope containment, in-deck membership + orphan cleanup, ≤2/≤1 caps, color-identity recompute; mark card-tagging implemented — prereq: the schema migration + the trait layer (specs: [card-tagging](card-tagging.md), [collection-api](collection-api.md), [data-access-backends](data-access-backends.md)) — **done**: 11 `CollectionStore` methods (tag CRUD + scope, assign/unassign with containment + in-deck + ≤2/≤1 caps + idempotency, `card_tags`/`cards_with_tag`, `deck_commanders` with read-derived WUBRG color identity, holding/desire board re-label with split/merge + binder guard), each a `shared/tags.rs` DTO implemented by `HostedBackend` + mirrored by `NativeBackend` over new hosted routes. Proven end-to-end vs. Neon **dev** (temp driver, reverted); full merge gate green. card-tagging → implemented (see its tag/board Findings)

- [x] catalog-ingestion → POC subset. Ingest a representative subset (multi-face, tokens/checklist, varied sets/colors/types/rarities) so catalog search runs end-to-end against real rows — prereq: the migrations (specs: [catalog-ingestion](catalog-ingestion.md), [data-model](data-model.md)) — **done**: the whole bulk path shipped (`server --ingest <poc|bulk>` + `scripts/ingest.sh`, migration `0007` on dev, `catalog_ingest` role on both branches) and the POC ran against the Neon **dev** branch: 1,045 sets, 1,333 filter-matched + 1,643 closure-pulled → **2,976 printings / 2,665 cards / 9,654 rulings**; every opted-in layout present (checklist = layout `token`; battles now `transform`), zero unresolved `all_parts` links, re-run writes 0 catalog rows (hash gate + first-seen-per-run oracle winner). Live surprises recorded in the spec Findings: /bulk-data still serves the legacy JSON-array layout (identity-encoded without gzip negotiation — pipeline sniffs both), and per-printing oracle variance forced the deterministic first-seen rule. Stage-2 `bulk` mode is written and gate-tested but not yet run at full scale
- [x] catalog-search → query engine. Scryfall query-subset parser → SQL against the catalog, query↔rail sync, URL-state serialization, wired into collection-api's search endpoint; mark catalog-search implemented — prereq: the search endpoint shell + POC data (specs: [catalog-search](catalog-search.md), [collection-api](collection-api.md)) — **done**: `app/src/search/` (pure `parse.rs` grammar + `sql.rs` QueryBuilder emission, binds only) replaces the shell WHERE in `HostedBackend::search`; parse errors → 422 naming the term. Migration `0008` on dev (`oracle_search_text` generated column closing the multi-face `o:` gap + trgm GINs). 25 unit tests (TDD) + an `#[ignore]`d live test against the dev POC catalog — which caught a real NULL-poisoning bug in negated color terms (fixed with a locked-in coalesce). Rail sync + URL state ship as contract (query string = canonical state); the rail widgets ride the UI-phase catalog screen. catalog-search → implemented (see Findings)


## Phase 5 — app UI

Replace the scaffold counter with the real app: the nine wireframed screens, the
shell, and the three custom gap components, built page by page so every task
ships independently through the merge gate. Design authority is the Phase 1b
artifacts (wireframes.pen, information-architecture, add-flow-prototype,
command-palette, component-gap-analysis) distilled into
[app-ui](app-ui.md); the per-task agent loop (orchestrator dispatches: an
implementation subagent builds + verifies platforms + authors e2e → an
adversarial-review subagent reviews code and test strength → validate → land;
revised 2026-07-25, fully Claude-driven) is [ui-work-loop](ui-work-loop.md).
The full three-browser e2e tier runs at the end of **every** task (revised
2026-07-20); stage boundaries (marked below) additionally carry the Android
release smoke at phase end.
UI work stays on the ~3K-printing POC catalog (maintainer decision
2026-07-17); quick-add realism / perf findings are recorded as deferred.

### Stage 0 — loop infrastructure

- [x] Android e2e spike: prove/disprove automated e2e against the Android emulator (Tauri debug webview remote debugging → `adb forward` + Playwright `connectOverCDP`, or Playwright's experimental `_android` API); record the outcome in ui-work-loop Findings and fix the loop's platform matrix per its decision tree (specs: [ui-work-loop](ui-work-loop.md)) — **PASS, decision-tree path 1**: `connectOverCDP` fully drives the debug webview (asserted counter increment + dark-toggle on device; live dev-server content proxied behind `tauri.localhost`); web + Android e2e every task, one release smoke at phase end; recipe + constraints in ui-work-loop Findings, probe `end2end/android-cdp-check.mjs`
- [x] Work-loop skills + permissions: `.claude/skills/ui-task-loop`, `e2e-suite`, `android-smoke` (+ `scripts/smoke-android.sh`), shaped by the spike outcome, mirrored into `.agents/skills/`; permission allowlist added to `.claude/settings.json` (diff surfaced for maintainer approval) — prereq: the spike (specs: [ui-work-loop](ui-work-loop.md)) — **done**: three skills landed with matrix path 1 baked in (Codex command names verified against the installed plugin), `smoke-android.sh` for the phase-end release smoke, mirrors byte-identical, allowlist exactly as specced (flagged in the PR for maintainer review); drive-by: realigned the stale `validate` skill with validate.yml (native-backend line, hosted bench)
- [x] E2E baseline reset: delete the counter suite; pre-seeded verified test user on the Neon dev branch + login fixture (email verification is ON — naive signup hangs on OTP); tier tags (`@fast` chromium per task, full chromium+firefox+webkit at boundaries); keep the `.mjs` probes as the probe layer (specs: [ui-work-loop](ui-work-loop.md)) — **done**: tiered config + storageState login fixture + baseline smoke (SSR asserted request-level); `seed-e2e-user.sh` idempotent from any state (fresh-checkout recreate verified); fast tier 4/4, full tier 7/7; Codex review 3 fixed / 1 disputed; found the release-build→watch asset-clobber hydration trap (e2e-suite skill + Findings)
- [x] Dev seed data: script building the test user's realistic tree via existing `CollectionStore` methods (Inbox + nested binders + a deck with commander/boards + holdings/desires across the POC catalog) — prereq for any `/my/*` screen (specs: [app-ui](app-ui.md)) — **done**: `app/src/seed.rs` (debug+hosted only) + `server --seed-dev <uuid>` + `scripts/seed-dev-data.sh`; every write through the real store methods (search→detail→first printing); sentinel idempotency + cleanup-on-error; ran against dev (5 collections / 29 copies / 4 desires / 21 moves / commander tag); Codex review 4 fixed (incl. release-binary exposure → debug_assertions gate) / 1 disputed (see app-ui Findings)
- [x] Dark palette + token migration: `.dark` OKLCH block in `style/input.css`; migrate hardcoded-hex pages (home remnants, auth_pages) onto tokens; vendor `theme_toggle`; class-on-`<html>` + persistence per the theme-persistence decision (specs: [app-ui](app-ui.md)) — **done**: full Rust/UI token set both modes, dark default via SSR-read `tr_theme` cookie on `<html>`, theme_toggle vendored (cookie-initialized both sides — no flash) + live bench section, HomePage/auth_pages hex-free (bounce-page raw HTML exempt); verified web (6/6 probe, hydration clean, fast tier) + Android webview on-device; Codex review 3/3 accepted+fixed (app-ui Findings)

### Stage 1 — component vendoring (vendor-component skill; bench section same commit)

- [x] Vendor batch V1 — static primitives: `button`, `input`/`input_group`, `badge`, `kbd`, `separator`, `checkbox`+`label`, `toggle_group`, `breadcrumb`, `skeleton`, `card` (specs: [app-ui](app-ui.md), [ui-components](ui-components.md)) — **done**: 11 components + bench sections in one commit; `variants!` hand-expanded (no leptos_ui/paste), undefined-token variants dropped, icons inlined, label peer-class upstream bug fixed as deviation; verified web (bench-check CLEAN incl. new interaction asserts, fast tier 4/4) + Android on-device; Codex review: 4 refuted with SSR evidence, 3 fixed, 2 disputed (app-ui Findings + gap-analysis adoption notes)
- [x] Vendor batch V2 — overlay foundations: `use_scroll_lock` asset, `dialog`, `popover`, `sheet`; deterministic caller-supplied IDs (no `use_random_id`), Leptos-owned open state, ESC-listener cleanup; verify CSS anchor positioning on the Android webview, JS fallback if unsupported (specs: [app-ui](app-ui.md), [ui-components](ui-components.md)) — **done**: scroll_lock as pure-Rust (refcounted + generation-guarded), dialog/popover/sheet with caller IDs + one-signal open state + overlay-stack topmost-ESC + inert/aria; anchor positioning confirmed on-device (Android Chrome 145) with a JS fallback for engines lacking it; bench-check extended (stacked-ESC, scroll-lock body styles, ID stability); Codex review 8/10 fixed (app-ui Findings)
- [x] Vendor batch V3 — `command` (rewired fully reactive — the shared core of quick-add / destination picker / ⌘K), `hover_card` (hover-intent timers in Leptos), sonner per the engine decision (specs: [app-ui](app-ui.md), [ui-components](ui-components.md)) — **done**: `command` reactive item-registry (filter Memo + ↑↓/⏎ nav, no JS script), `hover_card` shared-timer hover intent, `sonner` as a native Leptos toaster (programmatic `ToastHandle::show` + undo action) per the maintainer decision; bench-check extended (reactive filter+keyboard, toast+undo, hover handoff); verified web + Android on-device; Codex review 3 real bugs fixed (hover handoff, highlight clamp) / sonner cleared / order-caveat documented (app-ui Findings)

### Stage 2 — shell & public surface

- [x] App shell + routing: `/` auth redirect, route skeletons for the full map, top-bar mode switch, sidebar rail frame, mobile bottom tabs, `/my/*` auth-guard wrapper; delete the counter + `get_count`/`increment_count`/`storage`; fold the `/cards` placeholder into the shell; rewrite the shell e2e smoke — **stage boundary** (specs: [app-ui](app-ui.md)) — **done**: shell.rs (top bar + mode switch, rail frame, bottom tabs, user-menu popover, route skeletons), SSR 302 redirects (`SsrMode::Async`) + `data-ssr-path` webview shim (the Tauri proxy swallows 302s — hydration panic reproduced + fixed on-device), shared current-user resource with full-page auth transitions, `?next` honored (open-redirect hardened); Codex review 1 high fixed / 1 medium deferred to queue / 6 clean; full three-browser tier 28/28 + Android anon surface on-device (authed flows blocked by dev-proxy body/cookie stripping — ui-work-loop Findings); e2e mutation pass: 10/10 kills
- [x] Catalog page `/catalog`: `search_catalog` server-fn adapter (the exemplar) over `CatalogStore::search`; query bar with URL-canonical `?q=` state, debounce + stale-discard, SSR first page; grid/list toggle; card tiles (lazy images, skeletons) + list view on the vendored table; anonymous variant with sign-in prompts on quick actions (specs: [app-ui](app-ui.md), [catalog-search](catalog-search.md)) — **done**: `app/src/catalog.rs` + the `search_catalog` adapter shipped as a **GET** server fn (`GetUrl` codec — the Tauri dev proxy strips POST bodies, so a POST read is unverifiable on-device; verified working through it), opportunistic auth reusing `routes::catalog_backend`; URL is the sole query state with per-session (not per-keystroke) history; debounce closed at 250 ms and stale-discard *verified* as reactive_graph's version counter rather than hand-rolled; `?view=list` for SSR-correct layout; roving focus landed on `toggle_group` (V1's parked item) via a new `tabindex` prop + bench assertion. Codex review 2 fixed (errors blanked results — probe-confirmed; URL⇄field clobber — latent, hardened) / 1 accepted-as-documented / 1 disputed→queued; e2e mutation pass 11/11 kills, which exposed 2 vacuous tests. Verified web (SSR, hydration CLEAN ×4, bench CLEAN, fast tier 25/25) + Android webview 8/8 on-device. Paging (`?cursor=`) and the server-fn status-semantics gap queued as follow-ups
- [x] Filter rail + query↔rail sync: rail widgets two-way-bound to the grammar (comma-OR serialization; unrecognized terms preserved verbatim); mobile slide-over sheet with active-filter badge (specs: [app-ui](app-ui.md), [catalog-search](catalog-search.md)) — **done**: query grammar moved to `shared/` (the rail edits it in wasm) with `parse_tokens` keeping raw text so unowned terms survive byte-for-byte; one `owns_every_match` rule shared by read/rewrite; `t:` gained comma-OR (grammar + `ILIKE ANY`, deviation recorded in catalog-search); `<details>` sections, mobile sheet + badge; fixed an SSR regression where `bind:value` rendered no `value` attribute (the query bar included); added a `data-hydrated` test seam that also fixed 8 pre-existing firefox/webkit failures; needed `#![recursion_limit = "512"]` for aarch64-linux-android. Verified: unit 20/20 + 22 in shared, e2e 118/118 three browsers x3, Android on-device CDP check, Codex mutation pass 10/10 caught (app-ui Findings)
- [x] Card detail `/cards/:id` + previews: full page (printings, rulings, your-copies when authed), desktop `hover_card` preview, touch bottom-sheet with expand; includes the multi-face image projection fix (`COALESCE` with `faces->0`) in the hosted summary/detail projections (specs: [app-ui](app-ui.md)) — **done**: `app/src/cards.rs` (detail page + the `CardPreview` composite) behind a `card_detail` GET server-fn adapter with opportunistic auth; the route needed `SsrMode::Async` — under the default out-of-order streaming the page shipped a skeleton with the content in a `<template>`, and the SSR test couldn't tell (now asserts the skeleton's absence). The projection fix went to **all six** sites, not the two the spec named — 128/128 DFC printings in the POC catalog resolve art, zero working rows touched. `hover_card` gained a `disabled` prop (+ bench section) because touch fires a synthetic `mouseenter`, so a tap opened both affordances; review found the disable must also cancel the pending timer. Preview bodies mount lazily — eager rendering duplicated every card name into the DOM and broke three pre-existing catalog tests. Grid tiles opt out of hover (deviation: the tile is already the art). Mutation pass 5/5 kills, two only after rewriting vacuous assertions (a tap-based `disabled` test that passed for an unrelated reason, and `toBeVisible()` on a sheet that stays in layout when closed). Full three-browser tier 148/148, Android real-webview probe 11/11, bench CLEAN, gate green. Codex step-2 could not be read in an autonomous run — substitute reviewer used, gap filed (see Findings)
- [x] Destination picker + Want/Have quick actions + undo toasts: sticky `popover`+`command` picker on catalog surfaces, add adapters over batch-add, sonner undo wiring — **stage boundary** (specs: [app-ui](app-ui.md)) — **done**: `app/src/catalog/destination.rs` (picker + shell-level state, `tr_dest` cookie following `theme_toggle`'s SSR-readable reasoning) + `QuickAddButton`/`raise_add_toast` in `app/src/catalog.rs` + `quick_add`/`undo_quick_add` adapters. Three framings did not survive contact: **batch-add can't back it** (`Vec<LineResult>` carries no ids, so no undo handle) — a Have routes through `move_cards` with `from = None`, the intake form of a move, which is the only call that *returns* the move id undo targets; **`+ Want` is not undoable at all** and its toast ships without the action rather than lying (desires are outside the move ledger and no desire-quantity op exists — queued as a follow-up); and **`CardSummary` needed `printing_id`** because holdings are per-printing while a catalog row is per-oracle — both per-oracle projections now share one `REPRESENTATIVE_PRINTING_JOIN` lateral returning the same printing the old image-only subquery did. The `command` mount-order caveat was **discharged, not carried**: the picker sorts its data before any item mounts and typing hides rather than reorders, so registration order still equals document order — no `compareDocumentPosition` needed (caveat stands for quick-add and ⌘K). Codex review 2/2 accepted and fixed (stale destination after a rename/delete — the module doc was contradicting its own code; and a caller-supplied `AddLine` that let `quantity: 20` through an endpoint meaning "one copy" — severity argued down from medium, since `moves` RLS is `ENABLE`+`FORCE` and the same caller can already reach the machine route, but fixed anyway). Mutation pass **6/6 kills** after strengthening three assertions the analysis exposed — the undo test passed with `undo_move` stubbed out, and now brackets the add with real `collection_view` reads (`present` n→n+1→n). Verified: unit 84, full three-browser tier 175/175, hydration CLEAN anonymous *and* authed (added `hydration-check-authed.mjs` — the anonymous probe can't see a session-gated component), bench CLEAN, Android dev-attach 8/8 on the real webview (anonymous surface only, per the fixed platform matrix)

### Stage 3 — My-cards surface

- [x] Collection tree, read-only: nesting, per-node collapse, pinned system rows, selection, rolled-up count badges; tree server-fn adapter (specs: [app-ui](app-ui.md)) — **done**: `app/src/my/tree.rs` (pure client-side assembly — Inbox pin, sibling order, orphan/cycle defense, unit-tested — plus `CollectionTreeNav`) over a new one-round-trip `CollectionStore::collection_tree` read (own-present per collection + shopping-short badge; lazy Inbox via a shared `ensure_inbox`; hosted SQL + native client + `GET /api/collections/tree` + GetUrl adapter), rendered in the shell rail with pinned All-cards/Shopping-list rows, per-node collapse on the newly vendored `collapsible` (+aria, +inert-when-closed) with `item` rows (variants! hand-expanded, href arm), prefix-match selection, and the mobile tab badge off one shared shell resource refetched by quick-add/undo. Codex review 2/2 accepted+fixed: the reparent endpoint now rejects the Inbox (409 verified live — a nested Inbox defeated the pin), and selection survives a collection's `/needs` subpage. Verified: SSR authed curls, hydration CLEAN anon+authed, bench CLEAN (new sections asserted), full tier **196/196**, Android webview probe PASS, mutation pass **7/7 kills** (app-ui Findings)
- [x] Collection tree, management: drag reparent/reorder, context-menu create/rename/delete with dialog confirms, cycle-guard error surfacing (specs: [app-ui](app-ui.md)) — **done**: `app/src/my/tree_manage.rs` (shared context menu, three confirm dialogs, drag commit via a pure unit-tested `plan_drop`) + drag/menu wiring in `app/src/my/tree.rs` + newly vendored `context_menu` (rewired to `popover="manual"` with our own outside-pointerdown/ESC dismissal + a deferred, generation-guarded open — `popover="auto"`'s light-dismiss races the right-click across engines) + five thin server-fn adapters in `app/src/lib.rs`. Cycle guard is client-first (dragged node's subtree undroppable, no request sent) with the unchanged 409 as backstop; the e2e pins both. The `ContextMenu` provider had to move inside the `Suspense` (a provider above the async boundary doesn't reach `use_context_menu()` inside it). Codex review 4/4 resolved (delete-confirm now snapshots its subject — high; generation-guarded deferred open — med; honest partial-commit toast — med; position-collision documented + queued). Mutation pass 12 analyzed → 2 real test gaps strengthened and kill-verified (bench outside-click, cycle-guard no-request). Verified: unit 81, SSR authed markup, hydration CLEAN anon+authed, bench CLEAN, full three-browser tier **226/226**, Android webview `android-tree-manage-check.mjs` PASS on Chrome 145, gate green. Queued two follow-ups: keyboard/touch move path (drag is mouse-only) and the fractional-index rebalance (app-ui Findings)
- [x] Android release auth check: verify sign-in + an authed page work in the release APK — the dev proxy strips POST bodies and Cookie headers, so the release protocol handler's behavior is unproven (specs: [ui-work-loop](ui-work-loop.md)) **done**
- [x] The merge gate's `cargo leptos build --release` clobbers `target/site/pkg` under a running debug watch server, hydration-panicking every page until the watch is restarted; a source touch does not reliably fix it. Currently only a documented workaround in the e2e-suite skill. Give the gate its own `--target-dir` so verifying-after-gating stops costing a restart (specs: [ui-work-loop](ui-work-loop.md)) — **done**: `cargo leptos build` has no `--target-dir` flag, so `site-root` became the `CARGO_TARGET_DIR/site` marker (resolves to `target/site` by default — watch/Docker/Tauri unchanged) and the gate now runs `CARGO_TARGET_DIR=target/gate cargo leptos build --release`, writing its site to `target/gate/site`. Verified live: full gate green (8/8, macOS host incl. `three_rings`) while a watch served :3000, `target/site/pkg` byte-identical before/after, `/login`+`/dev/components` hydrated CLEAN with no restart. Only the release step is isolated (clippy/test keep sharing `target/` for dep reuse); CI unchanged (no watch there; preserves rust-cache paths). Docs updated: validate skill (+`.agents` mirror), CLAUDE.md/AGENTS.md, e2e-suite clobber note (see ui-work-loop Findings)
- [x] Re-add the component variants V1 dropped for want of tokens (`button` Warning/Success/Bordered, `badge` Success/Warning/Info) together with the `style/input.css` tokens they need — the polish task's error/empty states are the screens that want them (specs: [app-ui](app-ui.md), [ui-components](ui-components.md)) — **done**: full upstream `success`/`warning`/`info` token families (both modes) + `@theme` mappings + bench-panel mirror; **four value deviations for WCAG AA** — upstream's light bases are 2.9–3.3:1 under white text, and `hover:bg-*/90` alpha-composites over the page so the base needs hover headroom (light bases → 0.48 L, dark warning → 0.67, dark info-foreground → dark text; computed in gamma-encoded sRGB, Codex-recomputed to four decimals, every enabled pair ≥ 4.5) — plus `Bordered` on the token border instead of upstream's hardcoded zinc. bench-check gained token-variant assertions (bg/text emission, family distinctness, Bordered currentcolor guard); mutation pass 4/4 kills. Full tier 226/226, Android webview variant drive PASS on-device (see app-ui Findings)
- [x] Decide Playwright-in-CI before marking ui-work-loop implemented — the spec's open question is deferred "to phase end" but the polish task doesn't carry it, so it would expire unanswered (specs: [ui-work-loop](ui-work-loop.md)) — **decided: no** — the e2e tiers stay a local-loop precondition for `[x]`; the gate stays creds-free. Four grounds (standing no-Neon-creds rule; hosted-auth fixture dependency; concurrent-run write races on the seeded e2e user; per-push gate latency), the technically-workable-but-declined anonymous `--no-deps` subset, and explicit revisit conditions all recorded in ui-work-loop Findings + the Decisions log. Codex adversarial review of the decision: 4 findings, 3 accepted (rationale de-overstated ×2, stale playwright.config.ts tier comment fixed), 1 no-change
- [x] Count stepper (custom): hover/focus-revealed `− n +`, click-to-type, keyboard ±, commit-on-blur; optimistic update + undo toast; standalone bench section — **done**: `app/src/components/ui/count_stepper.rs` (composes vendored `Button`+`Input`; caller-owned `value` signal, optimistic `on_commit(from,to)` fired last, undo toast re-commits through the same channel) + bench section (`app/src/bench/count_stepper.rs`: happy-path + failing-save steppers). Three lifecycle traps solved: blur-commit is **deferred** (the display⇄edit swap fires a spurious `focusout` — immediate commit would close edit mode the instant a click opens it; removing the defer breaks click-to-type, mutation-verified), the ± buttons `preventDefault` pointerdown + programmatically anchor focus (WebKit doesn't focus a clicked `<button>`), and the edit field is the vendored `Input` so the `value` attribute is seeded. Codex review 4/6 fixed (deferred-commit disposal guard, read `label` before `on_commit`, edit-mode `max` clamp, inert ± at bounds) + 2 addressed in e2e; mutation pass strengthened commit-cardinality + optimistic-first assertions (2 gaps kill-verified), min-bound dead-session gap documented as unobservable/harmless. Verified: hydration CLEAN (anon+authed), bench-check CLEAN, `android-stepper-check.mjs` PASS on Chrome 145, full three-browser tier 250/250, gate green. Wiring into the collection view rides `/my/collections/:id` (app-ui Findings) — **build click-to-type on the vendored `Input`, not a raw `<input>`**: `bind:value` is a client-side binding that emits no `value` attribute, so a hand-rolled SSR'd field renders empty until wasm lands. `Input` seeds the attribute from `bind_value` itself (PR #43); a bare element re-inherits the trap, and it is invisible in review because the field merely looks empty for a beat (specs: [app-ui](app-ui.md))
- [x] Double-faced cards have no way to see the back face. `/cards/:id`, the hover preview and the touch sheet all render face 0 only, and the heading shows the combined `"Front // Back"` name — so the back is unreachable in the UI. Needs a flip control on the card art that swaps **both** the image and the oracle block (name / mana cost / type line / text / P-T). Scope note from the card-detail task: `CardDetail.card_faces` already carries the per-face *oracle* data as raw jsonb, but the **back-face image is not plumbed through** — `PrintingSummary.image_uri` flattens to a single string, while the `printings.faces` jsonb array holds an `image_uris` per face. So this needs a DTO change (a per-face image list) plus the projection to fill it, not just a UI control. Applies to `transform` / `modal_dfc` / `reversible_card`; `split` / `flip` / `adventure` / `aftermath` are one image and want rotation or nothing, so key the control off `layout` rather than "has more than one face". **No ingestion work needed** — verified on the dev POC catalog that both faces' `image_uris` are present in `printings.faces` and per-face names/text in `cards.card_faces`; it is entirely projection + DTO + UI (specs: [app-ui](app-ui.md), [data-model](data-model.md)) — **done**: `shared::BACK_FACE_LAYOUTS` (widened to `double_faced_token`/`art_series` — same per-face art in the data, same defect) drives one gate across SQL (`summary_select()` CASE), the server-side `CardSummary.faces`/`CardDetail::flip_faces()` builds (both fail closed), and the UI; `PrintingSummary.face_image_uris` + `CardFaceSummary` DTOs (`serde(default)` for version skew); `FlipButton` on the detail hero and inside both preview bodies swaps name/mana/type/oracle/stats **and** art per face, combined name kept as a subtitle. Codex review: zero findings; mutation pass 4/4 kills after it exposed the art-pairing gap (tests only checked "src changed" — now exact equality in previews, `/front/`↔`/back/` path on detail). Verified: shared units 26, hydration CLEAN anon+authed, full tier **265/265**, `android-dfc-check.mjs` 12/12 on-device, gate 8/8 (app-ui Findings)
- [x] `/my` All cards view: aggregate table with three-count columns + expandable location summary, quick search, keyset paging (specs: [app-ui](app-ui.md)) — **done**: `app/src/my/all_cards.rs` over a reworked `CollectionStore::all_cards(q, page)`. Three framings changed on contact: the everything-view was **missing every card you only want** (`all_cards` inner-joined `holdings`, so a desired-but-held-nowhere card existed on the shopping list and nowhere in `/my` — now `held FULL OUTER JOIN wanted`); "same row treatment as collection view" is enforced by rendering the **same DTO** (`AllCardsRow` carries a whole `CardSummary`, so the row reuses `CardPreview`/flip and the SQL reuses a new `summary_select_with()`), with `owned`/`in_collections` *removed* as fields and derived instead so no stored copy can contradict `locations`; and the spec's `7 across 3 collections` phrasing only works plural, so the WHERE column has three shapes (`—` / `3 in Trade Binder`, linked / the expandable summary). `NeedLocation` → `CardLocation` (identical shape, two callers). `/catalog`'s query bar was extracted to `app/src/components/query_bar.rs` and shared rather than copied — four separately-earned behaviors (debounce, `self_pushed` URL⇄field guard, per-search-session history granularity, timer cleanup). Quick search is a plain name substring, not the catalog grammar, but through the same escaping helper (`?q=%` matches nothing). Paging is forward-only by design (Back walks the pages; the pager adds "Back to the start"), and a search edit drops the cursor. **The dev seed grew a bulk box** (`app/src/seed.rs`, own sentinel, 60 cards, skips whatever is currently short) because at ~19 distinct cards against a 50-row page the "Next page" link was unreachable by any browser test. Codex review: **zero findings in the feature code**, 3 in the tests, all 3 fixed (WANTED verified against itself → now cross-checked against the shopping list's independent projection; Next-page never clicked → now clicked; the paging probe unregistered → `probe:*` npm scripts + e2e-suite skill). Verified: unit 85, full three-browser tier **301/301**, paging probe at limits 3 and 7, hydration CLEAN anon+authed (incl. `?q=` and `?cursor=`), bench CLEAN, `android-all-cards-check.mjs` 10/10 on Chrome 145, gate green (app-ui Findings)
- [x] `/my/collections/:id` binder/deck view: header counts + needs chip, folder rows above cards, deck variant (format/commander header, type groupings, Want-led default, teardown), stepper in place, mobile drill-down (specs: [app-ui](app-ui.md)) — **done**: `app/src/my/collection.rs` (binder + deck variant on one page — breadcrumb, mobile drill-down that walks *up the tree*, needs chip, commander card with derived colour identity, folder rows above cards sharing the three numeric columns, `CountStepper` in HERE, deck type/board sections with slot counts, Want-led add default, teardown dialog, in-collection quick search and keyset paging) over a reworked `CollectionStore::collection_view` — which **returned no desire-only rows** until this task (inner join on `holdings`, so a card a deck wants and doesn't hold didn't exist in the view while the chip counted it; same `FULL OUTER JOIN` fix `all_cards` needed), plus `totals`, `commanders`, `holding_id`, `faces` and `q` threaded through trait / native client / router. Two adversarial review rounds, 8 findings then 6. Round 1's **blocker**: committing HERE to 0 ran `DELETE FROM holdings` while the stepper's own Undo re-POSTed the dead id — success toast, copies gone; floored at `min=1`, with undoable removal filed for the move-flows task. Also fixed: a tree refetch remounting stepper rows and silently disarming Undo, a URL-keyed optimistic delta that double-counted the header after teardown, and three render rules that were **unfalsifiable against the seed fixture** (no rollup, no nested folder with children, no repeated printing — fixed in `app/src/seed.rs` with a guarded `Depth Box → Depth Shelf → Depth Drawer` block). Round 2's needs-chip finding **disputed and upheld** (the IA puts the chip on "a deck or collection header"; the spec's deck-variant bullet was the line that read wrong, corrected). Verified: merge gate all 8 steps green, `cargo test --workspace --exclude frontend` 123 passed, hydration CLEAN on 7 URLs, SSR curls on binder + deck, Android probes 7/7 + 6/6 + CDP PASS, full three-browser tier **367/367**. Round 2's remaining findings parked as Phase 5 discoveries by maintainer decision on loop cost (app-ui Findings)
- [x] Quick-add panel: `command`+`popover`+`input`+`kbd` composite; keystroke contract `↑↓ ⏎ ⇧⏎ ⌥⏎`; Want-led in decks, Have-led elsewhere; typeahead adapter; e2e asserts the keystrokes; time-to-enter-50 observations recorded (POC-catalog realism deferred) (specs: [app-ui](app-ui.md)) — **done**: `app/src/components/quick_add.rs` wraps the page's `QueryBar` in a `Command`, with a pure `decode(key, shift, alt, rows, counting) -> Action` keystroke contract (12 unit tests), candidates from `search_catalog` keyed on the committed `?q=`, and an `IN THIS COLLECTION` context section folded out of the collection read. Supporting seams: `use_command_nav()`/`CommandNav` lets a **foreign** input drive `command`'s registry, `QueryBar` gained `on_key`/`reset`, and `quick_add` gained a server-clamped `quantity` (`clamp_quick_add_quantity`, `QUICK_ADD_MAX = 99`) because `⇧⏎ set count` would otherwise need one undo per copy. **The `command` ordering caveat needed no fix** — the list is rebuilt inside a `Suspend` per query, the full-remount case the caveat exempts. **Deviation: not the vendored `popover`** — `popover="auto"` light-dismisses on the same `pointerup` that focuses the field, and the router detaching this route's subtree hides a showing popover *without* firing `toggle`, desyncing the `open` signal; an absolutely-positioned panel with hand-rolled Escape/outside-pointerdown has neither problem, and the destination picker keeps using `popover`. **time-to-enter-50: 50 cards / 350 keystrokes (7.0 per card) / 1 pointer action / 97.3 s (1.95 s/card)**, of which typing is 0.36 s — the rest is the 250 ms debounce plus round trips. One review round, **zero majors**, 12 minors filed below. Verified: gate all 8 steps green (135 tests), hydration CLEAN on 3 anon + 5 authed URLs, bench probe CLEAN, SSR curls on binder (`⏎ Have`) and deck (`⏎ Want`), full chromium tier **131/131** (8 new), Android CDP + `android-quick-add-check` PASS (app-ui Findings)
- [x] Selection tray (custom gap component), read-only: docked thumbnail stack + count + "Move to…" (inert) + clear; cross-view selection state that survives mode switches and navigation; mobile docking above the tab bar; bench section in the same commit. **No writes** — the tray collects a selection and renders it; the move it triggers lands in the next task (specs: [app-ui](app-ui.md)) — **done**: `app/src/components/ui/selection_tray.rs` (the pill) + `shell::SelectionTrayDock` (the fixed positioning), splitting the wireframe's own Tray Wrap / Selection Tray frames so the bench can render the pill inline. The load-bearing decision is the **enum** selection key — `Held { collection_id, printing_id, board }` for collection rows vs `Card { oracle_id }` for `/my` rows, because a `/my` row aggregates by oracle and `CardSummary::printing_id` is only a has-art-first *representative*. Chosen over `from_collection_id: Option<Id>` on purpose: `None` in `MoveItem` means external intake, so an Option key would let batch move conjure copies; **the enum makes that a compile error and hands batch move a required resolution step**. `board` rides along even though `move_cards` is hardcoded to `'main'`, so mainboard/sideboard rows stay 1:1 with their checkboxes. Selectable where copies exist (`present > 0` / `owned > 0`); desire-only rows get no checkbox. Cross-view survival verified structurally — `provide_selection()` runs exactly once in `AppShell` above the whole routed subtree, and `Checkbox` is fully controlled, so the `/my/collections/:id` DOM churn re-derives rather than resets. Review CLEAN, zero majors; nine minors filed below, three of them severity-checked by the orchestrator rather than accepted on the reviewer's call. A reported reproducible `smoke.spec.ts:92` timeout **did not reproduce** — 138/138 in 52.3 s at default workers — so the proposed config change was dropped and filed as a suspected flake. Verified web (e2e 138/138, hydration CLEAN ×4, SSR non-empty ×3) + Android 12/12 on-device
- [x] Batch move: wire the tray's "Move to…" to `move_cards` over `suggested_destinations` — destination picker reused from the catalog quick actions, batch write, one undo covering the whole batch (specs: [app-ui](app-ui.md), [collection-api](collection-api.md)) — **done**: `app/src/my/move_selection.rs` + a `move_selection` server fn, writing through the existing `CollectionStore::move_batch` (collection-api's actual "Move (batch)", one transaction, all-or-nothing). Picker **extracted, not forked** — `catalog/destination.rs` now exports `DestinationList`/`DestinationOption`/`DestinationChoice` for both call sites, with tray options folded across the selection (`selection_destinations` sums shortfall per collection → `wants 2`) and ranked ahead of the rest. One undo for the batch needed a new `undo_moves(Vec<Id>)` trait method: the ledger has no batch id, so looping the per-move undo would be N transactions with a partial-revert failure mode — the defect shape this repo already hit. **Review found one major, promoted from the implementer's own "annoying" follow-up**: `present` groups by `(printing_id, board)` so a foil-only row is selectable, while `holding_take` matches the full grain — the entry returned `Conflict` and the single transaction killed the *entire* batch behind an error naming no card, on data present in dev today. Fixed by **refusing, not widening the write**: new ungrouped read `holdings_of_oracle`, resolution as pure functions deciding *movable* before any `MoveItem` exists, and named per-entry `Grain`/`Board`/`NoCopies` refusals that stay checked while the rest of the batch moves. Mutation-verified (reverting `movable` fails both new tests with the reported whole-batch death). The third task's fence held — no grain on `MoveItem`/`CardRow`/`holding_take`. RLS confirmed `FORCE`d on `moves`/`holdings`/`collections`, so the new undo route cannot reach another user's moves. Verified web (e2e 143/143 at `--workers=1`, hydration CLEAN, before/after data read back through the API) + Android 16/16 on-device
- [x] Undoable removal + deck teardown: `move_cards(to = None)` addressed by **grain** — needs finish/condition/language on `CardRow` (or a grain-addressed write) and a `board` on `move_cards`/`holding_take`, both currently hardcoded to `'main'`. Then drop the count stepper's `min=1` floor (it exists only because committing 0 ran `DELETE FROM holdings` behind a success toast) and add the deck "Empty deck…" teardown — single destination or "Return to previous locations". **Carries the trait/backend work of the three; the binder-removal discovery below is this task's real acceptance criterion** — **stage boundary** (specs: [app-ui](app-ui.md), [collection-api](collection-api.md)) — **done**: `migrations/0009_move_boards.sql` puts `from_board` **and** `to_board` on the ledger (a move's ends need not agree, and undo must reverse both exactly), and new `CollectionStore::move_holding` is addressed by **holding id**, resolving grain/board/collection/quantity off that row `FOR UPDATE` **inside the write transaction** — which also closes the row-path TOCTOU batch move had to leave open. The `min=1` floor is gone and the defect behind it is unreachable, not re-routed (`set_holding_quantity(id, 0)` now has no UI caller). Undo re-creates the holding with a **new id**; the row refetches via `HoldingsRevision`, and a stale id errors visibly rather than touching another row. Latent bug fixed in passing: `add_holding`'s intake move recorded no board, so undoing a `+ Have` onto a sideboard took copies off the mainboard. Batch refusals became capabilities — `SkipReason::Board` is gone; `Grain(n)` is deliberately kept for a stack with several grains and no default one. **Seven mutations, seven deaths**, including the two silent-corruption cases (undo ignoring the ledger's board; the write using the default grain). Review: **zero code majors**, one *operational* blocker — the deploy ordering below. Verified web (e2e 149/149 at `--workers=1`, hydration CLEAN, round trips read back through `/api/cards/{id}/holdings`) + Android stepper/collection probes green. ⚠️ **`scripts/migrate.sh prod` must run before this merges** — the new code names `moves.from_board`/`to_board` in three places and cannot run against the unmigrated schema; reads are untouched, so an unmigrated prod would look alive while every write 502s
- [x] Needs view + pick list + `/my/shopping`: owned-elsewhere vs short buckets, Pull / Pull-all → pick list grouped by source (client-composed from `move_cards` + `suggested_destinations`), shopping list page with text export (specs: [app-ui](app-ui.md)) — **done**: `app/src/my/needs.rs` + `app/src/my/shopping.rs`. **The task line's composition was wrong and the spec is being corrected, not the code** — `suggested_destinations` ranks *destinations* for a card, but here the destination is the page itself, so the read needed is source-side; shipped as `needs` + `holdings_of_oracle` + `move_batch`. **Quantity is never the caller's**: `PullItem` carries no quantity at all, and `pull_needs` re-runs the same allocation over its own fresh RLS-scoped read, accepting only *which* (card, source) lines to apply; a gap closed since render refuses as the new `SkipReason::NoLongerNeeded`. Review proved the pick-list walk is **order-independent** (locations sorted quantity-desc ⇒ draining one line leaves the others' greedy shares unchanged), and that the buckets cannot diverge from the needs chip (identical CTEs; `sum(allocate(gap, locations)) == owned_elsewhere` is an identity). **Needs stays board-blind, deliberately**: a board-aware need would manufacture rows whose Pull cannot work, since moves always land `to_board = main` — pinned by an e2e that fails if `needs()` is made board-aware. Review CLEAN, zero majors; ten of twelve minors filed, two fixed (the empty state's unqualified "Nothing missing" claim, and a hole where duplicate `PullItem`s multiplied a pull). Verified web (e2e 154/154 at `--workers=1`, hydration CLEAN, pull round-trips read back through `/api/cards/{id}/holdings`) + Android probe green including a real webview tap. ⚠️ Stacked on the removal/teardown PR — inherits its `scripts/migrate.sh prod` prerequisite
- [x] Catalog paging beyond the first page: `?cursor=` in the URL + a load-more/next-page control on `/catalog` (the search adapter already takes a cursor; the page only ever renders page one) — deferred from the catalog-page task (specs: [app-ui](app-ui.md), [catalog-search](catalog-search.md)) — **done**: `catalog_url` gained an always-explicit cursor parameter, the results `Resource` is keyed on `(url_q, url_cursor)`, and a forward-only `Pager` (Next + "← Back to the start") mirrors `/my`'s shapes. **Cursor clearing is a property of the URL the existing writers build**, not a new writer watching `q` — review's exhaustive grep found exactly three call sites navigating to a `/catalog` URL with a query (`QueryBar::commit`, `rail::use_navigate_query`, `ViewSwitch::go`), the first two passing `None`, with `use_commit` and `reset_all` both funnelling through `use_navigate_query`; no path can change `q` while keeping `cursor`. The **view switch preserves the cursor** deliberately (relayouting is not a query edit; the keyset compares tuples), which forced the pager hrefs to be reactive closures rather than baked. The filed rail/debounce race was **not compounded**: no timer and no `Effect` on `q` were added, so an armed debounce firing after a Next click commits page one of the newest text. One deliberate deviation from `/my`: its error arm is a dead end, but paging makes an error reachable *with nothing to fix* (a shared cursor link can be stale), so the error and empty arms both offer a way home. **Mutation testing found a real survivor** — "the last page offers no next" passed with the cursor ignored entirely, because `bolt` fits one page — strengthened until it died. Review CLEAN, zero majors; eight minors filed. Verified web (e2e 151/151 at `--workers=1`, hydration CLEAN ×4 including a past-the-end cursor, SSR curl of a cursored URL renders page two) + Android 11/11 on-device
- [x] Catalog search never fills `CardSummary::owned`, so the tile's "N owned" badge is unreachable: `HostedBackend::search`'s projection selects no `owned` column (only `card_summary` does), leaving it `None` on every search hit — the badge in `CardTile`/`ResultsList` is dead code and an authed catalog looks identical to an anonymous one. Needs the `owned_by_card` join inside the scoped tx, authed-only. Found while writing the card-detail e2e, which had to stop using `owned` to locate seeded holdings (specs: [app-ui](app-ui.md), [collection-api](collection-api.md)) — **done**: new `owned_by_oracle(&[Uuid])` does **one** `SELECT … WHERE oracle_id = ANY($1)` inside `scoped_tx`, and `card_summary` was rewritten onto the same helper so the tile and the detail page cannot drift. **The scoped and unscoped reads were kept separate deliberately** — folding the join into the paging query would wrap the public, cacheable, hottest read in a transaction for a column anonymous callers never see, or else duplicate the grammar/keyset/cursor construction; ownership also stays out of the paging query so it structurally cannot affect which rows return. `native.rs` needed no change (verified: opportunistic `catalog_backend` + JSON round trip). **Tenancy verified against real multi-tenant dev data** — `security_invoker=true` from the migration and live `pg_class`, `FORCE ROW LEVEL SECURITY` on both tables, pool role `rolbypassrls=false`, and for two oracle_ids genuinely owned by two different users the authed API returned this user's count, not the other's and not the sum. The card-detail e2e now *uses* `owned` to locate seeded holdings instead of walking four candidates. Review CLEAN, zero majors; seven minors filed. Verified web (e2e 153/153 at `--workers=1`, hydration CLEAN ×4, SSR shows the badge authed and **zero** occurrences anonymously) + Android anonymous half on-device
- [x] Set facet as a real picker: add a `list_sets` read adapter and replace the rail's comma-separated set-code text input with a multi-select over actual set names/codes (specs: [app-ui](app-ui.md), [catalog-search](catalog-search.md)) — **done**: `CatalogStore::list_sets` (trait, both backends, `GET /api/catalog/sets`, thin GET server fn, anonymous — sets carry no ownership) + `SetPicker` in the rail; `RailState.set` became `Vec<String>` and the comma round-trip is gone. **The selection is the query text and is never intersected with the fetched rows** — that is what stops the widget silently dropping a pasted code, and it means there is no "unknown code" branch to get wrong: `s:xyz` behaves exactly like `s:mh3`, as does a real code outside the 25-row window. **Bounded server-side search, forced by a measured O(n²)**: `CommandItem`'s `highlighted` memo calls `visible_ids()`, which clones the items vec and reads every item's `visible` signal, and `Command`'s highlight-reset `Effect` invalidates all of them per keystroke — so ~1050 sets is not viable (review confirmed the complexity claim independently). Rows remount fully per query, which is the safe case for the recorded `visible_ids()` ordering caveat. **Review found one major**: three states — not-fetched, fetch-failed, and genuinely-empty — were crammed into `Vec<SetSummary>`, so a failed fetch and a collapsed section both rendered "No set matches.", an authoritative claim about the user's catalog (and on native an offline phone is the *normal* failure). Fixed with `Option<Result<…>>` and four arms, an error arm carrying the shared `describe_error` wording plus a **Try again** that bumps an attempt counter, and the loading arm finally reachable; the lazy fetch was kept. **The failure was induced for real** (`page.route` fulfilling a 500 against the GET server fn), and two mutations each killed exactly their own test, with liveness proved by `strings`-ing the served wasm rather than trusting the watch. Verified web (e2e 162/162 at `--workers=1`, hydration CLEAN, SSR shows chips + list for `?q=s:mh3,lea` and zero rows bare) + Android rail probe 11/11 on-device
- [x] ⌘K command palette: `command`+`dialog`, desktop logged-in only, Places + fixed 3-command registry per design/command-palette.md — **`command` ordering caveat:** its item registry is built in *mount* order and `visible_ids()` returns that order, which equals DOM order only while items mount in document order or fully remount. If this surface keys a list that **reorders in place**, ↑↓ will silently visit rows out of visual order — add the `compareDocumentPosition` sort the V3 review described (app/src/components/ui/command.rs, the `visible_ids` comment). Remounting the list per query needs nothing. (specs: [app-ui](app-ui.md)) — **done**: `app/src/components/palette.rs`, mounted once in `AppShell` and rendering nothing unless desktop **and** signed in. **The caveat was live here and remounting alone did not discharge it** — three facts, each measured: a plain `{move || …}` closure is *not* a remount (tachys diffs an unkeyed view collection positionally and reuses the DOM nodes while re-running the item registrations, so `command`'s registry grew while the nodes stayed), fixed by `<For each=move || [rows.get()] key=RowSet::key>` — a `<For>` of **one** keyed on the row set's identity; and even then `CommandItem` registers from its *component body*, which Leptos runs at **view-construction** time, so building both groups at their `let` bindings and only *then* choosing DOM order left places always registered first. Typing one `n` drew COMMANDS on top while `visible_ids()[0]` was `Inbox` — highlight in the second group, `↑` clamping, top rows unreachable upward. Fixed by deciding order *before* constructing either group; **the `compareDocumentPosition` sort stays deferred**, now also because `visible_ids()` is a measured O(n²) path and a DOM-ordering pass would put layout reads inside it. Three deviations from the design, each forced: `should_filter=false` with a hand-written fuzzy ranker (the primitive's lowercase `contains` cannot match `trabin` → `Trade Binder` and cannot rank); **groups ranked by their top match** rather than fixed, because "grouped" and "best match pre-selected" conflict under a fixed order (and this is what made DOM order variable in the first place); and an invented cold-start PLACES group, without which a first-ever `⌘K ⏎` would run `New binder…`. `Undo last move` is **session memory, not an endpoint** (no `undo_last_move` server fn; it races a second tab), and the review's two orchestrator-promoted majors were both that invariant — teardown recorded nothing, so `⌘K → Undo last move` reversed an *older unrelated* move and left the teardown standing (`TeardownReceipt` now carries `move_ids` instead of a count that could disagree), and a toast's Undo never cleared the record, so the palette replayed a dead id and raised a **false success toast over a no-op** (new id-matched `LastMoveState::forget`, since a toast outlives the row that raised it). An audit of all five UI-reachable ledger writers found no third offender. Verified: gate **8/8** on macOS incl. `three_rings`, full chromium tier **190/190** at `--workers=1`, hydration CLEAN anon ×4 + authed ×5, bench CLEAN, SSR curls showing zero palette markup with `id="tree-create"`/`Trade Binder` as positive controls, Android CDP PASS + `probe:android-palette` CLEAN at 540 px with `CommandDialog` itself driven on the real webview as the negative check's positive control. Major-1's regression test confirmed failing before the fix and passing after; the `forget` fix mutation-checked (app-ui Findings)
- [x] `scripts/seed-dev-data.sh` cannot run in the canonical devcontainer: it shells out to `psql` to resolve the e2e user's uuid from `neon_auth."user"`, and the image ships no postgres client — so the documented prereq for every `/my/*` screen only works on a host that happens to have psql. Per CLAUDE.md a missing tool is an image bug: add `postgresql-client` to `.devcontainer/`, or resolve the uuid without psql. Worked around host-side during the card-detail task (specs: [dev-environment](dev-environment.md), [app-ui](app-ui.md)) — **done**: `postgresql-client` added to `.devcontainer/Dockerfile` and merged with the card-detail task (PR #43); verified `psql 17.10` in a local build, and the published `dgoings/three-rings:latest` now carries it (arm64 digest `e08bb209`, pushed 2026-07-20). `seed-dev-data.sh` and `migrate.sh` both run in-container again
- [~] Tree move has no keyboard/touch path: reparent/reorder is HTML5 drag-and-drop only, which doesn't fire on touch (no drag on mobile) and has no keyboard equivalent — so on a phone or from the keyboard a collection cannot be moved at all. The IA calls for "move via context menu" alongside create/rename/delete (design/information-architecture.md → My cards), which the management task scoped to drag. Add a context-menu **"Move to…"** action (a destination picker over the collection list, reusing the `command`/`popover` picker; it commits through the same `reparent_collection` + `reorder_collection` adapters) so move is reachable without a mouse (specs: [app-ui](app-ui.md))
- [ ] Polish + full pass: empty/error/loading states everywhere, responsive audit against all 9 wireframe screens, full e2e tier + Android release smoke, resolve any `@flaky` quarantine; mark app-ui and ui-work-loop implemented — **stage boundary** (specs: [app-ui](app-ui.md), [ui-work-loop](ui-work-loop.md))

## Later / parked (not in the queue — promote to a phase before working)

### Phase 5 discoveries

- [ ] **The ⌘K palette has no focus trap and stacks over other modals** — two separate gaps in one surface, both in `app/src/components/palette.rs`: (a) `:1113-1123` focuses the field on open but installs nothing to keep focus, so `Tab` walks out of the dialog into the page behind the scrim, against the design's "the palette holds focus while open" — this is a **vendored-`Dialog` gap, not palette-specific**, so fixing it there fixes every dialog in the app; (b) `:716-721` the ⌘K window listener toggles unconditionally, including while a tree create/rename/delete dialog or the quick-add panel is already open, stacking a second overlay (specs: [app-ui](app-ui.md), [ui-components](ui-components.md))
- [ ] **`command` has no scroll-into-view for the highlighted row.** `CommandList` caps at ~21 rem, so `↑↓` past the fold moves the highlight off-screen with no scroll — the user is navigating a row they cannot see. Affects the ⌘K palette, the destination picker and quick-add equally (upstream's deleted vanilla-JS layer did this), so it belongs in `app/src/components/ui/command.rs`, not in any one consumer. Related: `visible_ids()`'s known O(n²) now runs over the palette's whole row set on **every keystroke** — fine at fixture scale, felt by a user with hundreds of collections, and no cap was added (specs: [ui-components](ui-components.md), [app-ui](app-ui.md))
- [ ] ⌘K palette minors from its review round, none reaching the major bar: (a) `palette.rs:954-968` — `RowSet::key` omits `Place::meta`, so a collection reparented while the palette is open keeps its stale parent-path meta (`Rares — Shoebox` after it moved under Trade Binder); only reachable if the tree changes mid-open; (b) `palette.rs:954` — the same key concatenates place `token`+`name` and command `slug` with one `\u{1f}` separator and **no section delimiter**, so a collision needs a collection literally named `new-binder`/`new-deck`/`undo-last-move`, but a section marker would cost nothing; (c) `my/needs.rs:731-780` — `Undo last move` from the palette bypasses the pull's `on_undo` callback, so a pick-list line stays ticked/struck-through (`data-state=pulled`) after the copies were put back and `toggle` (:741) refuses to re-tick it; self-clears only when the pick panel is closed; (d) `palette.rs:1103-1109` — `is_mac()` reads the deprecated `navigator.platform` (`navigator.userAgentData.platform` is the non-deprecated read but is not in web-sys, which is why it was not used); (e) `palette.rs:110` — `tr_recent_places` in `localStorage` is per-**origin**, not per-user, and is not cleared on sign-out, so foreign collection ids survive a user switch and are only silently discarded by `at_rest`'s index reconcile (:406) — no names leak, but the reconcile is the only thing standing between it and a cross-account row; (f) `my/tree_manage.rs:121` — hoisting `provide_tree_manage` to the shell means `create_open`/`rename_open`/`menu_target`/`drag` now survive a Catalog ⇄ My-cards mode switch (previously reset by `CollectionTreeNav` remounting), so a dialog left open across a mode switch reappears on return; (g) `TreeDialogs` sits inside the `hidden md:block` aside, so tree dialogs are invisible below `md` — pre-existing and harmless for a desktop-only palette, but it means `New binder…` has no mobile story if the palette ever gains one; (h) `Undo last move`'s **recording** is verified end-to-end only on the removal path (the e2e drives a real removal and reverses it via ⌘K, asserting through `/api/cards/{id}/holdings`); the add / batch-move / pull record sites are one-liners beside toasts whose own Undo buttons are covered elsewhere, so a dropped `record` call on those three would not fail the suite (specs: [app-ui](app-ui.md))
- [ ] The teardown **toast** still has no Undo button. `⌘K → Undo last move` can now reverse a teardown (the palette task gave `TeardownReceipt` its `move_ids`), so the ledger side is done and the confirm dialog's "every move is in the history" is finally true — but the toast that appears after emptying a deck offers no direct way back, and the palette is desktop-only, so on a phone a teardown is still one-way through the UI (specs: [app-ui](app-ui.md), [collection-api](collection-api.md))
- [ ] **Nothing enforces `.claude/skills/` ⇄ `.agents/skills/` parity, and four of six mirrors had silently gone stale.** Found 2026-07-26: `.agents/skills/` was last written at `225c87c` (#55) — one commit *before* the cost recalibration in `3671084` (#57) — so `adversarial-review`, `e2e-suite`, `ui-task-loop` and `validate` still prescribed the loop the maintainer had explicitly switched off: the full three-browser tier "at the end of every task", unbounded review rounds, and mutation passes. An agent reading the mirror would have reproduced the ~1.44M-token behavior the recalibration exists to prevent, while the `.claude` copy said otherwise; the drift is invisible because both trees are plausible in isolation. Resynced by copy (`diff -rq` clean on all six, zero Codex-specific divergence in either tree, so byte-identical is the correct relation), but the repair is one-time and the gap is structural. Cheapest durable fix is a merge-gate step asserting `diff -rq .claude/skills .agents/skills`; the alternative is making one tree a symlink of the other, if the agent harnesses tolerate it (specs: [ui-work-loop](ui-work-loop.md), [delivery-pipeline](delivery-pipeline.md))
- [ ] A set chip cannot show its set **name** unless that set happens to be in the current 25-row window — chips render the uppercase code. Resolving selected codes to names needs a by-codes read (specs: [app-ui](app-ui.md), [catalog-search](catalog-search.md))
- [ ] `CommandItem`'s `aria-selected` means "keyboard-highlighted", so the set picker's **multi-select membership rides `data-selected` and is not exposed to assistive tech**. Fixing it properly is a primitive-level a11y change affecting every `command` consumer (specs: [ui-components](ui-components.md), [app-ui](app-ui.md))
- [ ] The set picker offers **sets with no ingested printings** — the dev catalog has 1045 sets but only ~2976 printings, so most picks return zero results. Near-moot in prod with a full ingest; a "has cards" filter would be a join (specs: [catalog-ingestion](catalog-ingestion.md), [catalog-search](catalog-search.md))
- [ ] Set-picker minors from its review round, none reaching the major bar: (a) the server-side search window is fixed at **25 with no "N more" indicator** — `q=commander` matches 108 sets in the dev catalog and shows 25, newest-first, so a set reachable only past the window is silently unreachable through the picker and only the query bar can name it (`SetQuery::limit` exists; the `list_sets` server fn never passes one); (b) the search term is interpolated into `ILIKE '%' || $1 || '%'`, so **`%` and `_` typed into the box act as SQL wildcards** — parameterized so not an injection, just wrong-looking results; (c) `ORDER BY released_at` resolves to the `released_at::text AS released_at` **output alias**, not the `date` column, so the sort is lexicographic on text — correct only because ISO `YYYY-MM-DD` collates like the date, and a `DateStyle` change would silently reorder the picker; (d) **two identical `list_sets` round trips per SSR** of any `?q=s:…` — the shell's `FilterRail` and the page's `FilterSheet` each own a `SetPicker` with its own `Resource` (50 `set-option` rows in one document, half inside a `md:hidden` sheet); sharing one needs shell-level provision; (e) a **debounced-read / immediate-Enter race**: `CommandInput` sets `ctx.query` per keystroke but the row list re-keys 250 ms later and `should_filter=false` keeps stale rows "visible", so typing toward "limited edition alpha" and pressing Enter mid-window adds `s:leb` (Beta sorts first, newer) rather than `s:lea` — visible and removable, but a term the user did not type; (f) `s:mh3,mh3` (the grammar's `csv` does not dedupe) renders **two chips with identical `data-testid`/`data-code`** — a Playwright strict-mode violation for any future test, and removing one leaves the row's ✓ showing while the badge drops 2→1; (g) `filter-rail.spec.ts`'s "a set pick drops the page cursor" has **no positive control** that the cursor was present on the loaded page, so it would pass if `page.goto` never carried one; (h) the unit test `a_code_the_picker_never_lists_is_still_the_users_selection` rewrites `"s:xyz"` using state derived from `read("s:xyz,mh3")` — two different fixtures, so it really only tests `toggle_code` + `set_term`; (i) `EitherOf4` inside a `Suspend` is a first use in this repo and its hydration is covered only by the passing probe, not by a targeted assertion (specs: [app-ui](app-ui.md), [catalog-search](catalog-search.md))
- [ ] **A recurring vacuous-test shape, filed as a pattern rather than a fifth individual finding.** Four instances surfaced in one session, all the same failure: *a test cannot distinguish behaviors its fixture does not distinguish.* (1) `addHave` posted only `{printing_id, quantity}`, so every fixture holding was `nonfoil/nm/en/main` and the grain path was untestable across the whole suite — batch move fixed it by giving `addHave` a grain; (2) the dev seed desires every card from exactly one collection, so "WANTED is the cross-collection sum" and "WANTED is the max" are indistinguishable (already filed separately, needs a third seed block); (3) catalog paging's "the last page offers no next" passed *with paging disabled*, because `bolt` fits one page — caught by a mutation pass and strengthened; (4) `catalog.spec.ts`'s `ownedBadgeFor(none.oracle_id)).toHaveCount(0)` passes whether the tile is present-without-a-badge or absent entirely, and needs a positive control. **The generalizable guards**: a negative assertion needs a positive control proving the subject was rendered at all; a fixture whose values are uniform cannot pin an assertion about those values (add a distinctiveness precondition that fails loudly, as the owned-badge test now does); and an assertion sharing provenance with the code under test has no independent authority — both owned-badge mutations were killed *only* by assertions that read as redundant. Worth promoting into the e2e-suite skill as authoring rules (specs: [ui-work-loop](ui-work-loop.md))
- [ ] Owned-badge minors from its review round, none reaching the major bar: (a) `hosted.rs:314` — `search` now opens an RLS transaction it previously never needed, so a failure in the ownership read turns a previously-working catalog search into a **500 for signed-in users only** (anonymous keeps working); there is no fallback degrading `owned` to `None` while still serving results, and adding one is small; (b) `catalog.spec.ts` — the negative badge assertion is vacuous if the tile is absent (see the pattern entry above); (c) `catalog.spec.ts` — `top`/`none` are chosen from a `limit=15` API call while the page renders `limit=50`, sound only because `ORDER BY c.name, c.oracle_id` makes the first 15 a prefix of the first 50, and that coupling is undeclared; (d) `catalog.spec.ts` — the SSR pin `expect(html).toContain("${top.owned} owned")` is a whole-page substring check that any other card with the same count satisfies, and the exact-number assertion runs post-hydration, so nothing pins the exact number to a specific tile pre-hydration; (e) `hosted.rs:100` — `EXPLAIN` shows the `holdings` side as a `Seq Scan` per page (irrelevant at 101 rows; belongs with the existing large-collection aggregate item); (f) `catalog.rs:760` — `ResultsList`'s `ml-2` moved off the `Badge` onto a wrapper `<span>`, equivalent today only because `Badge` renders `inline-flex`; (g) `android-owned-badge-check.mjs` covers only the anonymous half by design (the dev proxy strips Cookie headers), so the authed on-device badge is never exercised (specs: [app-ui](app-ui.md), [collection-api](collection-api.md))
- [ ] Three definitions of "owned" live in `hosted.rs` — the `owned_by_card` view (now shared by `search` and `card_summary`), `all_cards`' inline `held` CTE, and `collection_tree`'s `shopping_short` `o` CTE. They agree today; nothing enforces that they continue to (specs: [collection-api](collection-api.md))
- [ ] Promote two traps out of code comments into the skills that would prevent them: (a) the **`{..}` spread trap** — a component prop ending in a bare path immediately before `{..}` parses as struct-update syntax (`E0797` plus a bogus "no field on PropsBuilder"), documented only in a `cards.rs` comment and rediscovered twice; belongs in **vendor-component**; (b) **`cargo leptos watch` silently drops a save that lands while a rebuild is in flight**, and `touch` does not retrigger it (it appears to hash content) — only a genuinely different content change forces the rebuild, so any mutation pass must confirm its mutation actually compiled before trusting a green run; belongs in **e2e-suite** or the loop skill (specs: [ui-components](ui-components.md), [ui-work-loop](ui-work-loop.md))
- [ ] **The native backend splices `cursor` into the upstream query string unencoded** (`app/src/backend/native.rs:221-223`): only `q` goes through `urlencode`, so a hand-pasted `/catalog?cursor=x%26limit%3D200` injects extra upstream parameters on Android/desktop. Bounded — the limit is clamped, only the pasting user's own session is affected, no auth material rides the query string, and RLS still scopes the read — so it is hygiene rather than an auth hole. **Pre-existing in `collection_view` and `all_cards` too**, which is why this is its own task: fixing only the catalog call site would leave the pattern inconsistent, so encode the cursor at all three. Found by the catalog-paging review, which made it reachable from a public page (specs: [app-ui](app-ui.md), [data-access-backends](data-access-backends.md))
- [ ] Catalog-paging minors from its review round, none reaching the major bar: (a) `catalog.rs:279-281` — the result count states *this page's* row count with no page qualifier, so the last page of a 73-result search reads "23 results" while mid-pages read "50+ results"; the same number feeds the mobile filter sheet's "Show N results" footer. Keyset has no offset, so a "51–73 of 73" form needs a separate count query or a page ordinal in the URL; (b) `catalog.rs:560-575` — the previous result set's "Next page →" stays clickable while a newer search is in flight (the `Transition` keeps the old view), and clicking it navigates to `(old_q, old_cursor)`, after which `QueryBar`'s re-seed effect reverts the box to `old_q` — **silently undoing the text the user just typed**; self-consistent but surprising, and the most user-visible of these; (c) `catalog.rs:551` — `paged` flips as soon as the URL moves, so during an in-flight Next the still-displayed page one grows a "← Back to the start" control before page two arrives; (d) `catalog.rs:550` — on a single-page result set an empty `<nav aria-label="Pagination">` still renders, a named landmark with no content; (e) `catalog.rs:180-184` — `last_good` now retains page-N rows, so a later grammar error on a fresh page-one query renders page-N results as the dimmed "stale" set underneath it; (f) `end2end/android-catalog-paging-check.mjs:93-103` — step 4 has the exact survivor weakness that *was* fixed in the Playwright suite: it asserts "no Next" but never asserts the pre-cursor card is absent, so it passes on a cursor-ignoring build because `bolt` fits one page (specs: [app-ui](app-ui.md), [catalog-search](catalog-search.md))
- [ ] No catalog equivalent of `probe:paging` — nothing walks the whole search keyset asserting no duplicate or skipped rows across page boundaries. The search keyset is covered only by the `#[ignore]`d `query_engine` test. Related: a corrupt cursor surfaces as `ApiError::Validation`, so the UI renders "invalid cursor" in the *query* error box even though the query is fine — a variant of the already-filed server-fn status-semantics item (specs: [catalog-search](catalog-search.md), [ui-work-loop](ui-work-loop.md))
- [ ] Reverse paging (Previous) is unbuilt on **both** paged surfaces — `/my` and now `/catalog`. Both are forward-only for the same structural reason: a keyset cursor means "everything after this row", so Previous needs a reverse-ordered query plus a `before` cursor. Browser Back covers a session that walked forward, but a deep-linked cursored page has no way back except the start. Build it once and share it if it is worth building (specs: [app-ui](app-ui.md), [collection-api](collection-api.md), [catalog-search](catalog-search.md))
- [ ] **The selection checkbox is a 16×16 px tap target on mobile** — `SelectionCheckbox` renders the vendored `Checkbox` at `size-4` and the surrounding padding sits on the `<td>`, not on the control, so only the inner 16 px responds. On a 390 px phone this is the tray feature's primary interaction, well under the 44 px touch guideline, and the next cell is the card-detail link — a mis-tap navigates away. Held at minor on the selection-tray review because the navigation is recoverable and the selection survives it, but this was the closest of that round's nine to a major. Fix is small: wrap the control in a padded `<label>` so the whole cell is the target (specs: [app-ui](app-ui.md), [ui-components](ui-components.md))
- [ ] Selection-tray minors from its review round, none reaching the major bar: (a) the tray counts **entries, not copies** — a selected row holding 4 reads "1 card"; decide the label's meaning alongside the batch-move quantity semantics, which is why this one matters most; (b) multi-grain rows (`holding_id == None` with `present > 0`, a cell summing several finish/condition/language holdings) are **selectable**, and `Held { collection, printing, board }` cannot say which grain — the count stepper already refuses those cells for exactly this reason, so batch move must either resolve the grain or drop the checkbox there; (c) selecting the same physical card from `/my` (oracle grain) and from a collection row (held grain) yields two tray entries reading "2 cards", two identical thumbnails, and no way to tell them apart; (d) a visible toast paints over the tray's clear "×" on both breakpoints (sonner is `fixed bottom-6 right-6 z-[200]`, the dock `z-50` at `bottom-16 md:bottom-0` — ~44 px of overlap at 1440 px, the bottom ~20 px of the pill on mobile); (e) the dock is `fixed inset-x-0` and centers on the **viewport**, so on desktop the tray is offset from the content column by half the 240 px sidebar rail rather than centered over the table it describes; (f) the `aria-live="polite"` count enters the DOM together with its first content (the whole tray is inside `Show`), so the **first** pick is typically not announced, only later changes; (g) selection entries can go **stale** — a key can reference a holding or collection invalidated by a later stepper commit, teardown or deletion, and nothing prunes the tray; harmless while read-only, but batch move needs a stale-entry policy; (h) `end2end/tests/selection-tray.spec.ts:137-149`'s `collectionWithCards` accepts any collection with `present > 0` then waits for the first `collection-row` checkbox, so a collection whose held rows fall past the first name-ordered keyset page would hang the test — pin the fixture collection instead (specs: [app-ui](app-ui.md))
- [ ] `/my` has no "select all on this page" affordance — the header select column is `sr-only`-labelled but empty. Relatedly, the `/my` location breakdown (expanding a multi-collection row shows per-collection quantities) offers no per-collection checkbox, which is the natural place for the user to resolve the oracle-grain gap: a `/my` selection names only the card, so batch move cannot tell which collection the copies come from. A per-location checkbox would let the user answer that directly instead of the app guessing (specs: [app-ui](app-ui.md))
- [ ] The selection tray has no keyboard entry point: `specs/app-ui.md`'s per-row select affordance mentions `x` as a shortcut and only the tab-reachable checkbox shipped (specs: [app-ui](app-ui.md))
- [ ] Needs/shopping minors from its review round, none reaching the major bar: (a) `needs.rs:458` — **one `pending` signal is shared by every Owned-elsewhere row**, so one in-flight row Pull disables every row's button; (b) `needs.rs:724` — a tick is marked "pulled" on `!outcome.pulled.is_empty()`, so a line whose source lost copies between snapshot and tick is struck through even when *fewer* copies than the label moved, and the residual silently drops out of the walk; (c) `needs.rs:614` — a row-level Pull that closes a need while the pick list is open leaves those lines on the checklist, where they can only ever produce a `NoLongerNeeded` error toast; (d) `needs.rs:531` — a Pull whose items resolve to nothing raises **no toast at all** (`report` is a no-op on an empty outcome), a silent no-op rather than a refusal; (e) `needs.rs:872-916` — the Short table's Want/Here/Short columns don't visibly reconcile for a partly-fillable row (`4 / 0 / 2`), with no owned-elsewhere column explaining the difference; (f) `lib.rs:1296-1360` — `needs`, `holdings_of_oracle` and `move_batch` run in **three separate transactions**: check-then-act across transactions, the same window `move_holding` deliberately closed for the row path; (g) `move_selection.rs:951-959` — `every_refusal_says_what_is_wrong` was not extended with the new `SkipReason::NoLongerNeeded`, so the one reason this task added has no phrase-coverage assertion; (h) `needs.rs:166` — `default_grain` duplicates the private `move_selection::default_grain`; (i) `collection.rs:603` — `/my/collections/:id/needs` is linked **only** from the needs chip, which is `None` when nothing is missing, so the page's empty state is unreachable by navigation; (j) `shopping.rs:245` — when `execCommand("copy")` returns false the UI shows nothing, and the intended "the text is still selected, copy it yourself" fallback has no on-screen hint (specs: [app-ui](app-ui.md))
- [ ] The dedupe guard on `pull_needs` is pinned only at the helper, not at the call site: `a_duplicated_pull_line_does_not_multiply_the_move` tests `my::needs::dedupe`, so **deleting the `dedupe(...)` call in the `#[server]` body would leave the suite green**. The adapter was bound by direct evidence during the task (`POST /api/pull_needs` with the same line twice, gap 2, source holding 4 → one ledger row, `copies: 2`) but nothing standing guards it. Needs an API-level e2e; the same gap applies to any invariant enforced inside a `#[server]` body, which unit tests cannot reach (specs: [app-ui](app-ui.md), [ui-work-loop](ui-work-loop.md))
- [ ] Correct the task line's stale composition in specs/app-ui.md: the needs pick list is composed from `needs` + `holdings_of_oracle` + `move_batch`, **not** `move_cards` + `suggested_destinations`. `suggested_destinations` ranks destinations for a card, but on the needs page the destination is the page itself, so the read needed is source-side. The wording predates `NeedRow.locations` existing (specs: [app-ui](app-ui.md), [collection-api](collection-api.md))
- [ ] The dev fixture's Inbox has accumulated **88 desires for Lightning Bolt** from earlier quick-add e2e runs (owned 2 → shortfall 86), and `/my/shopping` is the first page that surfaces it — it dominates the list. Needs a cleanup pass, plus quick-add tests that scope their own wants so it stops growing. Related: `cleanup-mutation-leftovers.mjs` exists but is not wired into the loop, and timed-out Playwright runs still leak scratch collections past the `finally` (nine accumulated during one task's mutation runs) (specs: [ui-work-loop](ui-work-loop.md))
- [ ] The needs pick list has no print/share affordance, and no way to tick a **partial** quantity — you pulled 2 of the 3 a line asks for, and the tick is all-or-nothing (specs: [app-ui](app-ui.md))
- [ ] `set_holding_quantity(id, 0)` still `DELETE`s the row with no ledger entry — the same footgun that produced the stepper's `min=1` floor. No shipped caller reaches it (the stepper's `on_commit` intercepts `to == 0` and routes through `move_holding(to = None)` instead), so this is a loaded gun with no finger on it rather than a live defect. The clean fix is routing quantity *decrements* through the ledger too; making only the 0 case a move would be a new inconsistency, so it is a spec-owner call rather than an obvious patch (specs: [collection-api](collection-api.md))
- [ ] Removal/teardown minors from its review round, none reaching the major bar: (a) `collection.rs:1300` — `on_commit` captures `holding_id` for the row's lifetime, so after remove→Undo the stepper is re-shown with the **dead id** until the `revision` refetch lands, and an edit in that window shows "Couldn't save: not found: holding"; (b) `collection.rs:1260` — an *earlier* stepper Undo toast on the same row (e.g. 3→1) survives a subsequent removal, and pressing it posts the dead id and errors; (c) `hosted.rs:964` — `remove_holding` sends `quantity: None` (the whole stack) while the toast reports the *rendered* count, so a stack grown in another tab loses more copies than the toast names; (d) `collection.rs:989` — deck section slot counts (`SIDEBOARD · N`) ignore `here_delta`, so after a removal the section header contradicts the row's "—" and the page header until a refetch; (e) `collection.rs:1108` — a removed row keeps its selection checkbox (computed from the pre-removal `present`), so selecting it yields a `NoCopies` refusal instead of being unselectable; (f) `hosted.rs:1094` — `teardown` does not reject `to_collection_id == collection_id`; the UI excludes it but the endpoint accepts it, collapsing all boards onto `main` and writing ledger rows with `from == to` that `apply_move` forbids elsewhere; (g) `collection.rs:1461` — the teardown dialog says "every move is in the history", but `TeardownReceipt` carries only a count and **no surface undoes a teardown**; (h) `hosted.rs:2098` — `previous_location` ignores `undone_at`, so an undone move can still decide a "return to previous" destination; (i) `hosted.rs:1959`/`:2004` — the new `FOR UPDATE` locks rows in item order inside `move_batch`, so two concurrent overlapping batches in opposite order can deadlock into a 502; (j) migration 0009 has no backfill for pre-existing `add_holding` intake rows written at a non-`main` board (API-only path today), so undoing such a historical row still takes from the mainboard; (k) `routes.rs:343` — the `/// POST /api/moves` doc line is now attached to `move_holding` and `move_cards` lost its doc comment; (l) `bench/count_stepper.rs:66` — the bench toast reads "1 copies" while the product path pluralizes correctly (specs: [app-ui](app-ui.md), [collection-api](collection-api.md))
- [ ] Teardown's destination-grouped **preview** (collection-api:177) is unimplemented — the dialog confirms directly rather than showing what would go where. Related: nothing undoes a teardown, though the moves are in the ledger (specs: [app-ui](app-ui.md), [collection-api](collection-api.md))
- [ ] A multi-grain cell (`holding_id = None` — several finish/condition/language stacks summed into one rendered row) still has **no removal or edit affordance**, because the row cannot name which stack the control means. This is the one remaining "can't remove" case after the removal task, and it needs the per-row move affordance or a per-grain expansion the spec calls for (specs: [app-ui](app-ui.md))
- [ ] Three near-identical undo adapters — `undo_quick_add`, `undo_move`, `undo_selection_move` — all bottom out in `CollectionStore::undo_move`. Consolidate or document why each exists. Relatedly, the e2e API helpers are now duplicated across three spec files and want lifting into `helpers.ts` (specs: [app-ui](app-ui.md), [ui-work-loop](ui-work-loop.md))
- [ ] `scripts/migrate.sh` reported `migrations: up to date` on a run that had **not** embedded the new migration file — its `touch app/src/db.rs` did not force the re-embed in time, and a second run applied it. A migration tool that can report success without having applied anything is the wrong failure mode for the one manual step that gates a prod deploy. Make the embed deterministic, or have the script verify against the database rather than the binary (specs: [delivery-pipeline](delivery-pipeline.md), [data-model](data-model.md))
- [ ] **The e2e suite's `hydrated(page)` helper does not imply an island is interactive, and four tests flake on it under parallel load.** `hydrated()` waits on the global `<html data-hydrated>` stamp, which says the document hydrated — not that a *streamed* Suspense island's controls are wired up. Under worker pressure the click lands before the island is live, is swallowed, and the test times out waiting for what the click should have produced. Confirmed across `smoke.spec.ts:92` and `:114`, `collection-tree.spec.ts:65`, and `selection-tray.spec.ts:239`: a different subset fails each run at the default worker count (measured 2 failures / 141 passed on the batch-move task's pass), every one of them passes in isolation, and `--workers=1` gives a clean 143/143. **The loop's single full pass now runs at `--workers=1`** (recorded in the e2e-suite skill) as the cheap mitigation — 3.8 min vs 1.4 min unreliable plus a retry. The real fix is a hydration-aware click helper that waits for the specific island, which is why this is filed rather than patched: it spans three spec files and is a suite-wide policy choice, not one task's business. **Supersedes** the batch-move task's earlier "suspected flake, did not reproduce" note on `smoke.spec.ts:92` — it does reproduce; a single green run was not evidence of stability (specs: [ui-work-loop](ui-work-loop.md))
- [ ] Batch-move minors from its review round, none reaching the major bar: (a) `POST /api/moves/undo-batch` and `undo_selection_move` accept an **uncapped** `Vec<Id>` while `move_selection` caps at `SELECTION_MOVE_MAX = 100` — the undo counterpart has no limit; (b) `selection_destinations` silently `.take(SELECTION_MOVE_MAX)`s the oracle list instead of erroring like `move_selection` does, so a >100 selection gets a ranking computed from an arbitrary subset; (c) `MoveSelection`'s `suggested` resource keys on the deduped oracle list, so **every checkbox toggle re-fires it even with the picker closed**, and the server side is one sequential `suggested_destinations` query per oracle — a set-based query and/or gating on picker-open would fix it (gating alone introduces a re-render race where the test clicks); (d) resolution reads holdings in a transaction separate from `move_batch`'s, so "exactly one candidate" is decided against a snapshot the write does not hold — see the residual TOCTOU note in app-ui Findings; (e) `HoldingsRevision` is consumed only by `/my` and `/my/collections/:id`, so `/cards/:oracle`'s ownership block is **not** invalidated by a move; (f) the tray picker passes `empty="No collection to move to."` into `CommandEmpty`, so that string also appears when a *search* filters every row out (the catalog says "No collection matches." there); (g) `selection-tray.spec.ts` asserts that empty state with `toContainText`, which reads `textContent` while `CommandEmpty` hides via `style:display` — the assertion passes whether or not the element is shown, i.e. it is vacuous; (h) no client-side cap on selection size, so selecting >100 rows produces a raw server-fn error string in the toast rather than being prevented in the tray; (i) `SkipReason::Board` renders as "is on the side board" / "is on the maybe board" (spacing); (j) the `Toaster` (`fixed bottom-6 right-6 z-[200]`, 360 px) overlaps the tray dock (`z-50`), so a refusal toast can cover the tray's own "Move to…" and clear controls on narrow viewports — same overlap already filed from the selection-tray round; (k) two independent `list_collections` resources are live when the catalog picker and the tray are both mounted; (l) undo does not put the moved entries back in the tray (deliberate — revisit if it reads wrong) (specs: [app-ui](app-ui.md), [collection-api](collection-api.md))
- [ ] Batch move is one copy per entry, fixed server-side. A per-row quantity — or a "move every copy of this row" affordance — needs a quantity on `SelectedCard` and a tray control. The toast says "(1 copy each)" precisely because the pill counts entries, so this is honest today rather than wrong, but it is the obvious next capability (specs: [app-ui](app-ui.md))
- [ ] Ambiguous `/my` selections are **refused**, not disambiguated: a card in more than one collection (or with copies across several printings) comes back as `ManyCollections(n)` / `ManyPrintings(n)` telling the user to open a collection and select the row there. A "which copies?" disambiguation step in the picker would let the batch complete without leaving the page. Relatedly, non-default-grain and non-mainboard entries are refused by name (`Grain` / `Board`) until the removal/teardown task widens the write path (specs: [app-ui](app-ui.md))
- [ ] `/api/catalog/search` returns `owned: null` even for an authed caller — `into_summary(None)` at `hosted.rs:272`, while `card_summary` and `all_cards` fill it, contradicting `CardSummary::owned`'s own doc. Found during the batch-move review. **Note this is the same defect already filed above** from the card-detail e2e work (the `owned_by_card` join missing from `HostedBackend::search`'s projection); recorded here only because a second independent review rediscovered it, which is evidence it is worth doing rather than a separate item (specs: [app-ui](app-ui.md), [collection-api](collection-api.md))
- [ ] E2E hygiene: timed-out tests leak `zz-e2e-*` scratch collections into the dev branch, and duplicate names then make by-name locators ambiguous on the next run. Six leaked ones were deleted by hand during the batch-move task, and `batch-move.spec.ts` gives its collections random-suffixed names; the other specs still use worker+seq only. A `globalTeardown` sweeping `zz-e2e-*` would close it for good — **this restates the already-filed teardown item below** and adds the name-collision consequence, which is new (specs: [ui-work-loop](ui-work-loop.md))
- [ ] Superseded — see the `hydrated(page)` entry above. (Original note: suspected e2e load flake in `smoke.spec.ts:92` ("desktop mode switch swaps Catalog and My cards"): the selection-tray implementer reported it timing out **reproducibly** at the default worker count once seven tests were added to the `@fast` tier, passing only at `--workers=3`, and proposed lowering the suite's local worker count or swapping its `waitForURL(..., waitUntil: "load")` for `toHaveURL`. The task's full chromium run then went **138/138 in 52.3 s at the default workers** with that test green, so the "reproducible" premise the fix rested on did not hold and no change was made. Filed rather than dismissed: one green run does not prove it never flakes, and the diagnosis (a real document load of `/catalog` whose SSR exceeds 30 s under saturation) is plausible. If it resurfaces, `toHaveURL` is the cheaper fix than throttling the whole suite.) It resurfaced on the very next task, and the mechanism turned out to be hydration timing rather than SSR saturation — kept here as a record of how the wrong diagnosis was reached from one green run (specs: [ui-work-loop](ui-work-loop.md))
- [ ] `design/component-gap-analysis.md` was not updated for the selection tray. The vendor-component skill asks for deviations to be recorded there, but the tray is a custom component with no upstream to deviate from and the analysis already records `action_bar` as ruled out. Decide whether custom gap components get an entry there at all, or whether the skill's instruction should say "vendored components only" (specs: [ui-components](ui-components.md))
- [ ] **The `/my/collections/:id` route detaches and re-attaches its whole DOM subtree ~800 ms after every `?q=` navigation** (measured: gone ~400 ms, same nodes back). `/catalog` and `/my` do not. Blast radius beyond quick-add: it silently closes any native popover on the page (removing a showing popover fires no `toggle`, so the Rust `open` signal desyncs), rebuilds the DOM per search, and blurs the focused field — the quick-add panel only survives it via a 120 ms focus keeper, a workaround at one call site. Find what this route does that the other two don't (specs: [app-ui](app-ui.md))
- [ ] Quick-add panel minors from its review round, none reaching the major bar: (a) `PresentSection` has no empty-query gate, so with `?q=` empty — panel opened at rest, and the moment after every add clears the field — `IN THIS COLLECTION` renders the collection's entire first page; (b) `Escape` decodes to `Pass` when `rows == 0`, so Escape cannot close a panel open with no candidates (outside-click still can); (c) the two early-return failure paths in `add` (no destination; Have with no printing) leave `count` set, so a subsequent `⏎` without typing re-uses it — the `× 4` chip does render and any keystroke clears it, which is why this is a minor; (d) `quick_add.rs:190`'s doc claims `here` is "the same number the page's HERE column renders", but that cell renders `present` plus a separate dimmed `+rollup` — the sum matches `here_total`, not the cell; (e) candidates go through `search_catalog` (catalog grammar) while `collection_view`'s `q` is a plain name substring, so "answers to the same question" holds only for plain names, and a grammar error renders as panel text with no partial-term suppression; (f) the post-add `clear()` commits `""` with `replace = false`, so every add pushes a history entry; (g) after Escape the field keeps focus, so `focusin` cannot re-fire — reopening needs typing or a click away and back; (h) no `aria-expanded`/`aria-controls`/`aria-activedescendant` on the field, and the `role="listbox"` holds `<a>` links and a footer alongside the `role="option"` rows; (i) `use_command_nav().expect(...)` is a wasm panic rather than a compile-time guarantee; (j) `Candidate.index` is a position within its own result set while `nav.highlighted()` indexes the whole registry — if two result sets are ever briefly co-registered during a swap they disagree (two chips); (k) `Resource::get()` keeps the previous payload while refetching, so for one `collection_view` round trip after switching collections the panel's destination/kind/present describe the previous collection (cosmetic today — the header and body are equally stale) (specs: [app-ui](app-ui.md))
- [ ] `end2end/probe-add-tmp.mjs` is a stale temp probe committed by an earlier task and still sitting in `end2end/`. Delete it or promote it to a registered probe (specs: [ui-work-loop](ui-work-loop.md))
- [ ] `AddToast` has 8 fields and `QuickAddPanel` 10 props, both at the edge of comfortable — group them if either grows again (specs: [app-ui](app-ui.md))
- [ ] DFC flip follow-ups from the adversarial-review skill shakedown (two independent re-reviews of `c088006`, 2026-07-25 — the task Codex scored zero-findings): (a) **vacuous test, kill-verified** — dropping the `faces.len() < 2` guard in `CardFaceSummary::build` survives the shared suite 26/26; the "fail closed" claim's fewer-than-two-faces leg is untested, and a one-element `faces` list still ships over the wire to native clients; (b) no test asserts the mana-cost / type-line / stats swap — the e2e fixture card has no P/T on either face, so a stats-pinned-to-front-face mutation analytically survives every unit test, all four e2e tests, and the Android probe (`stats_line` itself has zero unit coverage); (c) the keywords badge row is card-level (Scryfall unions both faces) and doesn't participate in the face swap, so a back face renders front-only keywords beside back-face oracle text; (d) preview flip state survives close/reopen — `PreviewBody` mounts once behind the `hovered`/`sheet_seen` latches so its `face` signal persists, contradicting the "each starting at the front" comment; reset or re-document, and pin either behavior with a test; (e) flip-control a11y: static `aria-label`, no pressed/face state, no announcement on swap; (f) defense drift: `jsonb_array_elements(p.faces)` hard-errors the whole detail/search/summary query on a non-array where the old `->0` projection was shape-tolerant — unreachable via today's ingester, note or harden (specs: [app-ui](app-ui.md))
- [x] **A card cannot be removed from a binder at all.** — **fixed** by the removal/teardown task (2026-07-25): grain-addressed `move_holding` resolves grain and board off the holding row `FOR UPDATE` inside the write transaction, the `min=1` floor is gone, and the removal is undoable at the original grain and board. Mutation-verified. The one remaining case is a multi-grain cell (`holding_id = None`), filed separately above. Original entry: The binder/deck view's stepper floor is `min=1` because committing 0 ran `DELETE FROM holdings` while the stepper's own Undo re-POSTed the dead id (HTTP 500, copies gone behind a success toast). The floor makes that unreachable rather than reachable-and-lying, but teardown is deck-only and the spec's per-row move affordance is unshipped, so binder rows are now permanently un-removable. The real fix is an undoable removal — `move_cards(to = None)` addressed by **grain**, which needs finish/condition/language on `CardRow` (or a grain-addressed write) and a `board` on `move_cards`/`holding_take`, both currently hardcoded to `board = 'main'`. **The move-flows task must restore removal and drop the floor** (specs: [app-ui](app-ui.md), [collection-api](collection-api.md))
- [ ] Board-blind needs arithmetic contradicts board-aware rows on the same page: `needs()` and the collection view's totals group desires/holdings by `oracle_id` alone, while the card rows and deck slot counts group by `(oracle_id, board)`. A deck holding a card on `main` and wanting it on `side` renders a `Sideboard` row reading HERE — / WANTED 1 while the needs chip is absent and `/needs` returns `rows: []`. Pre-existing in `needs()`; the binder/deck view is what puts both on one page. Fixing only the page's totals would make the chip disagree with the page it links to — the honest fix is `NeedRow.board` + a board-aware `needs()` (specs: [collection-api](collection-api.md), [app-ui](app-ui.md))
- [ ] A timed-out Playwright test leaks its `zz-e2e-…` scratch collection into the dev branch: the `request` fixture is torn down before the `finally` runs, so `deleteCollection` fails with "Target page, context or browser has been closed". Documented as acceptable in `collection-tree-manage.spec.ts`, now also reachable from `collection-view.spec.ts` (two leaked during a mutation round). A `globalTeardown` sweeping `zz-e2e-*` would close it for good (specs: [ui-work-loop](ui-work-loop.md))
- [ ] Nine Android probes are unregistered folklore — only `probe:android-collection` is in `end2end/package.json`; `android-cdp-check`, `android-stepper-check`, `android-tree-check`, `android-all-cards-check`, `android-card-detail-check`, `android-dfc-check`, `android-quick-actions-check`, `android-rail-check` and `android-tree-manage-check` are not. An unregistered probe is one nobody runs. One commit registers the lot (specs: [ui-work-loop](ui-work-loop.md))
- [ ] The e2e `tr_jwt` cookie expires in ~20 minutes, and the symptom is the page rendering `Couldn't load this collection: unauthorized: invalid token` — which reads like a page bug and has now cost two separate debug cycles. `npx playwright test --project=setup` refreshes it. Worth a line in the e2e-suite skill, next to the storageState note (specs: [ui-work-loop](ui-work-loop.md))
- [ ] Collection-view test-strength leftovers from review round 2, all kill-verified: (a) the `here_delta` reset's `is_some()` guard is **inert** — removing it survives the whole spec — and the comment justifying it is factually wrong about Leptos 0.8 (`ArcAsyncDerived` never clears `value` on re-run, and `<Transition>` renders nothing during a refetch rather than the stale payload); correct the comment and either test the guard or drop it; (b) the deck `color-identity` badge is asserted against `/commanders` → `commanders_in` → `union_color_identity`, the same helper that produced the badge, so reversing WUBRG order in `shared/src/tags.rs:139` survives the e2e (only `cargo test -p shared` kills it) — `cards_with_tag` already returns `color_identity` per card and the test types it away; (c) the "three columns agree" test flaked 1-in-3 full-tier runs on firefox because the write tests (`stepper`, `floor`, `teardown`) all seed scratch collections from `somePrinting()` = Trade Binder's first card, oscillating the global `owned` that test reads — point the write tests at a Depth-Box printing (specs: [app-ui](app-ui.md))
- [ ] The stepper's zero-floor e2e covers only the `−` click and typed-`0`-then-⏎; the keyboard (`ArrowDown`, `-`), paste, negative-number and non-numeric paths were verified live by review but are unasserted (specs: [app-ui](app-ui.md))
- [x] **The UI work loop costs too much per task and has no stopping rule.** The binder/deck task ran ~2.5 h and 1.44M subagent tokens (impl 60 min/454k, review 1 30 min/198k, fix 26 min/523k, review 2 36 min/269k). Drivers: (a) the full three-browser tier grows every task (196 → 355 → 367) and implementer *and* reviewers each run it, ~6 runs per task — only the gate should; (b) the mutation pass is uncapped at 10–12 mutations, each costing a rebuild/serve/revert cycle; (c) review rounds are unbounded, and round 2's findings had already drifted from correctness to craft — cap at 2 rounds with round 2 scoped to blockers/majors, surviving minors becoming discoveries; (d) task lines that bundle three surfaces (this one: binder + deck + stepper + teardown + mobile + search + paging) get absorbed by one agent in one context. — **done** (2026-07-25, maintainer decision): the loop is recalibrated for an early-stage MVP with one developer, not a hardened production service. **Chromium only** (firefox and webkit are not run at all; cross-browser deferred wholesale, WKWebView to be covered by desktop later); **one e2e run per task** after the review fixes, never by implementer or reviewer (they use `@fast` chromium); **one review round**, code-only, running no tests, blockers/majors only; **no mutation passes** (assertion strength gets its own sweep after the spec work); **minors are filed as Phase 5 discoveries, never fixed**. Bundled task lines get flagged for splitting before dispatch. Landed in `specs/ui-work-loop.md` (Design + Calibration + Findings) and the `ui-task-loop`, `adversarial-review` and `e2e-suite` skills (specs: [ui-work-loop](ui-work-loop.md))
- [ ] Cross-browser compatibility pass, deferred wholesale on 2026-07-25: the loop now runs **chromium only**, so firefox and webkit breakage accumulates unseen from the binder/deck task onward. `end2end/playwright.config.ts` still defines both projects, so the debt is one `npx playwright test` away from being measured. Decide when to pay it down — and note the original webkit rationale (it proxies WKWebView, so a webkit-broken overlay breaks the macOS shell) now rides on the desktop smoke instead (specs: [ui-work-loop](ui-work-loop.md))
- [ ] Assertion-strength sweep, deferred from the per-task loop on 2026-07-25: mutation passes are off, so vacuous tests are no longer caught at the moment they are written. Two known-vacuous patterns already shipped and are recorded in the `e2e-suite` skill as authoring guidance rather than checks — a test can only distinguish behaviors the **fixture** distinguishes, and overflow must be measured on the scroll container. Run one deliberate mutation sweep across the accumulated suite once the Phase 5 spec work lands (specs: [ui-work-loop](ui-work-loop.md))

*Relocated from Stage 3 on 2026-07-26 (maintainer decision): deferred bugs, perf
notes and open decisions that had accumulated in the stage list. Stage 3 keeps only
real UI surfaces and blockers. `/my` reverse paging was dropped rather than moved —
the broader both-surfaces entry above already covers it.*

- [ ] Server-fn error channel loses `ApiError` status semantics: `api_err` collapses every variant onto `ServerFnError::ServerError` (HTTP 500), so a 422 validation reaches the UI as a 500 carrying the right message — the JSON API channel maps status correctly, the Leptos channel doesn't. Needs a custom server-fn error type across all adapters; Codex review medium on the catalog-page task, disputed-and-deferred (specs: [app-ui](app-ui.md), [collection-api](collection-api.md))
- [ ] Google sign-in honors `?next`: carry the post-auth destination through the web OAuth callback (state param) and the Tauri poll path — Codex review medium on the shell task, deferred (specs: [app-ui](app-ui.md))
- [ ] `+ Want` cannot be undone: undo is the `moves` ledger's `undone_at` flag, but `add_desire` writes no ledger row and the trait exposes no desire-quantity operation to compensate with — desires can only be added, board-relabeled, or cascade-deleted with their collection. So the catalog quick-add's Want toast deliberately ships **without** an Undo action while Have has one (specs/app-ui.md Findings). Needs `set_desire_quantity(desire_id, SetQuantity) -> Option<DesireLine>` on `CollectionStore` mirroring `set_holding_quantity` (0 deletes the row), both backend implementations, a `POST /api/desires/{id}/quantity` route + path constant, then wire the toast's action. Note the quick-add adapter has no desire id today — `add_desire` returns `DesireLine`, so `quick_add` would need to carry it back in `QuickAddReceipt` for the toast to target. Found building the destination-picker task (specs: [collection-api](collection-api.md), [app-ui](app-ui.md))
- [ ] Rail edit vs pending query-bar debounce: a facet click inside the 250 ms window is overwritten by the debounce firing with its captured text, losing the rail edit. Needs the pending-timer handle shared between the rail (`app/src/catalog/rail.rs`, which commits through its own `use_navigate_query`) and the query bar (`app/src/catalog.rs` — the `StoredValue` timeout handle is private to `QueryBar`). The context has to be local-storage rather than a plain one because `use_navigate`'s closure is not `Send`; note that is a constraint on the *navigate* closure, not on the `TimeoutHandle` itself. Codex review medium on the filter-rail task, deferred (specs: [app-ui](app-ui.md))
- [ ] Rail sections do not open when a filter arrives from the query bar: `<details>` openness is seeded once (so a reactive re-derive can't slam a section shut mid-click), which means typing `r:rare` shows only the collapsed Rarity summary badge. Decide whether a first-time-populated section should spring open (specs: [app-ui](app-ui.md))
- [ ] Overlay children are instantiated eagerly, and the V2 review's "revisit only if a specific overlay's content proves expensive" has now been triggered: the card-detail previews duplicated every card name into the DOM and broke three pre-existing catalog tests, fixed with a per-caller latch. Decide whether `dialog`/`popover`/`sheet` should gate children on open by default so the next consumer doesn't rediscover the latch pattern (specs: [app-ui](app-ui.md), [ui-components](ui-components.md))
- [ ] `#![recursion_limit]` — **diagnosed 2026-07-26 (rustc 1.96.1), and the original entry was wrong on both of its load-bearing claims.** Measured by lowering `app/src/lib.rs`'s limit to rustc's default 128 and compiling every configuration: (1) **It is not Android-specific.** A plain host `cargo build -p three_rings` reproduces it exactly, as does a host `cargo build -p frontend --target wasm32-unknown-unknown`. Meanwhile `cargo check -p app --features native --target aarch64-linux-android` *passes* at 128. So no NDK, emulator or Android job is needed to test it — the "only detonates on `aarch64-linux-android`" attribution was an artifact of build order, not a property of the target. (2) **There is one limit that matters, not two.** The error is `error: queries overflow the depth limit!` and rustc's help names the crate to fix: *`app`* — even when the crate being compiled is `frontend` or `three_rings`, because the offending generic (`hydrate_async`'s async block over the rail's stacked `<details>` sections, `app/src/catalog/rail.rs`) originates in `app`. Raising `frontend/src/lib.rs`'s 256 does nothing for it. (3) **`cargo check` and `cargo clippy` are structurally blind to it**, so none of the gate's six clippy lines can ever catch it: the overflow happens while *computing the layout* of a monomorphized async block, which is a codegen-time query that `check` never runs. Only a full `cargo build` sees it. (4) **Headroom is 2×**: `three_rings` fails at `app` = 128 and builds clean at 256; the committed value is 512. What this leaves genuinely uncovered is narrow — the gate's `cargo leptos build --release` does codegen the wasm/`hydrate` instantiation, so a shared view tree is already protected; only a view tree reachable *solely* under `#[cfg(feature = "native")]` is never monomorphized in CI. Cheapest closure is one `cargo build -p three_rings` step in validate.yml, which needs no new tooling (the runner already installs Tauri's Linux libs for the workspace clippy line) (specs: [app-ui](app-ui.md), [ui-work-loop](ui-work-loop.md), [delivery-pipeline](delivery-pipeline.md))
- [ ] Hoist `CardPreview`'s pointer-type detection into one shared context: it currently runs a `match_media` + signal per card (~60 per catalog page) for a global fact, and has no `MediaQueryList` change listener, so a convertible flipping modes mid-session keeps the stale arrangement. Cost multiplies as Stage 3 surfaces adopt `CardPreview` (specs: [app-ui](app-ui.md))
- [ ] The `/auth/app-return` bounce and OAuth callback pages are raw HTML served without the stylesheet, so they can't take theme tokens and will render unstyled under the dark default. They aren't in the route map, so a responsive/visual audit would skip them (specs: [app-ui](app-ui.md), [auth](auth.md))
- [ ] The merge gate never builds a Tauri **release**, which is how a crash-on-launch shipped to the published `latest` release (2026-07-20, both the `.dmg` and the APK: `build_router` renders the app to enumerate routes, which spawns, and the Tauri setup hook held no Tokio runtime — specs/architecture-spike.md Findings). The gate only *lint-checks* `src-tauri`; nothing ever exercises the embedded-server startup path, and the validate suite's own web coverage cannot see it because `server` is `#[tokio::main]`. Cheapest real coverage is a headless launch assertion, not a full bundle: build the release binary and assert it binds its port and answers `GET /catalog` (the crash was pre-window, so no GUI is needed). Decide where it runs — a linux release build in the existing gate job would have caught this one, since the bug is platform-independent (specs: [ui-work-loop](ui-work-loop.md), [delivery-pipeline](delivery-pipeline.md), [architecture-spike](architecture-spike.md))
- [ ] Fractional sibling `position` can collide / exhaust precision: drag-reorder drops the moved node at the midpoint of its neighbors' positions (`plan_drop`, app/src/my/tree_manage.rs). Repeated inserts between one pair, or an existing tie, can produce a position equal to a neighbor's, so `ORDER BY position, name` falls back to name and the node lands off from where it was dropped. Unreachable at the POC scale (integer seed positions; ~50 midpoint inserts between one pair to exhaust f64) — deferred rather than building a rebalance. Fix when it bites: renumber a sibling group to evenly-spaced integers when a midpoint gets too close to a neighbor (Codex review, tree-management task) (specs: [app-ui](app-ui.md), [collection-api](collection-api.md))
- [ ] The dev seed desires every card from exactly one collection, so "WANTED is the cross-collection **sum**" and "WANTED is the max" are indistinguishable against it — the `/my` mutation pass could not kill a max-instead-of-sum regression, and neither will the needs/shopping specs. Add a third independently-sentinelled block to `app/src/seed.rs` (the `BULK` block's shape) that wants one card from a second collection, then assert the summed total explicitly. Note the trap the bulk block hit: a new block must not quietly own the cards the tree deliberately wants and holds nowhere (specs: [app-ui](app-ui.md))
- [ ] A vendored `collapsible`'s `class` padding leaks height when closed: the prop lands on the inner div, and `min-h-0` zeroes its content box but not its padding, so `class="pt-1"` leaves a visible sliver of open track under every collapsed row (found on `/my`'s location summary, worked around by moving the padding onto the child). Either apply `class` to the clipped region or document the constraint on the prop — the current signature reads as if padding is safe there (specs: [ui-components](ui-components.md), [app-ui](app-ui.md))
- [ ] Three near-identical percent-encoders now exist — `catalog::encode_query_value` (query values, shared with `/my`), `shell::encode_path_for_query` (paths, keeps `/` literal) and `backend::native::urlencode` (the native client). The differences are real but undocumented as a set, so the next surface will pick one by proximity rather than by rule. Consolidate into one module with the two variants named, or write down which is for what (specs: [app-ui](app-ui.md))


### UI / Performance issues noted during testing

- [ ] Catalog first load is slow
- [ ] Catalog card size adjustment (resizes with window, but default largest size should be capped, cards get too big currently)
- [ ] Black background too dark, card borders blend in
- [ ] DFC flip should be viewable on main catalog page (and everywhere the card is rendered besides hover view)
- [ ] No back button from card detail view to catalog page
- [ ] Auth flow on web doesn't open another browser tab, final "you can close this tab" doesn't redirect back to the app. Works for desktop, not on web 

### Catalog updates

- [ ] collection-api → keyset for needs / shopping-list (follow-up). `needs(collection)` and the global `shopping_list()` currently return full result sets — bounded in practice but unpaginated. If profiling at real scale shows them hot, add keyset paging like the other reads (specs: [collection-api](collection-api.md))
- [ ] catalog-ingestion → full load + update flow. Full Scryfall bulk import (bootstrap; thereafter the manual rebuild/true-up that also refreshes prices), then the manifest-driven daily incremental (`/cards/manifest` sweep → `/cards/collection` hydration) on a Render cron job + Scryfall migrations reconciliation, all idempotent; mark catalog-ingestion implemented — prereq: the POC subset (specs: [catalog-ingestion](catalog-ingestion.md))
- [ ] card-tagging → per-card tag orphan cleanup (follow-up). When a card's **last** holding and desire leave a deck (quantity → 0 via the stepper / a move / teardown of a single line), drop its now-orphaned `card_tags` rows. Whole-collection teardown is already covered by the `ON DELETE CASCADE` on `card_tags.collection_id`; this is the per-line case on the holdings/desires write paths, surfaced only if a UI shows stale tags (specs: [card-tagging](card-tagging.md), [collection-api](collection-api.md))
- [ ] Bundled read-only catalog for offline browsing on desktop/mobile (deliberately deferred)

### Other

- Full-catalog quick-add disambiguation + list-perf validation — Phase 5 builds against the ~3K-printing POC subset (maintainer decision 2026-07-17), so type-ahead disambiguation realism and large-result-list performance can't be judged until the full ingest (parked above) runs; re-exercise the quick-add and catalog screens then
- Validate the add-flow keystroke budget against real usage (disambiguation frequency at 4–6 typed chars; ⇧⏎ set-count for playsets) — needs the implemented add flow; projected numbers in ui-design Findings
- Large-collection aggregate performance — profile owned / present-rollup / needs at catalog-scale collections (~100K rows) and, if hot, promote `owned_by_card` to a materialized view + add collection-read indexes (data-model's named escape hatch). Needs the implemented collection endpoints + real-scale data ([collection-api](collection-api.md) §Read models, [data-model](data-model.md))
- Decks and sharing features
- Import/export (CSV, Moxfield)
- App-update delivery, Android — **Play Store internal-testing track** (maintainer decision 2026-07-13): $25 one-time dev account, CI upload (fastlane or a gh-action), Play provides update notifications + in-place installs; adopt when the first non-maintainer tester appears. Version plumbing already ships in artifacts.yml (`versionName 0.1.0+<sha>`, monotonic `versionCode`). Interim sideload polish (versioned release tags + Obtainium) evaluated and declined; a bespoke in-app APK updater rejected outright (Play makes it throwaway)
- App-update delivery, desktop — **tauri-plugin-updater, deliberately paired with the deferred Developer ID + notarization work** so the signing pipeline is touched once (notarization recipe in [delivery-pipeline](delivery-pipeline.md)): minisign update key as a repo secret, CI publishes `latest.json` + `.app.tar.gz` + `.sig` beside the `.dmg`, in-app check → prompt → swap → relaunch. This is the long-term update path for direct-download macOS distribution — only a Mac App Store listing would supersede it

## Decisions log

- 2026-07-25: **Phase 5 loop goes fully Claude-driven — Codex removed from the loop.** Implementation and adversarial review each run in a clean-context subagent — the reviewer via the new `adversarial-review` skill (review-only findings contract, repo trap hunt list, mutation pass with the rebuild-wait mechanics) — while the main session is a pure orchestrator: queue/git bookkeeping, host-process lifecycle, fix-loop arbitration, gate, land. Grounds (the human-only `/codex:*` commands + fire-and-forget rescue agent forced main-session polling and implementation-level context into the orchestrator; the card-detail task already needed a substitute reviewer) and the preserved review discipline are in ui-work-loop Findings. The Codex plugin stays installed for human-invoked use; permission allowlist unchanged.
- 2026-07-23: **Playwright stays out of the merge gate** (resolves ui-work-loop's deferred open question before the spec flips to implemented). The e2e tiers remain a local-loop precondition for `[x]` — full three-browser tier every task — and the gate stays creds-free. Grounds: running the *real* suite (authed, data-backed) always ends at a Neon credential in GitHub, against the standing no-Neon-creds rule (reaffirmed 2026-07-16 for the ingestion cron); the auth fixture's hard dependency on the *hosted* Neon Auth service (all three browser projects depend on `auth.setup.ts`, so a CI-local seeded PG can't run the suite at all — per-run Neon branches *would* carry their own branch auth, but that's the credential again: policy, not feasibility); concurrent CI runs racing the suite's writes on the one seeded e2e user (push + PR events are separate concurrency groups — structural flake); and per-push minutes on the workflow auto-merge waits on. A creds-free **anonymous-only** subset (`--no-deps` skips the auth setup) is technically workable but declined on marginal value — it exercises only the anonymous shell, duplicating the loop's per-task probes. The creds-free CI runtime smoke worth having is the already-queued headless release-binary launch assertion. Explicit revisit conditions (self-hostable auth / Neon OIDC-federated short-lived tokens / non-loop contributors / creds-free write isolation) recorded in ui-work-loop Findings, with the Codex review of the decision (4 findings, 3 accepted) noted there.
- 2026-07-16: **catalog-search spec reviewed + accepted — flat term grammar, substring SQL, name-order keyset.** V1 syntax = flat AND-of-terms (bare/name, `o:`, `t:`, `s:`, `r:`, `c:`, `id:`, `mv:` with comparisons), per-term `-` negation, and a **comma-OR micro-extension** (`r:rare,mythic`) — it's what the rail's multi-select facets serialize to, since flat Scryfall syntax can't express within-facet OR; no `or`/parens (real grammar, deferred). **Substring semantics everywhere** via trgm ILIKE; a generated `cards.oracle_search_text` column (+ GIN) closes data-model's multi-face `o:` gap; `t:` gets a type_line trgm GIN; `c:` handles multi-face via jsonb containment on `card_faces` (top-level colors is empty on DFCs by design). **Printing-scoped terms (`s:`/`r:`) share one EXISTS** (Scryfall's per-printing semantics); negated ones get their own NOT EXISTS. **Unknown syntax errors, naming the term** (never silently-wrong results); the "N advanced terms" rail indicator is dropped for v1. **Order = (name, oracle_id)** — Scryfall's own default and the shell's existing keyset — correcting collection-api's "relevance, then name" line (note added there); relevance deferred to an `order:`/tsvector extension. Reviewed and implemented **stacked on the ingestion-POC branch** (maintainer: rebase-merge, both PRs ride together) so the engine develops against the live dev subset.
- 2026-07-16: **catalog-ingestion spec reviewed + accepted — manifest-driven dailies, bulk for bootstrap/true-up, Render cron.** Facts verified live against Scryfall/Neon/Render docs and APIs (recorded in the spec): bulk files are now gzipped JSONL (streamable line-by-line); the `*.scryfall.io` bulk origins are rate-limit-free; every `api.scryfall.com` call needs an accurate `User-Agent` (403 without); upstream prices change once/day, period. Design: **two acquisition paths over one shared core** — the `default_cards` bulk path (POC filter → full bootstrap → occasional *manual* true-up that also refreshes all prices; hash-gated upserts so true-ups write only deltas) and the **daily incremental** (`/cards/manifest` sweep, 15,000 entries/page ≈ 8 pages ≈ 1 min → hydrate changed/new ids via `/cards/collection` 75/req → same upserts; new-card detection = unknown ids, the daily payoff). **Price freshness is an explicit non-goal** (maintainer): prices ride hydration + true-up cadence; no daily all-cards price job, and no daily 560 MB reprocessing. **Scheduling = a Render cron job** (~$1/mo minimum, at-most-one-run guaranteed, same root `Dockerfile`, command `server --ingest update`; the `catalog_ingest` credential lives *only* there — the web service keeps `app_runtime` only). Chosen over the same-day pg_cron due-marker idea (verified: no `http`/`pg_net` on Neon, ticks fire only while the compute is awake → usage-proportional freshness) and over GitHub Actions (standing no-DB-creds-in-GitHub rule). Scryfall `/migrations` reconciled catalog-side only (`merge`/`delete` iff unreferenced); user-row repointing is a manual owner runbook (RLS-forced tables; Neon can't mint `BYPASSRLS`). Manifest observation 2026-07-16: `data_updated_at`/`created_at`/`oracle_id` NULL on all sampled entries (maintainer's read: NULL is the steady state — cards rarely change); the diff reads them whenever present. New ingestion-owned migration queued with the work: `ingestion_runs`, `ingest_hash` + `manifest_*_updated_at` columns, `catalog_ingest` grants.
- 2026-07-16: **card-tagging tag/board API implemented — spec `implemented`.** The
  eleven-method tag/board surface landed on the `CollectionStore` trait (tag CRUD +
  scope, `assign`/`unassign`, `card_tags`/`cards_with_tag`, `deck_commanders`,
  `set_holding_board`/`set_desire_board`), each a `shared/tags.rs` DTO implemented
  once by `HostedBackend` and mirrored by `NativeBackend` over new hosted JSON
  routes. Decisions settled in implementation: **tag scope is derived** from the
  two nullable FKs (`TagScope::from_fks`), never a stored enum; **assignment
  enforces** deck-tag containment + card-in-deck (`Validation`) + the built-in caps
  (`commander` ≤ 2 / `companion` ≤ 1, counting *distinct* oracles and **excluding
  the one being assigned** so re-assign is idempotent), with **full
  legal-commander / companion-restriction validation deferred to a rules engine**
  (cap-only v1, matching the spec's lean); **deck color identity is derived on
  read** (WUBRG union of the commander-tagged cards' `color_identity` — no stored
  column, so "recompute" is inherent); **boards re-label in place** (upsert the
  destination-board row + `take_or_delete` the source, splitting/merging by
  quantity — **not** a `moves` entry) and are **rejected on binders** at the API
  (`Validation`), not just hidden in the UI. Proven end-to-end against the Neon
  **dev** branch with a temp driver (seed → exercise every path → assert → clean
  up, reverted). Per-card orphan `card_tags` cleanup on the last line leaving a
  deck filed as a follow-up (whole-collection teardown already cascades). Also
  closed the **data-access-backends native `401` silent-refresh + offline
  behavior** follow-up: `NativeBackend` now re-mints from `tr_session` on a `401`
  and retries once, with `Upstream` (unreachable) vs. `Unauthorized` (auth) made
  type-distinct (its own PR).
- 2026-07-16: **collection-api implemented — the full endpoint surface, both backends, verified on dev.** Built as six vertical slices, each proven end-to-end against the Neon dev branch before commit: tree CRUD + lazy inbox; holdings/desires writes + batch; CollectionView reads; moves (single/batch, undo, teardown, suggested-destinations); global reads (all-cards/needs/shopping); catalog (detail/summary/search shell). Every operation is a `CatalogStore`/`CollectionStore` trait method with a `shared/` DTO, implemented once by `HostedBackend` (sqlx) and once by `NativeBackend` (the HTTP client of explicit hosted JSON routes; paths in one shared module so they can't drift). Decisions settled in implementation: **endpoint surface = the explicit routes** (no per-op Leptos server-fns — those are a per-screen UI adapter, ride with the UI tasks); **`CardRow` grain = (printing, board)** with owned = global per-oracle, rollup = descendants per printing, desired = per (oracle, board); **moves are board-agnostic** (mainboard — board re-labels stay card-tagging's); **keyset pagination** on the large reads (opaque cursor + limit+1), **needs/shopping unpaginated** (bounded; follow-up filed); **catalog reads auth opportunistically** (HeaderMap→user_id, else anonymous); **ownership guard** validates the target collection is owned beneath the `holdings`/`desires` RLS (which only gates on their own `user_id`). Pull/pull-all are client-composed from `move_cards` + `suggested_destinations`. sqlx gained `uuid`+`json`. Full merge gate green in-container.
- 2026-07-16: **Data-access trait layer implemented; `ssr` decomposed into a substrate + two backends.** The `app` crate's `ssr` feature no longer *implies* Postgres. It is now the embedded-server **substrate** (router + auth core); a complete server build layers on exactly one **backend** — `hosted` (sqlx against Neon, the authorization terminus; what `server` builds) or `native` (an HTTPS client of the hosted API, **no sqlx linked**; what `src-tauri` builds), each implying `ssr`, with a `compile_error!` if neither is set. This retired the spike debt where the Tauri shell shipped the Postgres path. New **`shared/`** workspace crate holds the cross-backend DTOs + the one `ApiError` enum (wasm-safe: no sqlx/axum). Per-domain traits (`CatalogStore` anonymous / `CollectionStore` session-scoped), one backend struct per target carrying the per-request session credential; the hosted side runs every session query inside a `set_config('app.user_id',$1,true)` transaction so RLS scopes it beneath the terminus. Endpoints = trait methods projected to HTTP: the web UI calls in-process server fns, the native client calls explicit hosted JSON routes. Two CI consequences: a dedicated `cargo clippy -p app --features native` line (workspace feature-unification makes `hosted` win the backend cfg, masking the native-only arms that ship in the APK/`.dmg`), and the component-bench clippy line moved from bare `ssr` → `hosted`. **Prod migration deferred to post-deploy** (new code's `count(*) FROM cards` is compatible with both schemas, so migrating prod right after Render redeploys has no break window) and the native `401` silent-refresh stubbed (rides collection-api's native client) — both filed as follow-up tasks. sqlx gained the `uuid` feature.
- 2026-07-15: **Card annotation model added ([card-tagging](card-tagging.md) spec); `deck_commanders` superseded.** Reviewing the commanders table uncovered a broader gap: a deck needs many per-card categorizations — commander, mainboard/sideboard/maybeboard, companion, and user-defined labels (e.g. "Draw spells") at per-deck **and** per-account scope — a tagging problem the point-solution `deck_commanders` table didn't generalize (derived groupings by type/CMC/color stay read-time `GROUP BY`s, unstored). Split by cardinality: **boards** are a quantity-bearing partition → a `board card_board` column on `holdings`/`desires` (per-slot *physical-copy* allocation — "Option C" — was **rejected**: aggregate per-board quantity suffices, e.g. "2 Bolts main + 1 side; own 4, 3 in this deck"); **labels** (commander/companion/custom) are a `tags` + `card_tags` many-to-many, tags scoped system/account/deck via nullable `user_id`/`collection_id`, assignments anchored at `(collection, oracle)` so they span holdings+desires. Commander/companion become built-in (system) tags with app-enforced caps (≤2 / ≤1); commander retires `deck_commanders`. New spec `card-tagging` (`draft`) owns the design; [data-model](data-model.md) + [collection-api](collection-api.md) carry pointers + the endpoint surface. Two Phase 4 tasks added, **gated on card-tagging** — blocked until review accepts it (the queue's signal to review it next). Migration is expand-first (board defaults; `deck_commanders` empty).
- 2026-07-15: **Data-layer specs reviewed; Phase 4 recast from spec-fleshing to implementation.** Collaborative spec-review (2026-07-14) fleshed out and accepted the data-access/API stack: [data-access-backends](data-access-backends.md) (per-domain traits, one backend struct per target; owns the `shared/` types crate + the cross-backend error enum) and [collection-api](collection-api.md) (endpoints = the trait methods projected to HTTP; keyset pagination on *both* catalog search and collection reads, after the maintainer noted a collection can approach catalog scale ~100K; undo=flag, lazy inbox, app-side reparent cycle check, per-line batch add). [catalog-search](catalog-search.md)'s dependency was corrected — it depends on **data-model** (queryable-field vocabulary + base indexes), not catalog-ingestion, which is a *runtime* sibling; its SQL-vs-proxy OQ was already answered by collection-api (**SQL against our ingested catalog**), which put [catalog-ingestion](catalog-ingestion.md) on the critical path (POC representative-subset first, then full load + update flow). Workflow correction that drove the Phase 4 rewrite: "flesh out spec X" is the README's *blocked-queue spec-review meta-action*, **not** a queue task (no completed task in Phases 0–4 uses that pattern); and accepted→implemented is an explicit `mark <spec> implemented` step on a spec's terminal task (Phase 1/4 precedent), not an inferred transition. Phase 4 (renamed "data & API layer") now lists implementation tasks gated on `accepted` specs; still-draft specs (catalog-search, catalog-ingestion, ui-component-bench) keep blocked tasks until review accepts them.

- 2026-07-15: **ui-component-bench spec fleshed out; task repositioned as parallel UI infra.** The bench's second purpose — runtime SSR/hydration verification — was elevated to co-equal with styling review: the Phase 1b gap analysis reviewed the six interactive components (`dialog`/`popover`/`command`/`hover_card`/`sheet`/`sonner`) at source only and deferred runtime checks here, flagging four cross-cutting hazards (`use_random_id` SSR/client ID divergence, `window.ScrollLock`/vendored-asset dependence, `leptos_ui` nightly, and CSS-anchor positioning that silently breaks in WKWebView / Android WebView). The spec now carries a concrete per-adoption verification checklist and makes native-webview reachability a hard requirement. Decisions: gating = a dev-only **`component-bench` cargo feature** (over `debug_assertions`, so it can also be switched on in a local release build to exercise the embedded-Axum release SSR path); **build in parallel** — the task is independent of the blocked data specs (gated only on its own spec, renders just `table` today, value compounds with adoption), so it moves to lead the phase, and the "add the bench section in the same commit" convention is meant to be in force before component #2 is vendored; plus a bench-local theme toggle now and one page with anchored sections + jump-nav. Spec stays `draft` pending maintainer review.

- 2026-07-13: **App-update delivery parked; store-track endgame chosen.** Rolling-artifact installs (download + reinstall from the `latest` prerelease) stay the dev workflow — post-auth work is data layer + UI, mostly verifiable on the emulator or a locally built `.dmg`. When update delivery matters: Android goes straight to the **Play internal-testing track** (no interim sideload polish); desktop adopts **tauri-plugin-updater**, bundled with the deferred Developer ID + notarization work so the signing pipeline is touched once. Both queued in Later/parked with the details. The versioning prerequisite for every path already ships in artifacts.yml.
- 2026-07-13: **Android Google OAuth returns via deep link, not loopback.** The on-device test showed Chrome timing out on the embedded server's callback: Android freezes the backgrounded app, so the loopback-callback pattern (fine on desktop, per RFC 8252) can't work there. Android now sends the public web origin's `/auth/app-return` as the OAuth callback; that page bounces the verifier into the app via the `three-rings://` scheme (`tauri-plugin-deep-link`; intent-filter build-time-injected from `tauri.conf.json`), and the app completes the challenge/verifier exchange in-process once foregrounded. Upstream rejects custom-scheme callback URLs outright (`INVALID_CALLBACKURL`, probed live), which forced the web-origin bounce. Desktop flow unchanged. Details in specs/auth.md Findings.
- 2026-07-13: **Auth shipped — path-B cookie proxy over Neon Auth (Better Auth), Google included; verification on.** The browser/webview only ever holds our origin's httpOnly cookies (`tr_session` upstream session, `tr_jwt` 15-min EdDSA JWT, `tr_challenge` OAuth window); the server proxies the auth REST API and re-mints JWTs silently. Google stays server-side via the verifier/challenge exchange (mechanism excavated from Neon's own SDK — the naive browser-side `/token` bridge dies on partitioned third-party cookies). Toggles (maintainer): `require_email_verification=true` (OTP method — code-in-app works identically in webviews) + Google (shared creds), both branches; that single toggle both gates sign-up sessions and auto-mails the OTP. Two latent bugs found: the step-5 verifier rejected every real token (jsonwebtoken v9 needs the expected `aud` configured; `iss`/`aud` = the auth origin), and a unit `Suspense` fallback (`|| ()`) desyncs leptos hydration app-wide (never ship `fallback=|| ()`). Tauri desktop spike PASS — the full gated flow works inside WKWebView against the embedded server; Google-in-webview is impossible (Google policy + upstream callback rules), so desktop Google = system browser + loopback callback, queued as a Phase 3 task. `SET LOCAL app.user_id` still rides the data-model per-request transaction.
- 2026-07-12: **Dockerfile = cargo-chef dep caching; Render build cache verified persistent.** The delivery Dockerfile now cooks dependencies in layers keyed only on the manifests/lockfile (two cooks matching cargo-leptos 0.3.7's real invocations: native `server-release`, wasm `release` into `target/front`), so code-only pushes reuse them — local Docker measured cold 137s → warm 9s. Frontend tools (tailwindcss v4.2.1, wasm-opt/binaryen version_123, wasm-bindgen 0.2.103) are pinned onto PATH in the image because cargo-leptos prefers PATH tools over downloading — removes per-build GitHub downloads *and* the unpinned "latest" API checks (rate-limit risk on shared builder IPs); the wasm-bindgen ARG must track the Cargo.toml `=0.2.103` pin. mold added for native links (scoped via `$CARGO_HOME/config.toml`; identical flags for cook + build keep fingerprints stable). The spec's Render-cache caveat is resolved: deploy build logs show a registry cache-manifest import with `CACHED` build-stage layers on an ephemeral builder → persistence confirmed, sccache+S3 stays the fallback if cook layers run cold (follow-up task queued). Also discovered en route: PR #5's squash dropped `e5df220`, leaving main's Dockerfile copying the now-empty `target/release/` path — main deploys were `build_failed`; the standalone fix landed mid-task as PR #6, and this refactor preserves it in the new stage layout.
- 2026-07-12: **Migrations run as a deploy step, not from the app; web server runs as a non-owner role.** Neon (unlike Supabase) has no managed migration runner, so the spike wired `MIGRATOR.run` into server startup — removed. Migrations now run as a **Render pre-deploy command** under `neondb_owner` (owner URL in Render env + local dev only, never GitHub — consistent with "no DATABASE_URL in GitHub"); the running web server connects as the non-owner **`app_runtime`** role (created 2026-07-12 on both branches: CRUD + RLS-subject, no DDL/superuser/bypassrls). One runtime credential, no in-app role juggling. Rotation of Render's `DATABASE_URL` → `app_runtime` happens **after** migrations leave the app-startup path. Only affects the process that talks to Postgres directly (hosted web server + local dev); native clients never hold DB creds (data-access-backends). Work folded into the data-model migrations + data-access tasks.
- 2026-07-11: **Correction — Neon Auth is Better Auth, already provisioned, uuid users.** Live-DB inspection via the Neon MCP (project `steep-scene-29832344`) supersedes the Stack-Auth specifics in the entry below. Neon Auth runs **Better Auth**, **already provisioned on both `production` and `dev` branches**, storing a live `neon_auth."user"` table with a **uuid** id (not a text `users_sync` mirror). Data-model FKs corrected to `uuid … REFERENCES neon_auth."user"(id) ON DELETE CASCADE`; the async-sync/soft-delete caveat was a Stack Auth artifact and is dropped. Config is non-secret per-branch base_url/jwks (dev URL in `.env.example`, prod in Render); **no app secret key**. Specs corrected; the auth task stays `[~]` with the non-owner RLS role + Axum JWKS middleware + Tauri spike outstanding.
- 2026-07-11: **Auth = Neon Auth; a Neon Auth task added to Phase 3 ahead of the data-model migrations** (data-model spec review, maintainer's option 1). Neon Auth (Stack Auth-backed) manages users in `neon_auth.users_sync` (`id text` PK; `email`/`name`/`created_at`/`updated_at`/`deleted_at` soft-delete; `raw_json`), so data-model *references* that table with **text** `user_id` FKs instead of defining its own `users`, and its collection tables can't migrate until Neon Auth is enabled. Resolves auth's roll-our-own-vs-hosted question. RLS stays app-enforced via the `app.user_id` GUC set by the hosted Axum API (the authorization terminus per data-access-backends) — deliberately **not** Neon's Data API / JWT-RLS path. Caveats carried into the auth task: async user sync + soft delete → hard-FK-vs-soft-reference and first-login Inbox provisioning are open.
- 2026-07: API-first on Neon chosen over offline-first Turso designs. Rationale in README.
- 2026-07: Architecture spike prioritized ahead of data model — architecture unproven.
- 2026-07: Spec numbering dropped; filenames are the stable IDs, this file owns execution order.
- 2026-07: Tasks gated on spec status via `(specs: ...)` annotations; only humans accept specs.
- 2026-07: Spike decisions: web = local run only; mobile = Android; mobile SSR failure = stop and reassess; no time-box; Android gate moved ahead of DB work (fail fast).
- 2026-07: Scaffold base = tauri-leptos-ssr (embedded in-process Axum matches the README); start-tauri-fullstack rejected (thin shell → external server, csr default). Rationale in architecture-spike.md Findings.
- 2026-07: Dev environment = Docker devcontainer. Image `dgoings/three-rings` (`.devcontainer/Dockerfile`, layered on `dgoings/magic-assistant-dev`) carries the Rust/Leptos/Tauri toolchain so the host stays toolchain-free. All Rust dev + the web target build/run in the container; the Android *build* is containerized; macOS desktop + iOS and the Android *run* (emulator/device) are host-side.
- 2026-07: Consequence of the devcontainer split — macOS desktop (Phase 1) is deferred behind the Android gate, built later via a CI macOS runner or a minimal host install (keeps the host toolchain-free until the architecture is proven). Android SDK/NDK are added to the image as a second layer just before the gate.
- 2026-07-07: **Android build moved host-side** (reverses the containerized-Android-build part of the devcontainer decision). Google ships no linux-arm64 NDK — official Linux tooling is x86_64-only — so the planned Android layer on the arm64 image cannot work; an amd64-under-Rosetta image variant was viable but was passed over for the host toolchain (Android Studio SDK/NDK/JBR + brew rustup + binstalled cargo-leptos/tauri-cli). Web dev stays in the container; the "toolchain-free host" goal is relaxed to "Rust via brew rustup, no ad-hoc curl installs".
- 2026-07-07: **Android architecture gate passed** on the Samsung Flip 7 emulator — release APK, embedded in-process Axum serving SSR + hydration + server fns. Android-specific fixes (cleartext/signing in gradle, APK-asset extraction, on_page_load navigation) recorded in architecture-spike.md Findings; `src-tauri/gen/android` is now committed since it carries the gradle config.
- 2026-07-07: **macOS desktop built host-side** (resolves the parked CI-vs-host question) — the toolchain was already on the host from the Android gate; Xcode supplies the platform bits.
- 2026-07-07: **Phase 1 complete — architecture spike passed on all targets**; architecture-spike.md marked implemented, carry-forward items in its Conclusion. Neon `DATABASE_URL` lives in `.devcontainer/.env` (host shell exports it for native desktop runs).
- 2026-07-09: **Web deploy platform = Render** (maintainer decision at spec review, supersedes the Railway draft choice below; Fly.io fallback).
- 2026-07-09: **Repo is public** (was: private in the draft) — discovered at setup: the free-plan org can't enforce branch protection or auto-merge on a private repo, and the merge gate depends on both; public unlocks them at zero cost. Side effects: the rolling release needs no GitHub login from the phone, and GitHub-hosted minutes would be free too (Blacksmith remains the runner choice for speed and its own free tier). Repo lives at `threeringsdev/three-rings`.
- 2026-07-09: **Artifact cadence split by cost, not target** — Android APK per `main` merge (plus dispatch; docs-only pushes skipped via `paths-ignore`) keeps the phone-checkable rolling release fresh; macOS `.dmg` is `workflow_dispatch`-only from the start ($0.08/min would exhaust the Blacksmith free tier in ~15 merges; its consumer is at a Mac anyway, and pre-merge desktop checks happen as local host builds). Per-target path filtering rejected: the shared `app` crate means most code changes alter every artifact.
- 2026-07-09: **CI runners = Blacksmith** (maintainer decision, supersedes the GitHub-hosted draft; drop-in `runs-on` labels, any job reverts to GitHub-hosted independently). Consequence: the repo must be owned by a free GitHub organization — Blacksmith doesn't install on personal accounts. Budget framing changes from GitHub's 2,000 min / 10×-macOS multiplier to Blacksmith's 3,000 free min/month (2 vCPU x64 basis; macOS M4 $0.08/min), plus GitHub's $0.002/min control-plane fee on all Actions minutes (in effect since 2026-03).
- 2026-07-07: **Phases reorganized — delivery pipeline before data model/UI** (new Phase 2, specs/delivery-pipeline.md; foundations becomes Phase 3). Goal: agents work autonomously from any host with the repo as the contract; results checkable remotely. Decisions: GitHub private repo; two-tier CI (validate per-push linux-only; artifacts on `main` merges + manual — private-repo minutes, macOS 10×); one keystore-as-secret for in-place APK updates; rolling `latest` prerelease; web deploy = Railway building a repo Dockerfile (Render fallback; zero Actions minutes); no `DATABASE_URL` in CI. Android-in-CI uses NDK r28+ (x86_64 runners sidestep the linux-arm64 NDK gap and close the 16 KB finding).
- 2026-07-11: **Phase 2 non-deploy work landed** — validate + artifacts CI, rolling `latest` release, self-sufficiency docs (root `CLAUDE.md`, `.devcontainer/README.md`, `scripts/gen-android-keystore.sh`), Neon `dev` split (PR #1 + follow-up fixes on `main`). First real CI surfaced: (1) `useblacksmith/rust-cache` runs `cache-bin: true`, which cleans `binstall`'ed binaries out of the cached `~/.cargo/bin` on save but keeps `.crates2.json` → on a cache-hit run `binstall` skips `cargo-leptos` ("already installed") and `cargo leptos` is missing at build time → fixed with `binstall --force` in both workflows. (2) Node 20 action deprecation → bumped `actions/checkout`→v7, `actions/setup-java`→v5, `android-actions/setup-android`→v4; `useblacksmith/rust-cache` stays v3 (Blacksmith fork has no node24 build yet — the lone remaining warning). (3) Release signing/versioning needed a backward-compatible edit to `src-tauri/gen/android/app/build.gradle.kts` (release `signingConfig` from `keystore.properties` + version env, falling back to debug/`tauri.properties` for local builds). (4) Neon's default branch is `production` (not `main`); `dev` was branched off it. Deferred (out of this phase's scope): web deploy (Dockerfile + Render) and the loop's web/`.dmg` legs.
- 2026-07-11: **Web deploy live on Render.** Root `Dockerfile` (multi-stage: `rust:1-bookworm` runs `cargo leptos build --release` → `debian:bookworm-slim` carries the `server` binary + `target/site` + `ca-certificates`; 114 MB) merged via PR #2 and auto-merged on green — proving the full agent loop (branch → PR → validate gate → auto-merge). TLS backend is `ring` (no openssl/cmake build deps); `cargo-leptos` pinned to 0.3.7 for host/CI parity. Runtime config is entirely env-driven (`get_configuration(None)` reads `LEPTOS_SITE_ROOT=/app/site` etc. from the image's `ENV`; entrypoint maps Render's `$PORT` → `LEPTOS_SITE_ADDR=0.0.0.0:$PORT`). Verified locally (Docker vs Neon **dev** branch) and live at https://three-rings-6p5o.onrender.com (Docker runtime, auto-deploy on `main`, free tier w/ spin-down, Ohio region): `/` SSRs, `/cards` renders the 3 seeded **production**-branch rows. The service's only manual env var is `DATABASE_URL` (Neon production, direct host). Aside: the Render `create_web_service` MCP tool can't create Docker services, so the service was created in the dashboard (briefly in the wrong workspace, then recreated under the personal team). Remaining Phase 2: the end-to-end loop proof (web leg now done; `.dmg` dispatch still pending).
- 2026-07-11: **macOS `.dmg` signing = staged.** The downloaded `.dmg` read as "damaged" because the bundle was only linker-signed (invalid seal). Now: `bundle.macOS.signingIdentity = "-"` for a *valid* ad-hoc signature → opens on any Mac via a one-time right-click → Open (not notarized). Later: Developer ID Application + App Store Connect API-key notarization for a zero-prompt download — **blocked**, only the org's Apple **Account Holder** can create a Developer ID cert (maintainer's roles cover Apple Development / Distribution / Mac App / Installer — none notarize a downloadable `.dmg`). Mac Development + registered devices rejected (doesn't remove the prompt; restricts to registered UDIDs). Full drop-in recipe recorded in the spec's "macOS `.dmg` signing" section.
