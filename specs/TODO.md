# Project TODO — execution queue

**This file is the single source of truth for what to work on next.** The selection algorithm, spec gating, and definition of done are in [README.md](README.md) ("Working the queue"). State legend: `[ ]` available · `[~]` in progress · `[x]` done.

Phases execute top to bottom; tasks within a phase top to bottom. A task's `(specs: ...)` lists every spec it is gated on — all must be `accepted` (status is read from the spec files, not recorded here). Tasks without a specs annotation are ungated.

## Phase 0 — dev environment (devcontainer)

Keeps the Rust toolchain off the host; see the Decisions log and `.devcontainer/README.md`.

- [x] Author `.devcontainer/` and build the `dgoings/three-rings` dev image (v1: Rust/Leptos/Tauri CLIs — cargo-leptos, cargo-generate, cargo-tauri, leptosfmt); wire devcontainer (image, ports 3000/3001, env-file, zshrc mount)

## Phase 1 — architecture spike

Ordered riskiest-first; see the spec's Failure policy — if the Android gate fails, STOP the phase. Executed inside the devcontainer (Phase 0); macOS desktop is host/CI-side — see Decisions log.

- [x] Compare scaffold bases (start-tauri-fullstack vs. tauri-leptos-ssr); record choice + rationale in architecture-spike.md (specs: [architecture-spike](architecture-spike.md), [ui-components](ui-components.md))
- [x] Scaffold Cargo workspace from the chosen base; commit unmodified (specs: [architecture-spike](architecture-spike.md))
- [x] Verify web target: `server` binary locally, SSR + hydration (specs: [architecture-spike](architecture-spike.md)) — done early via devcontainer smoke (see Findings)
- [x] Build + run: Android (emulator OK) — the architecture gate; static page sufficient (specs: [architecture-spike](architecture-spike.md)) — **PASS** on the Flip 7 emulator, incl. hydration + server fns (see Findings)
- [x] Set up Neon project (free tier): one trivial table, seed rows, sqlx connectivity from the server path (specs: [architecture-spike](architecture-spike.md)) — verified in-container: `cards` migrated + seeded, probe read 3 rows (see Findings)
- [x] One server function + one page rendering DB rows, using at least one Rust/UI component (specs: [architecture-spike](architecture-spike.md), [ui-components](ui-components.md)) — `/cards` SSRs Neon rows in the vendored Rust/UI table (see Findings)
- [x] Build + run: macOS desktop target (embedded Axum) (specs: [architecture-spike](architecture-spike.md)) — **PASS**, host-built; dynamic port, SSR + `/cards` from Neon (see Findings)
- [x] Write up findings in architecture-spike.md; mark spec implemented (specs: [architecture-spike](architecture-spike.md)) — spec status → implemented; Phase 1 complete

## Phase 1b — UI design — parallel with Phase 1, human-led

- [x] Information architecture / nav structure (specs: [ui-design](ui-design.md)) — two modes (Catalog / My cards); see design/information-architecture.md + spec Findings
- [x] Wireframe core screens (catalog search, collection, add-flow, auth, shell) (specs: [ui-design](ui-design.md)) — design/wireframes.pen: 9 screens + overlay, desktop + mobile; see spec Findings
- [x] Prototype the add-to-collection flow (specs: [ui-design](ui-design.md)) — storyboards in design/wireframes.pen + input-cost accounting; see spec Findings
- [x] Component gap analysis vs. Rust/UI registry (specs: [ui-design](ui-design.md), [ui-components](ui-components.md)) — design/component-gap-analysis.md: 27 primitives vs rust-ui@43e1e32, 3 gaps (tree, count stepper, selection tray) + SSR code review of 6 components; see spec Findings
- [x] Design the global ⌘K command palette (actions, scope, wireframe) (specs: [ui-design](ui-design.md)) — design/command-palette.md + ⌘K region in wireframes.pen (P1/P2); places + 3-command registry, desktop-only; see spec Findings
- [x] Wireframe hygiene: delete disabled ghost nodes flagged by layout scans (mobile `/`-hint frames and similar), so problem scans stay a usable signal (specs: [ui-design](ui-design.md)) — 4 ghost `/`-hint frames deleted; scans clean bar the intentional filter-sheet clip; see spec Findings

## Phase 2 — delivery pipeline

Code leaves the laptop: CI validation, remote-checkable artifacts, agent self-sufficiency. Ordered by dependency; budget policy and design details in the spec.

- [x] GitHub: create a free org + public repo under it (Blacksmith requires org ownership; public because free-plan orgs can't gate private repos), push `main`, enable auto-merge, branch protection requiring the validate check (blocked on the validate workflow existing), install the Blacksmith GitHub app, add secrets (`ANDROID_KEYSTORE` + passwords; `RENDER_DEPLOY_HOOK` only if needed) — org/public repo/`main`/Blacksmith/`ANDROID_*` secrets done; branch protection (required check is the **job** name `Format, Clippy, Test & Build`, not the workflow name) + auto-merge enabled and **proven** — PR #1 and PR #2 both auto-merged on green (`RENDER_DEPLOY_HOOK` not needed; Render's GitHub integration auto-deploys) (specs: [delivery-pipeline](delivery-pipeline.md))
- [x] Replace template `ci.yml` with the validate workflow: fmt, clippy (ssr + wasm, incl. `src-tauri` via Tauri linux deps), test, `cargo leptos build --release`; rust caching — `.github/workflows/validate.yml` on `blacksmith-4vcpu-ubuntu-2404`, every push + PR, green on `main`; a rust-cache `cache-bin` bug (dropped `cargo-leptos`) and the Node 20 action deprecation were found + fixed on first CI (see the 2026-07-11 Decisions log) (specs: [delivery-pipeline](delivery-pipeline.md))
- [x] Android APK artifact job (`main` + `workflow_dispatch`, `paths-ignore` for docs-only pushes): NDK r28+ (closes the 16 KB finding), aarch64 APK, signed with the keystore secret — builds on merge to `main`; signed APK published to the rolling `latest` release, installed + ran on device; required a backward-compatible `src-tauri/gen/android/app/build.gradle.kts` signing/version edit (see the 2026-07-11 Decisions log) (specs: [delivery-pipeline](delivery-pipeline.md))
- [x] macOS `.dmg` artifact job (`workflow_dispatch` only — Blacksmith macOS at $0.08/min) — authored + gated; **valid ad-hoc signing** added (`signingIdentity: "-"` → opens on any Mac via one-time right-click → Open, no longer "damaged"); Developer ID + notarization deferred (blocked on org Account-Holder cert access — recipe in spec); dispatch to verify (specs: [delivery-pipeline](delivery-pipeline.md))
- [x] Rolling `latest` prerelease: artifact jobs publish APK + `.dmg` to stable URLs — live with the APK asset + per-asset SHA/timestamp provenance; `.dmg` half pending its first dispatch (specs: [delivery-pipeline](delivery-pipeline.md))
- [x] Web deploy: multi-stage `Dockerfile` (server binary + site, `PORT`→`LEPTOS_SITE_ADDR` entrypoint) + Render service with GitHub integration; Neon branch split (project main → Render `DATABASE_URL`, new `dev` branch → `.devcontainer/.env`); verify `/` + `/cards` at the public URL — **live** at https://three-rings-6p5o.onrender.com: root `Dockerfile` (rust:1-bookworm → debian:bookworm-slim, 114 MB) merged via PR #2 (auto-merged on green); Render `three-rings` service (Docker runtime, auto-deploy on `main`, free/spin-down, Ohio) SSRs `/` + `/cards` from the Neon **production** branch — `DATABASE_URL` is its only manual env var (all `LEPTOS_*` baked into the image); Neon `dev` split done earlier (specs: [delivery-pipeline](delivery-pipeline.md))
- [x] Agent self-sufficiency: root `CLAUDE.md` (commands, what-runs-where, queue conventions), in-container git/`gh` auth docs, update `.devcontainer/README.md` table (CI owns Android/macOS builds) — authored + merged, plus `scripts/gen-android-keystore.sh` (specs: [delivery-pipeline](delivery-pipeline.md))
- [x] Prove the loop end-to-end: fresh container → agent does a trivial task → PR → auto-merge on green (zero human touch) → verify web URL + APK update away from the laptop, dispatch and check the `.dmg`; mark spec implemented (specs: [delivery-pipeline](delivery-pipeline.md))

## Phase 3 — foundations

- [x] Auth (Neon Auth = Better Auth). **Done:** `app_runtime` non-owner role + `trusted_origins`; migrations moved out of the app (PR #3); JWKS EdDSA verify middleware + `AuthUser` extractor + `/api/me` probe (PR #4); **path-B cookie-session proxy** (`tr_session`/`tr_jwt`/`tr_challenge` httpOnly cookies, server fns at `/api/*`, silent JWT refresh), Leptos `/login`+`/signup`+OTP screens, **Google OAuth kept fully path B** via the verifier/challenge exchange (`/auth/callback`), toggles decided (email verification **on** — `require_email_verification`, OTP method; Google **on**, shared creds), and the **Tauri desktop webview spike PASS** (full gated sign-up→OTP→auto-sign-in inside WKWebView). Fixed en route: step-5 verifier 401'd all real tokens (jsonwebtoken v9 `aud`), and a unit `Suspense` fallback broke hydration app-wide (see auth.md Findings). All flows verified live on the dev branch (headless UI drives in `end2end/*.mjs`). Still riding other tasks: `SET LOCAL app.user_id` (data-model per-request tx) (specs: [auth](auth.md))
- [x] Build perf 1/2 — split the release profile so the SSR `server` binary skips the wasm-only `lto`/`codegen-units=1`/`opt-level=z` (`bin-profile-release` → `server-release`, = Rust stock release defaults); measured cold `-p server` on 18 cores **44.3s → 31.5s (~29%)**, OLD used only ~4× parallelism vs NEW ~10×; wasm bundle keeps size-opt. Render builder core count unverified — confirm on first deploy (specs: [delivery-pipeline](delivery-pipeline.md))
- [x] Build perf 2/2 — Dockerfile cargo-chef dependency-caching refactor (+ optional mold linker, tailwindcss prefetch); verify Render deploy time before/after — done: chef/planner/builder stages with two cooks mirroring cargo-leptos's exact invocations (native `server-release`; wasm `release` @ `target/front`); mold scoped to native triples; tailwindcss/wasm-opt/wasm-bindgen pinned on PATH (cargo-leptos prefers PATH tools → zero per-build downloads/API checks). Local Docker: cold **137s → 9s** on a source-only edit (cook layers CACHED, only workspace crates recompile). Render build-cache persistence **verified** from deploy build logs (cache-manifest import + CACHED build-stage layers); also surfaced that PR #5's squash had missed the `e5df220` Dockerfile fix — main deploys were `build_failed` until PR #6 landed it mid-task (specs: [delivery-pipeline](delivery-pipeline.md))
- [x] Build perf follow-up — confirm cook-layer reuse on Render — **confirmed: Render's registry cache import works across deploys on ephemeral builders**, twice over: the d7b3dc0 failed-deploy log showed `CACHED` build-stage layers, and the ba90076 (auth) deploy showed all chef toolchain layers `CACHED` while the two cooks *correctly* re-ran (that PR changed Cargo.toml/lock → new recipe; 7m00s vs 6m13s fully-cold). The pure warm case (no manifest change) is this task's own docs-only deploy — cook layers expected `CACHED` with only workspace crates rebuilding; verified post-merge in the PR record. sccache+S3 fallback not needed on current evidence — see delivery-pipeline "Build performance (deploy time)" (specs: [delivery-pipeline](delivery-pipeline.md))
- [x] Desktop Google OAuth via the system browser — **done, maintainer-confirmed on desktop**: opener plugin via a custom `open_url` command, `callbackURL = http://localhost:<embedded port>/auth/callback`, challenge/session parked in the single-user embedded server's memory (`app/src/auth/native.rs`), webview polls `current_user` (2 s, ≤3 min) instead of focus-refetch. Shipped with the production-shakedown fixes: `newUserCallbackURL` (first-time Google users skipped our exchange entirely), Google button on `/signup`, back-to-home links, `.env` autoload (dotenvy). Hard-won Tauri v2 findings (silent shell-open rejection; app commands are ACL-gated; loopback pages are *remote* origins needing `remote.urls` in the capability) in auth.md. **Android leg misbehaved and got its own fix**: a frozen backgrounded app can't serve the loopback callback, so Android returns via the `/auth/app-return` bounce + `three-rings://` deep link (PR #12) — maintainer-verified on device, first-time-Google path included (specs: [auth](auth.md))
- [x] Password reset flow — **done**: probed live first (the email-otp plugin's dedicated `request-password-reset` / `reset-password` routes exist on the hosted service; OTP shape chosen to match the verification UX), then `request_password_reset`/`reset_password` server fns with auto-sign-in after reset (upstream issues no session; it marks the email verified) and a ResetCard behind "Forgot password?" on `/login`. Verified live on dev — full API cycle (old password rejected, OTP replay rejected) + headless UI drive, no hydration regressions. Pure server-fn + page work, so it rides every surface (specs: [auth](auth.md))
- [x] Add email+password to a Google-first account — **done, maintainer-confirmed live on production (2026-07-13)**: "Forgot password?" with the google-only account set a password and landed signed in; password and Google sign-in now coexist on the account. Mechanism shipped with the reset flow (upstream's reset *creates* the `credential` account when none exists — better-auth source, quoted in auth.md Findings), and the sign-up "User already exists" error points at it (specs: [auth](auth.md))

## Phase 4 — data & API layer

Build the schema, the data-access layer, the API, and the catalog. Spec-review
(fleshing drafts → `accepted`) is the README's blocked-queue meta-action, not a
task here; these tasks are the implementation, each gated on its `accepted` spec
and carrying the `mark <spec> implemented` step on that spec's terminal task
(see the 2026-07-15 Decisions entry). Every Phase 4 spec is now `accepted` or
`implemented`. (ui-component-bench led the phase as independent UI infra and
is done — spec `implemented`.)

- [x] ui-component-bench → the bench page (independent UI infra — gated only on its own spec, runnable in parallel with the data chain below). Feature-gated (`component-bench`) `/dev/components` route in `app`: section registry + jump-nav, the `table` section, a static theme-token panel + bench-local light/dark toggle, and the "add a component's bench section in the same commit" adoption convention; reachable in the native webviews (WKWebView / Android WebView) for the anchor-positioning check. mark ui-component-bench implemented once the harness + convention are in place and the current vendored set (`table`) is covered (specs: [ui-component-bench](ui-component-bench.md)) — **done**: `app/src/bench/` registry + demos, one `--features component-bench` flag wired through `frontend`/`server`/`src-tauri` + `beforeDevCommand`, bench clippy lines in validate.yml, SSR/hydration/toggle verified via [end2end/bench-check.mjs](../end2end/bench-check.mjs) and feature-off exclusion confirmed; spec → implemented (route-macro workaround + decisions in its Findings)
- [x] data-model → migrations. Write + run the initial migrations (catalog group; collection group + RLS enable/force/policies + the `app_runtime` role) via [`scripts/migrate.sh`](../scripts/migrate.sh); collection tables FK `neon_auth."user"` (uuid, hard `ON DELETE CASCADE`); resolves data-model's execution-deferred OQs (denormalized `user_id`, condition/language granularity); mark data-model implemented (specs: [data-model](data-model.md), [auth](auth.md)) — **done**: `0002_catalog`/`0003_collections`/`0004_catalog_app_readonly` applied to the Neon **dev** branch; RLS verified end-to-end (per-user isolation, forced); a pre-existing Neon default-ACL over-granted catalog CRUD to `app_runtime` → `0004` revokes it on existing catalog tables and `0005` narrows the default privilege so all future tables are app_runtime-read-only by default (no per-table revoke needed again); `migrate.sh` hardened against the `sqlx::migrate!` new-file staleness bug. **Prod migration deferred** to the data-access-backends deploy (dropping spike `cards` is breaking vs. the live spike code — expand-first); spec → implemented (see Findings)
- [x] data-access-backends → the trait layer. Per-domain `CatalogStore`/`CollectionStore` traits, `HostedBackend` (sqlx) + `NativeBackend` (HTTPS) structs, the `shared/` crate (DTOs + the cross-backend error enum), the `ssr`/`hosted`/`native` feature split, the per-request `SET LOCAL app.user_id` tx; remove spike-era direct DB access (`app/src/db.rs`); **migrate the Neon production branch (`scripts/migrate.sh prod`) as part of this deploy** — the data-model migrations were applied to dev only, deferred on prod because dropping the spike `cards` breaks the live spike `/cards` (expand-first, see data-model Findings); mark data-access-backends implemented — prereq: the migrations (specs: [data-access-backends](data-access-backends.md), [data-model](data-model.md)) — **done**: new `shared/` crate (DTOs + `ApiError`); `ssr` decomposed → `hosted` (sqlx, the `server` build) / `native` (HTTPS client, the `src-tauri` build, **no sqlx linked**); per-domain traits, one backend struct per target carrying the session credential; hosted `scoped_tx` runs `set_config('app.user_id',$1,true)` per request; UI calls in-process server fns, native calls explicit hosted JSON routes (`/api/catalog/count`, `/api/collections`); spike `db.rs` probe + `get_cards`/`Card` removed, `/cards` now shows the catalog count via the seam. Gate reproduced in-container + proven live on dev (catalog read `{"cards":0}`, 401 on the session route, scoped_tx OK). Added a dedicated `-p app --features native` clippy gate line (workspace unification masks the native arms) and moved the bench line to `hosted`. **Prod migrated** (`0002`–`0006` applied to the Neon production branch 2026-07-16, verified identical to dev); the native `401` refresh rides the follow-up below. Spec → implemented (see Findings)
- [x] data-access-backends → native 401 refresh + offline behavior. Implement the native client's silent JWT re-mint from `tr_session` on a `401` (stubbed `TODO` in `app/src/backend/native.rs`) + a defined offline-unreachable behavior (data-access-backends OQ#3) — needs the session cookie-jar plumbing that lands with collection-api's native client, so it rides that work (specs: [data-access-backends](data-access-backends.md)) — **done**: `NativeBackend::send` re-mints a fresh JWT from `tr_session` (auth's `mint_jwt`) on a hosted `401` and retries once (hard one-retry bound); `authed` now carries `(tr_jwt?, tr_session?, origin)` and `require_session` accepts a refresh-only session; **unreachable → `Upstream`, auth-error → `Unauthorized`** is type-distinct (OQ#3 closed); cookie re-host stays the `current_user` poll's job (backend stays pure transport). Two `native.rs` unit tests lock the session classification; native + workspace gate green (the network path is device-exercised — needs a live deployment). (The sibling prod-migration deploy step was **done** 2026-07-16: `0002`–`0006` on the Neon **production** branch, verified identical to dev.)
- [x] collection-api → endpoints. Both domains: collection (tree CRUD + reparent cycle check + lazy inbox, holdings/desires + batch add, moves incl. undo/pull/teardown, keyset reads with per-page aggregates) and catalog (`card_detail`, `card_summary`, the search endpoint shell); mark collection-api implemented — prereq: the trait layer (specs: [collection-api](collection-api.md), [data-access-backends](data-access-backends.md), [data-model](data-model.md)) — **done**: built as six verified slices (tree/inbox, holdings-desires, CollectionView, moves, global reads, catalog), every op a `CatalogStore`/`CollectionStore` trait method with a `shared/` DTO, implemented by `HostedBackend` (sqlx) + `NativeBackend` (HTTP client) and mirrored by explicit hosted JSON routes. Keyset pagination on the large reads; per-page aggregates; opportunistic auth on catalog reads; board-agnostic moves (mainboard) with undo/teardown; ownership guard beneath RLS. Each slice proven end-to-end against the Neon **dev** branch (temp drivers, reverted); full merge gate green in-container. sqlx gained `uuid`+`json`. **Pull/pull-all** are client-composed from `move_cards` + `suggested_destinations`; **needs/shopping** return full lists (keyset follow-up below); **no per-op Leptos server-fns** (thin per-screen UI adapter, rides with the UI tasks); **tags on CardRow** stay card-tagging's. Spec → implemented (see Findings)

- [x] card-tagging → schema migration. Migration `0006`: `card_board` enum + `board` on `holdings`/`desires` (folded into their uniqueness keys), `tags` + `card_tags` (RLS enable/force/policies incl. world-readable system tags; seed the `commander`/`companion` system tags before FORCE; `app_runtime` write grants per `0005`'s convention), drop the empty `deck_commanders`; via [`scripts/migrate.sh`](../scripts/migrate.sh) (dev now, prod with the data-access deploy) — prereq: the data-model migrations (specs: [card-tagging](card-tagging.md), [data-model](data-model.md)) — **done**: `0006` applied to dev; boards folded into both uniqueness keys, system tags seeded before FORCE, dual `tags` policy (system-readable / owner-writable) + `card_tags` owner policy verified end-to-end; `app_runtime` SELECT came free from the `0005` default-privilege flip; `deck_commanders` dropped (see card-tagging Findings)
- [x] card-tagging → tag/board API. The tag CRUD + assignment + board + commander/companion endpoints (collection-api §Tags & boards): scope containment, in-deck membership + orphan cleanup, ≤2/≤1 caps, color-identity recompute; mark card-tagging implemented — prereq: the schema migration + the trait layer (specs: [card-tagging](card-tagging.md), [collection-api](collection-api.md), [data-access-backends](data-access-backends.md)) — **done**: 11 `CollectionStore` methods (tag CRUD + scope, assign/unassign with containment + in-deck + ≤2/≤1 caps + idempotency, `card_tags`/`cards_with_tag`, `deck_commanders` with read-derived WUBRG color identity, holding/desire board re-label with split/merge + binder guard), each a `shared/tags.rs` DTO implemented by `HostedBackend` + mirrored by `NativeBackend` over new hosted routes. Proven end-to-end vs. Neon **dev** (temp driver, reverted); full merge gate green. card-tagging → implemented (see its tag/board Findings)

- [x] catalog-ingestion → POC subset. Ingest a representative subset (multi-face, tokens/checklist, varied sets/colors/types/rarities) so catalog search runs end-to-end against real rows — prereq: the migrations (specs: [catalog-ingestion](catalog-ingestion.md), [data-model](data-model.md)) — **done**: the whole bulk path shipped (`server --ingest <poc|bulk>` + `scripts/ingest.sh`, migration `0007` on dev, `catalog_ingest` role on both branches) and the POC ran against the Neon **dev** branch: 1,045 sets, 1,333 filter-matched + 1,643 closure-pulled → **2,976 printings / 2,665 cards / 9,654 rulings**; every opted-in layout present (checklist = layout `token`; battles now `transform`), zero unresolved `all_parts` links, re-run writes 0 catalog rows (hash gate + first-seen-per-run oracle winner). Live surprises recorded in the spec Findings: /bulk-data still serves the legacy JSON-array layout (identity-encoded without gzip negotiation — pipeline sniffs both), and per-printing oracle variance forced the deterministic first-seen rule. Stage-2 `bulk` mode is written and gate-tested but not yet run at full scale
- [x] catalog-search → query engine. Scryfall query-subset parser → SQL against the catalog, query↔rail sync, URL-state serialization, wired into collection-api's search endpoint; mark catalog-search implemented — prereq: the search endpoint shell + POC data (specs: [catalog-search](catalog-search.md), [collection-api](collection-api.md)) — **done**: `app/src/search/` (pure `parse.rs` grammar + `sql.rs` QueryBuilder emission, binds only) replaces the shell WHERE in `HostedBackend::search`; parse errors → 422 naming the term. Migration `0008` on dev (`oracle_search_text` generated column closing the multi-face `o:` gap + trgm GINs). 25 unit tests (TDD) + an `#[ignore]`d live test against the dev POC catalog — which caught a real NULL-poisoning bug in negated color terms (fixed with a locked-in coalesce). Rail sync + URL state ship as contract (query string = canonical state); the rail widgets ride the UI-phase catalog screen. catalog-search → implemented (see Findings)


## Phase 5 — app UI

Replace the scaffold counter with the real app: the nine wireframed screens, the
shell, and the three custom gap components, built page by page so every task
ships independently through the merge gate. Design authority is the Phase 1b
artifacts (wireframes.pen, information-architecture, add-flow-prototype,
command-palette, component-gap-analysis) distilled into
[app-ui](app-ui.md); the per-task agent loop (Claude builds → Codex adversarial
review → platform verification → e2e authored + run → Codex e2e pass → gate) is
[ui-work-loop](ui-work-loop.md). Stage boundaries (marked below) trigger the
full e2e tier + Android release smoke if the tiered matrix is chosen by the
spike. UI work stays on the ~3K-printing POC catalog (maintainer decision
2026-07-17); quick-add realism / perf findings are recorded as deferred.

### Stage 0 — loop infrastructure

- [x] Android e2e spike: prove/disprove automated e2e against the Android emulator (Tauri debug webview remote debugging → `adb forward` + Playwright `connectOverCDP`, or Playwright's experimental `_android` API); record the outcome in ui-work-loop Findings and fix the loop's platform matrix per its decision tree (specs: [ui-work-loop](ui-work-loop.md)) — **PASS, decision-tree path 1**: `connectOverCDP` fully drives the debug webview (asserted counter increment + dark-toggle on device; live dev-server content proxied behind `tauri.localhost`); web + Android e2e every task, one release smoke at phase end; recipe + constraints in ui-work-loop Findings, probe `end2end/android-cdp-check.mjs`
- [x] Work-loop skills + permissions: `.claude/skills/ui-task-loop`, `e2e-suite`, `android-smoke` (+ `scripts/smoke-android.sh`), shaped by the spike outcome, mirrored into `.agents/skills/`; permission allowlist added to `.claude/settings.json` (diff surfaced for maintainer approval) — prereq: the spike (specs: [ui-work-loop](ui-work-loop.md)) — **done**: three skills landed with matrix path 1 baked in (Codex command names verified against the installed plugin), `smoke-android.sh` for the phase-end release smoke, mirrors byte-identical, allowlist exactly as specced (flagged in the PR for maintainer review); drive-by: realigned the stale `validate` skill with validate.yml (native-backend line, hosted bench)
- [x] E2E baseline reset: delete the counter suite; pre-seeded verified test user on the Neon dev branch + login fixture (email verification is ON — naive signup hangs on OTP); tier tags (`@fast` chromium per task, full chromium+firefox+webkit at boundaries); keep the `.mjs` probes as the probe layer (specs: [ui-work-loop](ui-work-loop.md)) — **done**: tiered config + storageState login fixture + baseline smoke (SSR asserted request-level); `seed-e2e-user.sh` idempotent from any state (fresh-checkout recreate verified); fast tier 4/4, full tier 7/7; Codex review 3 fixed / 1 disputed; found the release-build→watch asset-clobber hydration trap (e2e-suite skill + Findings)
- [x] Dev seed data: script building the test user's realistic tree via existing `CollectionStore` methods (Inbox + nested binders + a deck with commander/boards + holdings/desires across the POC catalog) — prereq for any `/my/*` screen (specs: [app-ui](app-ui.md)) — **done**: `app/src/seed.rs` (debug+hosted only) + `server --seed-dev <uuid>` + `scripts/seed-dev-data.sh`; every write through the real store methods (search→detail→first printing); sentinel idempotency + cleanup-on-error; ran against dev (5 collections / 29 copies / 4 desires / 21 moves / commander tag); Codex review 4 fixed (incl. release-binary exposure → debug_assertions gate) / 1 disputed (see app-ui Findings)
- [x] Dark palette + token migration: `.dark` OKLCH block in `style/input.css`; migrate hardcoded-hex pages (home remnants, auth_pages) onto tokens; vendor `theme_toggle`; class-on-`<html>` + persistence per the theme-persistence decision (specs: [app-ui](app-ui.md)) — **done**: full Rust/UI token set both modes, dark default via SSR-read `tr_theme` cookie on `<html>`, theme_toggle vendored (cookie-initialized both sides — no flash) + live bench section, HomePage/auth_pages hex-free (bounce-page raw HTML exempt); verified web (6/6 probe, hydration clean, fast tier) + Android webview on-device; Codex review 3/3 accepted+fixed (app-ui Findings)

### Stage 1 — component vendoring (vendor-component skill; bench section same commit)

- [x] Vendor batch V1 — static primitives: `button`, `input`/`input_group`, `badge`, `kbd`, `separator`, `checkbox`+`label`, `toggle_group`, `breadcrumb`, `skeleton`, `card` (specs: [app-ui](app-ui.md), [ui-components](ui-components.md)) — **done**: 11 components + bench sections in one commit; `variants!` hand-expanded (no leptos_ui/paste), undefined-token variants dropped, icons inlined, label peer-class upstream bug fixed as deviation; verified web (bench-check CLEAN incl. new interaction asserts, fast tier 4/4) + Android on-device; Codex review: 4 refuted with SSR evidence, 3 fixed, 2 disputed (app-ui Findings + gap-analysis adoption notes)
- [~] Vendor batch V2 — overlay foundations: `use_scroll_lock` asset, `dialog`, `popover`, `sheet`; deterministic caller-supplied IDs (no `use_random_id`), Leptos-owned open state, ESC-listener cleanup; verify CSS anchor positioning on the Android webview, JS fallback if unsupported (specs: [app-ui](app-ui.md), [ui-components](ui-components.md))
- [ ] Vendor batch V3 — `command` (rewired fully reactive — the shared core of quick-add / destination picker / ⌘K), `hover_card` (hover-intent timers in Leptos), sonner per the engine decision (specs: [app-ui](app-ui.md), [ui-components](ui-components.md))

### Stage 2 — shell & public surface

- [ ] App shell + routing: `/` auth redirect, route skeletons for the full map, top-bar mode switch, sidebar rail frame, mobile bottom tabs, `/my/*` auth-guard wrapper; delete the counter + `get_count`/`increment_count`/`storage`; fold the `/cards` placeholder into the shell; rewrite the shell e2e smoke — **stage boundary** (specs: [app-ui](app-ui.md))
- [ ] Catalog page `/catalog`: `search_catalog` server-fn adapter (the exemplar) over `CatalogStore::search`; query bar with URL-canonical `?q=` state, debounce + stale-discard, SSR first page; grid/list toggle; card tiles (lazy images, skeletons) + list view on the vendored table; anonymous variant with sign-in prompts on quick actions (specs: [app-ui](app-ui.md), [catalog-search](catalog-search.md))
- [ ] Filter rail + query↔rail sync: rail widgets two-way-bound to the grammar (comma-OR serialization; unrecognized terms preserved verbatim); mobile slide-over sheet with active-filter badge (specs: [app-ui](app-ui.md), [catalog-search](catalog-search.md))
- [ ] Card detail `/cards/:id` + previews: full page (printings, rulings, your-copies when authed), desktop `hover_card` preview, touch bottom-sheet with expand; includes the multi-face image projection fix (`COALESCE` with `faces->0`) in the hosted summary/detail projections (specs: [app-ui](app-ui.md))
- [ ] Destination picker + Want/Have quick actions + undo toasts: sticky `popover`+`command` picker on catalog surfaces, add adapters over batch-add, sonner undo wiring — **stage boundary** (specs: [app-ui](app-ui.md))

### Stage 3 — My-cards surface

- [ ] Collection tree, read-only: nesting, per-node collapse, pinned system rows, selection, rolled-up count badges; tree server-fn adapter (specs: [app-ui](app-ui.md))
- [ ] Collection tree, management: drag reparent/reorder, context-menu create/rename/delete with dialog confirms, cycle-guard error surfacing (specs: [app-ui](app-ui.md))
- [ ] Count stepper (custom): hover/focus-revealed `− n +`, click-to-type, keyboard ±, commit-on-blur; optimistic update + undo toast; standalone bench section (specs: [app-ui](app-ui.md))
- [ ] `/my` All cards view: aggregate table with three-count columns + expandable location summary, quick search, keyset paging (specs: [app-ui](app-ui.md))
- [ ] `/my/collections/:id` binder/deck view: header counts + needs chip, folder rows above cards, deck variant (format/commander header, type groupings, Want-led default, teardown), stepper in place, mobile drill-down (specs: [app-ui](app-ui.md))
- [ ] Quick-add panel: `command`+`popover`+`input`+`kbd` composite; keystroke contract `↑↓ ⏎ ⇧⏎ ⌥⏎`; Want-led in decks, Have-led elsewhere; typeahead adapter; e2e asserts the keystrokes; time-to-enter-50 observations recorded (POC-catalog realism deferred) (specs: [app-ui](app-ui.md))
- [ ] Selection tray + move flows: custom docked tray, cross-view selection, batch move with suggested destinations, teardown flow, mobile docking above the tab bar — **stage boundary** (specs: [app-ui](app-ui.md))
- [ ] Needs view + pick list + `/my/shopping`: owned-elsewhere vs short buckets, Pull / Pull-all → pick list grouped by source (client-composed from `move_cards` + `suggested_destinations`), shopping list page with text export (specs: [app-ui](app-ui.md))
- [ ] ⌘K command palette: `command`+`dialog`, desktop logged-in only, Places + fixed 3-command registry per design/command-palette.md (specs: [app-ui](app-ui.md))
- [ ] Polish + full pass: empty/error/loading states everywhere, responsive audit against all 9 wireframe screens, full e2e tier + Android release smoke, resolve any `@flaky` quarantine; mark app-ui and ui-work-loop implemented — **stage boundary** (specs: [app-ui](app-ui.md), [ui-work-loop](ui-work-loop.md))

## Later / parked (not in the queue — promote to a phase before working)

### Catalog updates

- [ ] collection-api → keyset for needs / shopping-list (follow-up). `needs(collection)` and the global `shopping_list()` currently return full result sets — bounded in practice but unpaginated. If profiling at real scale shows them hot, add keyset paging like the other reads (specs: [collection-api](collection-api.md))
- [ ] catalog-ingestion → full load + update flow. Full Scryfall bulk import (bootstrap; thereafter the manual rebuild/true-up that also refreshes prices), then the manifest-driven daily incremental (`/cards/manifest` sweep → `/cards/collection` hydration) on a Render cron job + Scryfall migrations reconciliation, all idempotent; mark catalog-ingestion implemented — prereq: the POC subset (specs: [catalog-ingestion](catalog-ingestion.md))
- [ ] card-tagging → per-card tag orphan cleanup (follow-up). When a card's **last** holding and desire leave a deck (quantity → 0 via the stepper / a move / teardown of a single line), drop its now-orphaned `card_tags` rows. Whole-collection teardown is already covered by the `ON DELETE CASCADE` on `card_tags.collection_id`; this is the per-line case on the holdings/desires write paths, surfaced only if a UI shows stale tags (specs: [card-tagging](card-tagging.md), [collection-api](collection-api.md))
- [ ] Bundled read-only catalog for offline browsing on desktop/mobile (deliberately deferred)

### Other

- Full-catalog quick-add disambiguation + list-perf validation — Phase 5 builds against the ~3K-printing POC subset (maintainer decision 2026-07-17), so type-ahead disambiguation realism and large-result-list performance can't be judged until the full ingest (parked above) runs; re-exercise the quick-add and catalog screens then
- Validate the add-flow keystroke budget against real usage (disambiguation frequency at 4–6 typed chars; ⇧⏎ set-count for playsets) — needs the implemented add flow; projected numbers in ui-design Findings
- Large-collection aggregate performance — profile owned / present-rollup / needs at catalog-scale collections (~100K rows) and, if hot, promote `owned_by_card` to a materialized view + add collection-read indexes (data-model's named escape hatch). Needs the implemented collection endpoints + real-scale data ([collection-api](collection-api.md) §Read models, [data-model](data-model.md))
- Decks and sharing features
- Import/export (CSV, Moxfield)
- App-update delivery, Android — **Play Store internal-testing track** (maintainer decision 2026-07-13): $25 one-time dev account, CI upload (fastlane or a gh-action), Play provides update notifications + in-place installs; adopt when the first non-maintainer tester appears. Version plumbing already ships in artifacts.yml (`versionName 0.1.0+<sha>`, monotonic `versionCode`). Interim sideload polish (versioned release tags + Obtainium) evaluated and declined; a bespoke in-app APK updater rejected outright (Play makes it throwaway)
- App-update delivery, desktop — **tauri-plugin-updater, deliberately paired with the deferred Developer ID + notarization work** so the signing pipeline is touched once (notarization recipe in [delivery-pipeline](delivery-pipeline.md)): minisign update key as a repo secret, CI publishes `latest.json` + `.app.tar.gz` + `.sig` beside the `.dmg`, in-app check → prompt → swap → relaunch. This is the long-term update path for direct-download macOS distribution — only a Mac App Store listing would supersede it

## Decisions log

- 2026-07-16: **catalog-search spec reviewed + accepted — flat term grammar, substring SQL, name-order keyset.** V1 syntax = flat AND-of-terms (bare/name, `o:`, `t:`, `s:`, `r:`, `c:`, `id:`, `mv:` with comparisons), per-term `-` negation, and a **comma-OR micro-extension** (`r:rare,mythic`) — it's what the rail's multi-select facets serialize to, since flat Scryfall syntax can't express within-facet OR; no `or`/parens (real grammar, deferred). **Substring semantics everywhere** via trgm ILIKE; a generated `cards.oracle_search_text` column (+ GIN) closes data-model's multi-face `o:` gap; `t:` gets a type_line trgm GIN; `c:` handles multi-face via jsonb containment on `card_faces` (top-level colors is empty on DFCs by design). **Printing-scoped terms (`s:`/`r:`) share one EXISTS** (Scryfall's per-printing semantics); negated ones get their own NOT EXISTS. **Unknown syntax errors, naming the term** (never silently-wrong results); the "N advanced terms" rail indicator is dropped for v1. **Order = (name, oracle_id)** — Scryfall's own default and the shell's existing keyset — correcting collection-api's "relevance, then name" line (note added there); relevance deferred to an `order:`/tsvector extension. Reviewed and implemented **stacked on the ingestion-POC branch** (maintainer: rebase-merge, both PRs ride together) so the engine develops against the live dev subset.
- 2026-07-16: **catalog-ingestion spec reviewed + accepted — manifest-driven dailies, bulk for bootstrap/true-up, Render cron.** Facts verified live against Scryfall/Neon/Render docs and APIs (recorded in the spec): bulk files are now gzipped JSONL (streamable line-by-line); the `*.scryfall.io` bulk origins are rate-limit-free; every `api.scryfall.com` call needs an accurate `User-Agent` (403 without); upstream prices change once/day, period. Design: **two acquisition paths over one shared core** — the `default_cards` bulk path (POC filter → full bootstrap → occasional *manual* true-up that also refreshes all prices; hash-gated upserts so true-ups write only deltas) and the **daily incremental** (`/cards/manifest` sweep, 15,000 entries/page ≈ 8 pages ≈ 1 min → hydrate changed/new ids via `/cards/collection` 75/req → same upserts; new-card detection = unknown ids, the daily payoff). **Price freshness is an explicit non-goal** (maintainer): prices ride hydration + true-up cadence; no daily all-cards price job, and no daily 560 MB reprocessing. **Scheduling = a Render cron job** (~$1/mo minimum, at-most-one-run guaranteed, same root `Dockerfile`, command `server --ingest update`; the `catalog_ingest` credential lives *only* there — the web service keeps `app_runtime` only). Chosen over the same-day pg_cron due-marker idea (verified: no `http`/`pg_net` on Neon, ticks fire only while the compute is awake → usage-proportional freshness) and over GitHub Actions (standing no-DB-creds-in-GitHub rule). Scryfall `/migrations` reconciled catalog-side only (`merge`/`delete` iff unreferenced); user-row repointing is a manual owner runbook (RLS-forced tables; Neon can't mint `BYPASSRLS`). Manifest observation 2026-07-16: `data_updated_at`/`created_at`/`oracle_id` NULL on all sampled entries (maintainer's read: NULL is the steady state — cards rarely change); the diff reads them whenever present. New ingestion-owned migration queued with the work: `ingestion_runs`, `ingest_hash` + `manifest_*_updated_at` columns, `catalog_ingest` grants.
- 2026-07-16: **card-tagging tag/board API implemented — spec `implemented`.** The
  eleven-method tag/board surface landed on the `CollectionStore` trait (tag CRUD +
  scope, `assign`/`unassign`, `card_tags`/`cards_with_tag`, `deck_commanders`,
  `set_holding_board`/`set_desire_board`), each a `shared/tags.rs` DTO implemented
  once by `HostedBackend` and mirrored by `NativeBackend` over new hosted JSON
  routes. Decisions settled in implementation: **tag scope is derived** from the
  two nullable FKs (`TagScope::from_fks`), never a stored enum; **assignment
  enforces** deck-tag containment + card-in-deck (`Validation`) + the built-in caps
  (`commander` ≤ 2 / `companion` ≤ 1, counting *distinct* oracles and **excluding
  the one being assigned** so re-assign is idempotent), with **full
  legal-commander / companion-restriction validation deferred to a rules engine**
  (cap-only v1, matching the spec's lean); **deck color identity is derived on
  read** (WUBRG union of the commander-tagged cards' `color_identity` — no stored
  column, so "recompute" is inherent); **boards re-label in place** (upsert the
  destination-board row + `take_or_delete` the source, splitting/merging by
  quantity — **not** a `moves` entry) and are **rejected on binders** at the API
  (`Validation`), not just hidden in the UI. Proven end-to-end against the Neon
  **dev** branch with a temp driver (seed → exercise every path → assert → clean
  up, reverted). Per-card orphan `card_tags` cleanup on the last line leaving a
  deck filed as a follow-up (whole-collection teardown already cascades). Also
  closed the **data-access-backends native `401` silent-refresh + offline
  behavior** follow-up: `NativeBackend` now re-mints from `tr_session` on a `401`
  and retries once, with `Upstream` (unreachable) vs. `Unauthorized` (auth) made
  type-distinct (its own PR).
- 2026-07-16: **collection-api implemented — the full endpoint surface, both backends, verified on dev.** Built as six vertical slices, each proven end-to-end against the Neon dev branch before commit: tree CRUD + lazy inbox; holdings/desires writes + batch; CollectionView reads; moves (single/batch, undo, teardown, suggested-destinations); global reads (all-cards/needs/shopping); catalog (detail/summary/search shell). Every operation is a `CatalogStore`/`CollectionStore` trait method with a `shared/` DTO, implemented once by `HostedBackend` (sqlx) and once by `NativeBackend` (the HTTP client of explicit hosted JSON routes; paths in one shared module so they can't drift). Decisions settled in implementation: **endpoint surface = the explicit routes** (no per-op Leptos server-fns — those are a per-screen UI adapter, ride with the UI tasks); **`CardRow` grain = (printing, board)** with owned = global per-oracle, rollup = descendants per printing, desired = per (oracle, board); **moves are board-agnostic** (mainboard — board re-labels stay card-tagging's); **keyset pagination** on the large reads (opaque cursor + limit+1), **needs/shopping unpaginated** (bounded; follow-up filed); **catalog reads auth opportunistically** (HeaderMap→user_id, else anonymous); **ownership guard** validates the target collection is owned beneath the `holdings`/`desires` RLS (which only gates on their own `user_id`). Pull/pull-all are client-composed from `move_cards` + `suggested_destinations`. sqlx gained `uuid`+`json`. Full merge gate green in-container.
- 2026-07-16: **Data-access trait layer implemented; `ssr` decomposed into a substrate + two backends.** The `app` crate's `ssr` feature no longer *implies* Postgres. It is now the embedded-server **substrate** (router + auth core); a complete server build layers on exactly one **backend** — `hosted` (sqlx against Neon, the authorization terminus; what `server` builds) or `native` (an HTTPS client of the hosted API, **no sqlx linked**; what `src-tauri` builds), each implying `ssr`, with a `compile_error!` if neither is set. This retired the spike debt where the Tauri shell shipped the Postgres path. New **`shared/`** workspace crate holds the cross-backend DTOs + the one `ApiError` enum (wasm-safe: no sqlx/axum). Per-domain traits (`CatalogStore` anonymous / `CollectionStore` session-scoped), one backend struct per target carrying the per-request session credential; the hosted side runs every session query inside a `set_config('app.user_id',$1,true)` transaction so RLS scopes it beneath the terminus. Endpoints = trait methods projected to HTTP: the web UI calls in-process server fns, the native client calls explicit hosted JSON routes. Two CI consequences: a dedicated `cargo clippy -p app --features native` line (workspace feature-unification makes `hosted` win the backend cfg, masking the native-only arms that ship in the APK/`.dmg`), and the component-bench clippy line moved from bare `ssr` → `hosted`. **Prod migration deferred to post-deploy** (new code's `count(*) FROM cards` is compatible with both schemas, so migrating prod right after Render redeploys has no break window) and the native `401` silent-refresh stubbed (rides collection-api's native client) — both filed as follow-up tasks. sqlx gained the `uuid` feature.
- 2026-07-15: **Card annotation model added ([card-tagging](card-tagging.md) spec); `deck_commanders` superseded.** Reviewing the commanders table uncovered a broader gap: a deck needs many per-card categorizations — commander, mainboard/sideboard/maybeboard, companion, and user-defined labels (e.g. "Draw spells") at per-deck **and** per-account scope — a tagging problem the point-solution `deck_commanders` table didn't generalize (derived groupings by type/CMC/color stay read-time `GROUP BY`s, unstored). Split by cardinality: **boards** are a quantity-bearing partition → a `board card_board` column on `holdings`/`desires` (per-slot *physical-copy* allocation — "Option C" — was **rejected**: aggregate per-board quantity suffices, e.g. "2 Bolts main + 1 side; own 4, 3 in this deck"); **labels** (commander/companion/custom) are a `tags` + `card_tags` many-to-many, tags scoped system/account/deck via nullable `user_id`/`collection_id`, assignments anchored at `(collection, oracle)` so they span holdings+desires. Commander/companion become built-in (system) tags with app-enforced caps (≤2 / ≤1); commander retires `deck_commanders`. New spec `card-tagging` (`draft`) owns the design; [data-model](data-model.md) + [collection-api](collection-api.md) carry pointers + the endpoint surface. Two Phase 4 tasks added, **gated on card-tagging** — blocked until review accepts it (the queue's signal to review it next). Migration is expand-first (board defaults; `deck_commanders` empty).
- 2026-07-15: **Data-layer specs reviewed; Phase 4 recast from spec-fleshing to implementation.** Collaborative spec-review (2026-07-14) fleshed out and accepted the data-access/API stack: [data-access-backends](data-access-backends.md) (per-domain traits, one backend struct per target; owns the `shared/` types crate + the cross-backend error enum) and [collection-api](collection-api.md) (endpoints = the trait methods projected to HTTP; keyset pagination on *both* catalog search and collection reads, after the maintainer noted a collection can approach catalog scale ~100K; undo=flag, lazy inbox, app-side reparent cycle check, per-line batch add). [catalog-search](catalog-search.md)'s dependency was corrected — it depends on **data-model** (queryable-field vocabulary + base indexes), not catalog-ingestion, which is a *runtime* sibling; its SQL-vs-proxy OQ was already answered by collection-api (**SQL against our ingested catalog**), which put [catalog-ingestion](catalog-ingestion.md) on the critical path (POC representative-subset first, then full load + update flow). Workflow correction that drove the Phase 4 rewrite: "flesh out spec X" is the README's *blocked-queue spec-review meta-action*, **not** a queue task (no completed task in Phases 0–4 uses that pattern); and accepted→implemented is an explicit `mark <spec> implemented` step on a spec's terminal task (Phase 1/4 precedent), not an inferred transition. Phase 4 (renamed "data & API layer") now lists implementation tasks gated on `accepted` specs; still-draft specs (catalog-search, catalog-ingestion, ui-component-bench) keep blocked tasks until review accepts them.

- 2026-07-15: **ui-component-bench spec fleshed out; task repositioned as parallel UI infra.** The bench's second purpose — runtime SSR/hydration verification — was elevated to co-equal with styling review: the Phase 1b gap analysis reviewed the six interactive components (`dialog`/`popover`/`command`/`hover_card`/`sheet`/`sonner`) at source only and deferred runtime checks here, flagging four cross-cutting hazards (`use_random_id` SSR/client ID divergence, `window.ScrollLock`/vendored-asset dependence, `leptos_ui` nightly, and CSS-anchor positioning that silently breaks in WKWebView / Android WebView). The spec now carries a concrete per-adoption verification checklist and makes native-webview reachability a hard requirement. Decisions: gating = a dev-only **`component-bench` cargo feature** (over `debug_assertions`, so it can also be switched on in a local release build to exercise the embedded-Axum release SSR path); **build in parallel** — the task is independent of the blocked data specs (gated only on its own spec, renders just `table` today, value compounds with adoption), so it moves to lead the phase, and the "add the bench section in the same commit" convention is meant to be in force before component #2 is vendored; plus a bench-local theme toggle now and one page with anchored sections + jump-nav. Spec stays `draft` pending maintainer review.

- 2026-07-13: **App-update delivery parked; store-track endgame chosen.** Rolling-artifact installs (download + reinstall from the `latest` prerelease) stay the dev workflow — post-auth work is data layer + UI, mostly verifiable on the emulator or a locally built `.dmg`. When update delivery matters: Android goes straight to the **Play internal-testing track** (no interim sideload polish); desktop adopts **tauri-plugin-updater**, bundled with the deferred Developer ID + notarization work so the signing pipeline is touched once. Both queued in Later/parked with the details. The versioning prerequisite for every path already ships in artifacts.yml.
- 2026-07-13: **Android Google OAuth returns via deep link, not loopback.** The on-device test showed Chrome timing out on the embedded server's callback: Android freezes the backgrounded app, so the loopback-callback pattern (fine on desktop, per RFC 8252) can't work there. Android now sends the public web origin's `/auth/app-return` as the OAuth callback; that page bounces the verifier into the app via the `three-rings://` scheme (`tauri-plugin-deep-link`; intent-filter build-time-injected from `tauri.conf.json`), and the app completes the challenge/verifier exchange in-process once foregrounded. Upstream rejects custom-scheme callback URLs outright (`INVALID_CALLBACKURL`, probed live), which forced the web-origin bounce. Desktop flow unchanged. Details in specs/auth.md Findings.
- 2026-07-13: **Auth shipped — path-B cookie proxy over Neon Auth (Better Auth), Google included; verification on.** The browser/webview only ever holds our origin's httpOnly cookies (`tr_session` upstream session, `tr_jwt` 15-min EdDSA JWT, `tr_challenge` OAuth window); the server proxies the auth REST API and re-mints JWTs silently. Google stays server-side via the verifier/challenge exchange (mechanism excavated from Neon's own SDK — the naive browser-side `/token` bridge dies on partitioned third-party cookies). Toggles (maintainer): `require_email_verification=true` (OTP method — code-in-app works identically in webviews) + Google (shared creds), both branches; that single toggle both gates sign-up sessions and auto-mails the OTP. Two latent bugs found: the step-5 verifier rejected every real token (jsonwebtoken v9 needs the expected `aud` configured; `iss`/`aud` = the auth origin), and a unit `Suspense` fallback (`|| ()`) desyncs leptos hydration app-wide (never ship `fallback=|| ()`). Tauri desktop spike PASS — the full gated flow works inside WKWebView against the embedded server; Google-in-webview is impossible (Google policy + upstream callback rules), so desktop Google = system browser + loopback callback, queued as a Phase 3 task. `SET LOCAL app.user_id` still rides the data-model per-request transaction.
- 2026-07-12: **Dockerfile = cargo-chef dep caching; Render build cache verified persistent.** The delivery Dockerfile now cooks dependencies in layers keyed only on the manifests/lockfile (two cooks matching cargo-leptos 0.3.7's real invocations: native `server-release`, wasm `release` into `target/front`), so code-only pushes reuse them — local Docker measured cold 137s → warm 9s. Frontend tools (tailwindcss v4.2.1, wasm-opt/binaryen version_123, wasm-bindgen 0.2.103) are pinned onto PATH in the image because cargo-leptos prefers PATH tools over downloading — removes per-build GitHub downloads *and* the unpinned "latest" API checks (rate-limit risk on shared builder IPs); the wasm-bindgen ARG must track the Cargo.toml `=0.2.103` pin. mold added for native links (scoped via `$CARGO_HOME/config.toml`; identical flags for cook + build keep fingerprints stable). The spec's Render-cache caveat is resolved: deploy build logs show a registry cache-manifest import with `CACHED` build-stage layers on an ephemeral builder → persistence confirmed, sccache+S3 stays the fallback if cook layers run cold (follow-up task queued). Also discovered en route: PR #5's squash dropped `e5df220`, leaving main's Dockerfile copying the now-empty `target/release/` path — main deploys were `build_failed`; the standalone fix landed mid-task as PR #6, and this refactor preserves it in the new stage layout.
- 2026-07-12: **Migrations run as a deploy step, not from the app; web server runs as a non-owner role.** Neon (unlike Supabase) has no managed migration runner, so the spike wired `MIGRATOR.run` into server startup — removed. Migrations now run as a **Render pre-deploy command** under `neondb_owner` (owner URL in Render env + local dev only, never GitHub — consistent with "no DATABASE_URL in GitHub"); the running web server connects as the non-owner **`app_runtime`** role (created 2026-07-12 on both branches: CRUD + RLS-subject, no DDL/superuser/bypassrls). One runtime credential, no in-app role juggling. Rotation of Render's `DATABASE_URL` → `app_runtime` happens **after** migrations leave the app-startup path. Only affects the process that talks to Postgres directly (hosted web server + local dev); native clients never hold DB creds (data-access-backends). Work folded into the data-model migrations + data-access tasks.
- 2026-07-11: **Correction — Neon Auth is Better Auth, already provisioned, uuid users.** Live-DB inspection via the Neon MCP (project `steep-scene-29832344`) supersedes the Stack-Auth specifics in the entry below. Neon Auth runs **Better Auth**, **already provisioned on both `production` and `dev` branches**, storing a live `neon_auth."user"` table with a **uuid** id (not a text `users_sync` mirror). Data-model FKs corrected to `uuid … REFERENCES neon_auth."user"(id) ON DELETE CASCADE`; the async-sync/soft-delete caveat was a Stack Auth artifact and is dropped. Config is non-secret per-branch base_url/jwks (dev URL in `.env.example`, prod in Render); **no app secret key**. Specs corrected; the auth task stays `[~]` with the non-owner RLS role + Axum JWKS middleware + Tauri spike outstanding.
- 2026-07-11: **Auth = Neon Auth; a Neon Auth task added to Phase 3 ahead of the data-model migrations** (data-model spec review, maintainer's option 1). Neon Auth (Stack Auth-backed) manages users in `neon_auth.users_sync` (`id text` PK; `email`/`name`/`created_at`/`updated_at`/`deleted_at` soft-delete; `raw_json`), so data-model *references* that table with **text** `user_id` FKs instead of defining its own `users`, and its collection tables can't migrate until Neon Auth is enabled. Resolves auth's roll-our-own-vs-hosted question. RLS stays app-enforced via the `app.user_id` GUC set by the hosted Axum API (the authorization terminus per data-access-backends) — deliberately **not** Neon's Data API / JWT-RLS path. Caveats carried into the auth task: async user sync + soft delete → hard-FK-vs-soft-reference and first-login Inbox provisioning are open.
- 2026-07: API-first on Neon chosen over offline-first Turso designs. Rationale in README.
- 2026-07: Architecture spike prioritized ahead of data model — architecture unproven.
- 2026-07: Spec numbering dropped; filenames are the stable IDs, this file owns execution order.
- 2026-07: Tasks gated on spec status via `(specs: ...)` annotations; only humans accept specs.
- 2026-07: Spike decisions: web = local run only; mobile = Android; mobile SSR failure = stop and reassess; no time-box; Android gate moved ahead of DB work (fail fast).
- 2026-07: Scaffold base = tauri-leptos-ssr (embedded in-process Axum matches the README); start-tauri-fullstack rejected (thin shell → external server, csr default). Rationale in architecture-spike.md Findings.
- 2026-07: Dev environment = Docker devcontainer. Image `dgoings/three-rings` (`.devcontainer/Dockerfile`, layered on `dgoings/magic-assistant-dev`) carries the Rust/Leptos/Tauri toolchain so the host stays toolchain-free. All Rust dev + the web target build/run in the container; the Android *build* is containerized; macOS desktop + iOS and the Android *run* (emulator/device) are host-side.
- 2026-07: Consequence of the devcontainer split — macOS desktop (Phase 1) is deferred behind the Android gate, built later via a CI macOS runner or a minimal host install (keeps the host toolchain-free until the architecture is proven). Android SDK/NDK are added to the image as a second layer just before the gate.
- 2026-07-07: **Android build moved host-side** (reverses the containerized-Android-build part of the devcontainer decision). Google ships no linux-arm64 NDK — official Linux tooling is x86_64-only — so the planned Android layer on the arm64 image cannot work; an amd64-under-Rosetta image variant was viable but was passed over for the host toolchain (Android Studio SDK/NDK/JBR + brew rustup + binstalled cargo-leptos/tauri-cli). Web dev stays in the container; the "toolchain-free host" goal is relaxed to "Rust via brew rustup, no ad-hoc curl installs".
- 2026-07-07: **Android architecture gate passed** on the Samsung Flip 7 emulator — release APK, embedded in-process Axum serving SSR + hydration + server fns. Android-specific fixes (cleartext/signing in gradle, APK-asset extraction, on_page_load navigation) recorded in architecture-spike.md Findings; `src-tauri/gen/android` is now committed since it carries the gradle config.
- 2026-07-07: **macOS desktop built host-side** (resolves the parked CI-vs-host question) — the toolchain was already on the host from the Android gate; Xcode supplies the platform bits.
- 2026-07-07: **Phase 1 complete — architecture spike passed on all targets**; architecture-spike.md marked implemented, carry-forward items in its Conclusion. Neon `DATABASE_URL` lives in `.devcontainer/.env` (host shell exports it for native desktop runs).
- 2026-07-09: **Web deploy platform = Render** (maintainer decision at spec review, supersedes the Railway draft choice below; Fly.io fallback).
- 2026-07-09: **Repo is public** (was: private in the draft) — discovered at setup: the free-plan org can't enforce branch protection or auto-merge on a private repo, and the merge gate depends on both; public unlocks them at zero cost. Side effects: the rolling release needs no GitHub login from the phone, and GitHub-hosted minutes would be free too (Blacksmith remains the runner choice for speed and its own free tier). Repo lives at `threeringsdev/three-rings`.
- 2026-07-09: **Artifact cadence split by cost, not target** — Android APK per `main` merge (plus dispatch; docs-only pushes skipped via `paths-ignore`) keeps the phone-checkable rolling release fresh; macOS `.dmg` is `workflow_dispatch`-only from the start ($0.08/min would exhaust the Blacksmith free tier in ~15 merges; its consumer is at a Mac anyway, and pre-merge desktop checks happen as local host builds). Per-target path filtering rejected: the shared `app` crate means most code changes alter every artifact.
- 2026-07-09: **CI runners = Blacksmith** (maintainer decision, supersedes the GitHub-hosted draft; drop-in `runs-on` labels, any job reverts to GitHub-hosted independently). Consequence: the repo must be owned by a free GitHub organization — Blacksmith doesn't install on personal accounts. Budget framing changes from GitHub's 2,000 min / 10×-macOS multiplier to Blacksmith's 3,000 free min/month (2 vCPU x64 basis; macOS M4 $0.08/min), plus GitHub's $0.002/min control-plane fee on all Actions minutes (in effect since 2026-03).
- 2026-07-07: **Phases reorganized — delivery pipeline before data model/UI** (new Phase 2, specs/delivery-pipeline.md; foundations becomes Phase 3). Goal: agents work autonomously from any host with the repo as the contract; results checkable remotely. Decisions: GitHub private repo; two-tier CI (validate per-push linux-only; artifacts on `main` merges + manual — private-repo minutes, macOS 10×); one keystore-as-secret for in-place APK updates; rolling `latest` prerelease; web deploy = Railway building a repo Dockerfile (Render fallback; zero Actions minutes); no `DATABASE_URL` in CI. Android-in-CI uses NDK r28+ (x86_64 runners sidestep the linux-arm64 NDK gap and close the 16 KB finding).
- 2026-07-11: **Phase 2 non-deploy work landed** — validate + artifacts CI, rolling `latest` release, self-sufficiency docs (root `CLAUDE.md`, `.devcontainer/README.md`, `scripts/gen-android-keystore.sh`), Neon `dev` split (PR #1 + follow-up fixes on `main`). First real CI surfaced: (1) `useblacksmith/rust-cache` runs `cache-bin: true`, which cleans `binstall`'ed binaries out of the cached `~/.cargo/bin` on save but keeps `.crates2.json` → on a cache-hit run `binstall` skips `cargo-leptos` ("already installed") and `cargo leptos` is missing at build time → fixed with `binstall --force` in both workflows. (2) Node 20 action deprecation → bumped `actions/checkout`→v7, `actions/setup-java`→v5, `android-actions/setup-android`→v4; `useblacksmith/rust-cache` stays v3 (Blacksmith fork has no node24 build yet — the lone remaining warning). (3) Release signing/versioning needed a backward-compatible edit to `src-tauri/gen/android/app/build.gradle.kts` (release `signingConfig` from `keystore.properties` + version env, falling back to debug/`tauri.properties` for local builds). (4) Neon's default branch is `production` (not `main`); `dev` was branched off it. Deferred (out of this phase's scope): web deploy (Dockerfile + Render) and the loop's web/`.dmg` legs.
- 2026-07-11: **Web deploy live on Render.** Root `Dockerfile` (multi-stage: `rust:1-bookworm` runs `cargo leptos build --release` → `debian:bookworm-slim` carries the `server` binary + `target/site` + `ca-certificates`; 114 MB) merged via PR #2 and auto-merged on green — proving the full agent loop (branch → PR → validate gate → auto-merge). TLS backend is `ring` (no openssl/cmake build deps); `cargo-leptos` pinned to 0.3.7 for host/CI parity. Runtime config is entirely env-driven (`get_configuration(None)` reads `LEPTOS_SITE_ROOT=/app/site` etc. from the image's `ENV`; entrypoint maps Render's `$PORT` → `LEPTOS_SITE_ADDR=0.0.0.0:$PORT`). Verified locally (Docker vs Neon **dev** branch) and live at https://three-rings-6p5o.onrender.com (Docker runtime, auto-deploy on `main`, free tier w/ spin-down, Ohio region): `/` SSRs, `/cards` renders the 3 seeded **production**-branch rows. The service's only manual env var is `DATABASE_URL` (Neon production, direct host). Aside: the Render `create_web_service` MCP tool can't create Docker services, so the service was created in the dashboard (briefly in the wrong workspace, then recreated under the personal team). Remaining Phase 2: the end-to-end loop proof (web leg now done; `.dmg` dispatch still pending).
- 2026-07-11: **macOS `.dmg` signing = staged.** The downloaded `.dmg` read as "damaged" because the bundle was only linker-signed (invalid seal). Now: `bundle.macOS.signingIdentity = "-"` for a *valid* ad-hoc signature → opens on any Mac via a one-time right-click → Open (not notarized). Later: Developer ID Application + App Store Connect API-key notarization for a zero-prompt download — **blocked**, only the org's Apple **Account Holder** can create a Developer ID cert (maintainer's roles cover Apple Development / Distribution / Mac App / Installer — none notarize a downloadable `.dmg`). Mac Development + registered devices rejected (doesn't remove the prompt; restricts to registered UDIDs). Full drop-in recipe recorded in the spec's "macOS `.dmg` signing" section.
